# Agent Lessons

记录会复发的 agent 失败模式、修法和门禁状态。成功且没有新经验时不记录。

## Gated

- 2026-05-10 v4.32: 方案 B block 曾漏检 `test_lines` 行尾逗号和 `console_log` 包装。已进 `.claude/content-block-rules.js`，并新增 bad fixtures: `missing-test-line-commas.json`、`bad-console-log.json`。

## Documented

## Observed
