# 墨祟：走阴录 — 战斗试玩 技术文档 v4.11

> 开发接手请先读 `DEVELOPMENT.md`。本文保留完整技术细节、版本历史和 Bug 追踪。

## 一、项目概况

一个纯前端的俯视角动作肉鸽原型，用 HTML5 Canvas 2D 渲染，没有框架依赖。

单局流程：选武器 → 波次战斗（9波，含关卡调制器）→ 波次间选遗物/进化 → 打Boss → 结算。

所有文件放在同一目录，双击 `game.html` 即可运行。


## 二、文件清单

| 文件 | 职责 |
|------|------|
| `game.html` | 游戏入口。武器选择、Canvas、HUD、遗物弹窗、暂停、结算的 DOM 结构 |
| `game.css` | 游戏专用样式。HUD、武器/遗物卡片、遮罩层、波次动画 |
| `styles.css` | 全站公用样式。配色变量、字体、纸张质感背景 |
| `gamedata.js` | 共享数据文件。武器/遗物/进化/波次/敌人/前置条件/封顶值等全部常量，game.html 和 wiki.html 共同引用 |
| `game.js` | 主游戏逻辑。IIFE 包裹，严格模式，约 4220 行 |
| `sound.js` | Web Audio API 合成音效。无音频文件，全部程序生成 |
| `mobile-controls.js` | 移动端虚拟摇杆。触摸检测+双摇杆+自动瞄准+闪避/暂停按钮+横屏锁定，canvas 绘制 |
| `app.js` | 首页 `index.html` 的构筑预览逻辑（与游戏无关） |
| `wiki.html` | 百科页面。自动读取 gamedata.js 渲染武器/遗物/进化/敌人/波次/构筑线/封顶值 |
| `index.html` | 概念展示首页，含导航至试玩和百科 |


## 三、核心架构（game.js）

整体是一个立即执行函数，内部用闭包维护游戏状态。

```
gamedata.js (共享数据，game.html/wiki.html 均加载)
├── 常量：画布尺寸(W/H/A)、调色板(C)
├── 数据表：WEAPONS(5)、RELICS(85)、EVOLUTIONS(25)、WAVES(动态)、ETYPE(28)、STAGE_MODS(9)、CURSES(19)、ACHIEVEMENTS(31)
├── 规则：PREREQS(32件前置条件)、CAPS(7项封顶值)、LIMITS(10类对象上限)、RANGES(17项距离阈值)、TUNING(70项可调参数)、WAVE_SCALE(2项波次缩放)
├── 映射：DEATH_COLOR、PCOL、BUILD_PREFS、JUDGMENTS

game.js (IIFE，约3680行)
├── 工具函数：dstSq（主用）/dst（仅需要实际距离时）、ang、cl、rn/ri、pick、shuf、moveScale、pushLimited
├── 性能：perfPressure、perfMul、markPerf
├── 对象入口：addFire、addEProj、pushAttack、spawnJudgment、mkMinion
├── 判词系统：spawnJudgment()
├── 游戏状态工厂：mkPlayer()、newGame()
├── 核心循环：
│   ├── pAtk()    玩家攻击（读stats，生成attack对象，含暴击/蓄力/骨血墨惩罚）
│   ├── hitE()    命中敌人（遗物触发、击杀结算，单次遍历恐惧/魂链/爆裂）
│   ├── hurtP()   玩家受伤（减免、收阴袋护盾、反伤、复活）
│   ├── update()  每帧逻辑（移动、敌人AI、碰撞、计时器、性能采样）
│   └── render()  每帧绘制（分层渲染17层）
├── 遗物选择：RELIC_RULES表驱动 + scoreRelicChoice()、pickRelicChoices()
├── UI 函数：updateHUD、showRelic、showEnd
└── 初始化：事件监听、主循环(requestAnimationFrame)
```

游戏主循环每帧执行 `update() → render()`，状态机控制流程：

```
playing → (清怪) → waveClear → (选遗物/进化) → playing → ... → victory/over
                  ↘ (ESC) → paused → (ESC) → playing
```

游戏循环使用独立 try-catch 保护 update/render/showEnd，防止单帧错误导致循环死亡。render 开头强制 `c.restore();c.save();c.translate(...)` 重置 Canvas 状态，防止偏移积累。


## 四、属性体系（stats）

玩家有一个 `stats` 对象，存放所有可被遗物/强化修改的数值属性：

```javascript
stats: {
  dmg: 1,        // 伤害倍率。武器基础伤害 × 此值
  spd: 1,        // 移速倍率。moveScale() 返回此值+击杀加速buff
  range: 1,      // 范围倍率。影响近战弧半径、弹道射程、AOE半径
  critRate: 0.2, // 暴击率。每次攻击 Math.random() < 此值 即暴击
  critDmg: 1.5,  // 暴击伤害倍率。暴击时 伤害 × 此值
  atkSpd: 1,     // 攻速倍率。冷却帧数 × 此值，越小越快
  multi: 1,      // 多重打击。1=单发，2=双发，3=三发……
  projSize: 1,   // 弹道体积倍率。影响碰撞半径和绘制大小
  def: 0,        // 伤害减免。受伤时 伤害 × (1 - 此值)
  returnInk: 0   // 折返墨迹强度。影响折返弹伤害和自动生成
}
```

**设计原则**：数值遗物改 `stats`，机制遗物改 boolean 标记。两者独立叠加，互不干扰。


## 五、武器系统

四把武器，各有独立的攻击生成逻辑和连段系统：

| 武器 | type | 连段机制 | multi 效果 |
|------|------|----------|-----------|
| 斩妖剑 | melee | 每3击加宽加伤+穿透 | 多刀偏移扇形劈 |
| 符骨笔 | ranged | 每5发大弹(1.75倍体积+1.6倍伤) | 扇形多弹散射 |
| 镇魂铃 | aoe | 连段递增范围(+6%/击) | 多层递减圈 |
| 伏魔伞 | dash | 受伤后反击1.8倍 | 多段偏移冲斩 |

攻击对象在 `pAtk()` 中创建，通过 `addAttack()` 推入 `g.attacks`。`addAttack()` 同时处理折返墨迹生成（`spawnReturnInk`）。


## 六、遗物系统（34件）

### 第一层：数值遗物（改 stats）

| 遗物 | 修改 | 值 |
|------|------|-----|
| 朱砂封钉 | critDmg | +0.4 |
| 青铜镇铃舌 | range | +0.25 |
| 血烛祭片 | range | +0.2 |
| 走马灯片 | spd | +0.15 |
| 纸甲残片 | def | +0.2 |
| 骨血墨 | critRate | +0.15（附带 guxuePenalty 标记） |

### 第二层：机制遗物（设标记，在 hitE/hurtP/onEnemyKilled/pAtk/update 中触发）

| 遗物 | 标记 | 触发位置 | 效果 |
|------|------|----------|------|
| 枯毫遗墨 | tripleBlade | pAtk | 每3攻追加墨刃 |
| 纸人替魄 | decoyHP | hurtP | 替身吸收伤害(30HP) |
| 无字谶符 | soulDmg | pAtk | +5固定伤害 |
| 倒写经卷 | bounce + returnInk | addAttack/update | 弹道40%寿命时折返 |
| 夜灯残烬 | fireOnKill | onEnemyKilled | 击杀留磷火 |
| 祟面香灰 | lowHpDmg | pAtk | 低血增伤+50% |
| 玄冰简穗 | slowOnHit | hitE | 命中减速40% |
| 鬼脊蝶印 | soulChain | hitE | 跳伤附近敌人 |
| 破镜残片 | pierceOnDodge | pAtk | 受伤后穿透 |
| 墨池残砚 | comboDmg | hitE | 连续命中递增伤害 |
| 镇墓兽首 | thorns | hurtP | 反伤30% |
| 引魂灯笼 | killHeal | onEnemyKilled | 击杀回血5 |
| 黄泉引路 | inkTrail | update | 移动留减速墨迹 |
| 傩面碎片 | fearOnCrit | hitE | 暴击恐惧周围敌人 |
| 墨龙珠 | chargeDmg | pAtk | 停顿蓄力增伤80% |
| 铜镜照妖 | weakpointDmg | hitE | 暴击标记弱点+25% |
| 招魂残幡 | revive | hurtP | 死亡复活一次(30%) |
| 骨笛残声 | killAtkSpd | onEnemyKilled | 击杀攻速提升3秒 |
| 走马灯片 | killSpeed | onEnemyKilled | 击杀加速（同时加spd+标记） |
| 血墨混染 | lowHpRange + extraDmgTaken | pAtk/hurtP | 低血扩范围+受伤+25% |

### 第三层：v1.3 构筑遗物（10件）

| 遗物 | 标记 | 触发位置 | 效果 |
|------|------|----------|------|
| 判官断文 | execCritBonus | onEnemyKilled | 暴击击杀后暴击率+20%(60帧) |
| 朱批残页 | weakSpread | onEnemyKilled | 弱点击杀扩散弱点至附近敌人 |
| 血契剑穗 | meleeCdRefund | onEnemyKilled | 近战击杀返还8帧攻击冷却 |
| 留影伞骨 | decoyOnDodge | startDodge | 闪避后原地留残影(50帧)吸引敌人 |
| 火祟牙 | fireExpand | onEnemyKilled | 磷火击杀将该火场固定扩大至42半径（每个火场只触发一次） |
| 魂钱串 | soulKillChain | onEnemyKilled | 魂伤击杀跳一次固定魂伤给最近敌人 |
| 铃木鱼 | ringSoulHit | hitE | 铃命中减速敌人追加小魂伤 |
| 返照铜片 | bounceExplosion | hitE | 折返弹命中留24范围墨爆 |
| 骨血墨 | guxuePenalty | pAtk | 暴击率+15%，非暴击伤害-20% |
| 收阴袋 | killShield + shieldStack | onEnemyKilled/hurtP | 击杀攒护盾(每层减4伤,上限5层)，受击清空 |

**新系统：诱饵（decoys）**

留影伞骨触发时在闪避起点生成诱饵，存入 `g.decoys`。诱饵持续50帧，120范围内吸引敌人转向。每帧递减，渲染为半透明朱砂圆。

**新系统：处决判词（floatTexts）**

高价值击杀（暴击/弱点/Boss/三连段）触发判词浮字，存入 `g.floatTexts`。每帧上浮并淡出，用楷体+朱砂色渲染。判词库 8 句，随机选取。

**性能护栏：对象数量上限**

后期构筑会叠出大量 ring、火场、粒子和敌弹。为避免镇魂铃/魂链/火场组合后期卡顿，运行时先限制对象数量；范围数值上限后续统一设计，不在本轮硬切。

| 对象 | 上限 |
|------|------|
| particles | 260 |
| fires | 72 |
| attacks | 90 |
| eProj | 90 |

`floatTexts / decoys / kites / frosts` 也有轻量上限，避免后期反馈对象累积。对象进入统一走 `pushLimited/addFire/addEProj/pushAttack`，不要直接 `g.xxx.push()`。

**性能观测（v1.4.1）**

按 `T` 可查看：

| 指标 | 含义 |
|------|------|
| fps | 平滑帧率估计 |
| en / atk / prj | 敌人、玩家攻击、敌方弹数 |
| par / fire / txt / dec | 粒子、火场、判词、诱饵数 |
| kite / frs | 纸鸢、冰冻区数量 |
| peak * | 本局历史峰值 |
| pressure | 当前对象压力百分比 |

`pressure` 由 `particles / fires / attacks / eProj` 的占用率取最大值。粒子生成会按压力自动降载，火场与回血 tick 会错峰执行，避免镇魂铃/魂链/火场叠加时同帧尖峰过高。


## 六B、武器进化系统

第叁波通关后替代遗物选择，展示武器进化选项。每把武器4个进化（3通用+1专属）。

进化标记及触发位置：

| 标记 | 触发函数 | 行为 |
|------|----------|------|
| wideSlash | pAtk | 弧度乘1.2 |
| seekBlade | pAtk | 每3击朝最近敌人发射追踪弹 |
| projPierce | pAtk | 弹道永远穿透 |
| projBurst | hitE | 命中时50范围溅射40%伤害 |
| bigSplit | hitE | 大弹命中分裂3颗小弹 |
| ringSlow | update | ring命中后slowT=45 |
| ringNoDecay | pAtk | 多圈时不递减 |
| dashReturn | pAtk | dash后8帧反向补刀 |


## 七、敌人与波次

六种普通怪 + 一个Boss：

| 敌人 | HP | 特点 |
|------|-----|------|
| 纸傀 (zhikui) | 45 | 近战，慢速 |
| 游魂 (youhun) | 30 | 远程投射，较快 |
| 焚灵 (fenling) | 55 | 近战+火径，慢但肉 |
| 食灰鬼 (shigui) | 78 | 坦克型，压迫站位 |
| 骨灯 (gudeng) | 42 | 远程三向弹，低速炮台 |
| 僵客 (jiangshi) | 88 | 蓄势后直线冲锋 |
| 画皮娘子 (boss) | 320 | 近战+8方向弹幕，50%血后加速 |

9个波次，HP和速度按 `1 + wave * 0.065` 缩放。关卡调制器：净坛、纸灰风、悬井、无面戏台、鬼灯阵、墨池、鬼市、镜殿。

波次间回复20%最大生命。


## 八、音效（sound.js）

Web Audio API 合成，28种音效全部用振荡器+噪音生成。无外部音频文件。含8种关卡氛围音（STAGE_AMBIENT）。


## 九、视觉管线

Canvas 960×640，每帧分层绘制：

```
1. 纸张底色 + 纹理噪点
2. 装饰墨斑
3. 关卡调制器地形
4. 火焰/墨迹区域
5. 敌人弹幕
6. 玩家攻击特效
7. 敌人（含HP条、受击闪白、死亡缩放、恐惧光环、弱点标记、冲锋预警）
8. 玩家（含替身光环、蓄力环、复活标记、攻速光环、移速拖影、朝向指示线）
9. 连段数字
10. 粒子系统
11. 诱饵（半透明朱砂圆）
12. 处决判词浮字（楷体+朱砂色）
13. 波次公告文字
14. Boss HP条
15. 处决闪光
16. 暗角渐变
17. debug信息（T键）
```

水墨风格核心：纸张色 `#f1e6d4`、墨色 `#171310`、朱砂 `#a33a2d`。


## 十、遗物选择算法

### 三路加权选择

`pickRelicChoices(g)` 从未拥有遗物池中选出3件，分三次调用：

| 路数 | 模式 | 倾向 |
|------|------|------|
| 第一路 | main | 武器共鸣、流派补强、遗物联动 |
| 第二路 | support | 补短板（生存、控场、暴击、击杀） |
| 第三路 | wild | 变招（诅咒、火、分裂、处决、闪避） |

每次对全池打分→取分最高的前8件→加权随机→标记已选。

### 评分规则 `scoreRelicChoice()`

基础分 18 + 少量随机。加分项：

| 维度 | 条件 | 加分 | 理由 |
|------|------|------|------|
| 武器共鸣 | 标签匹配武器标签 | +10/标签 | 直接适配 |
| 流派补强 | 标签匹配 BUILD_PREFS | +7/标签 | 方向正确 |
| 遗物联动 | 标签已拥有 | min(10, count*4) | 组合收益 |
| 标签过载 | 同标签已拥有4件+ | -4 | 避免偏科 |
| 血线保护 | HP<55% + 防御标签 | +9 | 活命优先 |
| 低血风险 | HP<35% + 诅咒 | -10 | 别找死 |
| 前期稳定 | wave≤2 + 防御标签 | +4 | 开局求稳 |
| 后期清场 | wave≥5 + 输出标签 | +6 | 收割节奏 |
| 遗物专属 | 30+条id→情境规则 | 5~10 | 见下表 |

### 遗物专属权重

| 遗物 | 触发条件 | 加分 | 理由 |
|------|----------|------|------|
| 倒写经卷 | 远程/伞/有折返 | +10 | 折返收益 |
| 黄泉引路 | 伞/高速度/有机动 | +10 | 速度联动 |
| 走马灯片 | 伞/有黄泉 | +8 | 速度链 |
| 破镜残片 | 伞/有闪避 | +10 | 闪避反击 |
| 墨龙珠 | 远程/铃 | +7 | 蓄势窗口 |
| 墨龙珠 | 伞 | -4 | 闪避无蓄势 |
| 墨池残砚 | multi>1 / 剑 | +6 | 连击收益 |
| 骨笛残声 | 有击杀遗物 | +6 | 击杀链 |
| 朱砂/铜镜 | 剑 / 有暴击 | +7 | 暴击链 |
| 青铜/血烛 | 剑 / 铃 | +5 | 范围收益 |
| 纸人替魄 | HP<60% | +7 | 替身保护 |
| 纸甲残片 | 剑 / 伞 | +6 | 前线防御 |
| 招魂残幡 | wave≤4 | +5 | 前期保命 |
| 玄冰简穗 | 铃 | +9 | 减速领域 |
| 傩面碎片 | 有暴击遗物 | +7 | 暴击恐惧 |
| 判官断文 | 剑/暴击/铜镜 | +9 | 处决共鸣 |
| 朱批残页 | 剑/铜镜/暴击 | +8 | 弱点联动 |
| 血契剑穗 | 剑 | +9 | 近战冷却 |
| 血契剑穗 | 有处决 | +5 | 处决补强 |
| 留影伞骨 | 伞/破镜/闪避 | +9 | 闪避反击 |
| 火祟牙 | 有夜灯/有火 | +10 | 火场扩散 |
| 魂钱串 | 远程/铃/有魂 | +9 | 魂杀链 |
| 铃木鱼 | 铃 | +10 | 铃魂联动 |
| 铃木鱼 | 有魂/控场/减速 | +6 | 控场魂伤 |
| 返照铜片 | 远程/折返/经卷 | +9 | 折返墨爆 |
| 骨血墨 | 有暴击/剑 | +7 | 暴击风险收益 |
| 骨血墨 | HP<35% | -8 | 低血不宜 |
| 骨血墨 | 无暴击且非剑 | -6 | 缺暴击支撑 |
| 收阴袋 | 无生存 | +10 | 缺生存 |
| 收阴袋 | 有击杀/wave≥5 | +6 | 击杀护盾 |

### 武器流派偏好 (BUILD_PREFS)

| 武器 | 偏好标签 |
|------|----------|
| 斩妖剑 (melee) | 近战、处决、暴击、击杀、生存、魂 |
| 符骨笔 (ranged) | 远程、法术、分裂、魂、暴击、击杀 |
| 镇魂铃 (aoe) | 控场、召物、魂、冰、暴击、击杀 |
| 伏魔伞 (dash) | 闪避、反击、机动、远程、火、击杀、生存 |

### 四条核心构筑线

| 构筑线 | 主武器 | 核心遗物 | 放大遗物 | 保底遗物 |
|--------|--------|----------|----------|----------|
| 处决暴击 | 斩妖剑 | 朱砂封钉、铜镜照妖 | 判官断文、朱批残页 | 血契剑穗、纸甲残片 |
| 闪避反击 | 伏魔伞 | 破镜残片 | 留影伞骨、黄泉引路 | 走马灯片、纸人替魄 |
| 火与击杀 | 任意 | 夜灯残烬 | 火祟牙、骨笛残声 | 收阴袋、走马灯片 |
| 魂与连锁 | 符骨笔/铃 | 无字谶符、鬼脊蝶印 | 魂钱串、铃木鱼 | 引魂灯笼、骨笛残声 |


## 十一、扩展指南

### 加新遗物

在 RELICS 数组末尾追加。数值遗物改 stats，机制遗物设 boolean 标记。机制遗物还需在对应触发函数中加检查逻辑。

### 加权重

每件新遗物必须补 `scoreRelicChoice()` 中的专属规则，否则只会靠通用标签得分，适配度低。

### 加新武器

在 WEAPONS 追加，在 pAtk 的 `w.type` 分支加攻击生成逻辑。同时更新 BUILD_PREFS。

### 加新敌人/波次/调制器

ETYPE 定义新类型，WAVES 引用。新调制器在 STAGE_MODS 加名称，在 startStage/updateStage/renderStage 实装。


## 十二、版本变更记录

### v1.0 初始版本
4武器、7遗物、7波次、4敌人、1Boss。

### v1.1 Codex 扩展
新增食灰鬼、骨灯、僵客。波次扩到9波，加关卡调制器。统一击杀/伤害钩子。加权遗物选择框架。文件从 ~827 行扩到 ~1167 行。

### v1.2 P0/P1 修复
- 遗物联动修复（磷火击杀、伞dash距离）
- 遗物描述对齐
- 视觉反馈（攻速光环、移速拖影、弱点标记、恐惧光环）
- 武器进化系统（16进化）
- 连段数字渐变

### v1.3 构筑深挖（本轮）

**新系统：**
- 处决判词（floatTexts + JUDGMENTS）
- 诱饵系统（g.decoys，留影伞骨触发）
- moveScale() 函数（移速倍率+击杀加速buff）

**新增遗物（10件）：** 判官断文、朱批残页、血契剑穗、留影伞骨、火祟牙、魂钱串、铃木鱼、返照铜片、骨血墨、收阴袋

**Bug 修复：**
- moveScale 未定义导致人物无法移动（Codex 重写时遗漏）
- killShield 护盾在 hurtP 中放在扣血之后，实际不生效→移到扣血前
- scoreRelicChoice 标签重复惩罚阈值3→4
- hasKill 检测遗漏4个击杀标记
- BUILD_PREFS 每把武器缺"击杀"标签
- 模式标签过窄（support/wild 覆盖不足）
- 6件遗物缺专属权重规则
- Canvas save/restore 不平衡导致偏移积累
- 游戏循环单 try-catch 导致一错全停→拆分为独立 try-catch

**文件行数：** game.js ~1298 行（v1.3 时代，已过时，当前见 v1.4 行数）

### v1.4 内容扩展

**新系统：**
- 冰冻区域（g.frosts，裂冰诀触发）
- 追踪纸鸢（g.kites，纸鸢引触发）
- 墨池关卡调制器（敌人增伤+击杀净化）
- 第二进化点（第陆波可选再进化）
- 敌人护盾机制（墨盾鬼）
- 敌人分裂机制（分身鬼）
- 敌人召唤机制（纸鸢匠）

**新增遗物（6件，34→40）：** 风行纸马、灯油旧芯、聚灵墨斗、急就章、纸鸢引、裂冰诀

**新增敌人（3种，6→9）：** 纸鸢匠（召唤型）、分身鬼（分裂型）、墨盾鬼（护盾型）

**新关卡调制器：** 墨池（墨池中敌人攻击力+30%，击杀可净化为加速区）

**数值封顶：**
- 暴击率封顶 0.65
- 铃连段 combo 封顶 15（范围最大 1.9×基础）
- 收阴袋护盾层数 5→3（减12伤）

**P3 视觉打磨（5项）：**
- Boss 血条统一（移除 CSS 死代码，Canvas 版加大+低血变色）
- 蓄力满音效（chargeReady：上扬正弦波）
- 连段消散动画（comboBreak 浮字缩至0+淡出）
- 敌人死亡粒子按类型着色（DEATH_COLOR 映射9种）
- 暂停 Canvas 遮罩（半透明墨色+楷体"暂停"）

**v1.4 优化（bug 修复 + 视觉 + 反馈）：**
- 火焰 fade 改用 maxLife 而非硬编码分母
- 墨池增伤对弹道投射物生效（eProj 传递 _src）
- 冰冻区碰撞半径与视觉同步缩小
- 风行纸马浮点安全（Math.round 避免 0.2999 问题）
- 敌人视觉区分（纸鸢匠金色/分身鬼蓝绿）
- 纸鸢朝向追踪目标旋转
- 冰冻区渲染层级移到敌人下方（地面效果）
- Boss 击杀反馈加强（freezeT 22/ shakeT 16）
- 无敌闪烁改为正弦平滑脉冲
- PCOL 移至模块级常量（每帧零分配）
- 背景静态纹理预渲染到 offscreen canvas（buildBg）
- killAtkSpd 动画冻结减至4帧（解决"卡顿感"）
- 粒子在 fastAtk 模式下降频（pMul=0.5）
- 蓄力满脉冲光环+粒子爆发（chargeReady）
- Boss 狂暴一次性触发（enraged 标记+音效+视觉）
- 玩家死亡动画（freezeT=15, shakeT=12, 粒子爆发）
- 受伤屏幕边缘闪红（hurtFlash 渐变叠加层）
- 低血量警告光环（HP≤30%时朱砂脉冲圈）
- 波次清除庆祝粒子（accent 墨爆）
- 波次开始敌人冻结30帧（公告可读性）
- 墨盾鬼护盾重生粒子+音效
- 纸鸢拖尾视觉（尾部半透明三角）
- 镇魂铃 ring 内部淡色填充（AOE可读性）
- Boss 血条加大(280×12)+低血脉冲+边框
- 连段数字颜色阈值修正（cc不再误截10）
- 无面戏台还魂浮字（"还魂"朱砂字）
- 波次间回血视觉（绿色墨粒子）
- 遗物拾取粒子爆发（accent墨爆）
- 8个新音效：shieldBreak/frostCreate/splitPop/summon/playerDodge/bossEnrage/revive/kiteHit

**新构筑线：**
- 冰控暴击流：玄冰简穗 + 裂冰诀 → 暴击→冰冻→减速→更多命中→更多暴击
- 召唤击杀流：纸鸢引 + 夜灯残烬 → 击杀产纸鸢→额外追踪→火杀链

**文件行数：** game.js ~1591 行，gamedata.js ~206 行

### v1.4.1 稳定版

**目标：** 不继续扩内容，先规整性能观测和后期对象压力。

**本轮调整：**
- debug 面板补充 fps、对象当前值、对象峰值、pressure 压力百分比
- `newGame().perf` 增加 `pressure / peaks`
- 粒子生成改为按 `perfMul()` 自适应降载
- 火场伤害 tick 与火场回血 tick 错峰执行，避免同帧全部扫描
- `floatTexts / decoys / kites / frosts` 明确纳入对象上限体系
- 文档补充“性能观测”和统一对象入口说明

**设计取向：**
- 不在这一版硬切范围数值上限
- 不新增遗物/敌人/波次
- 优先保证镇魂铃、魂链、火场类构筑在后期不出现明显同帧尖峰

### v1.4.2 遗物页修复

**目标：** 修掉遗物/进化卡弹窗的点击中断问题，保证选卡后能稳定回到战斗。

**本轮调整：**
- 修复 `showRelic()` 中遗物点击后的 `spawnInk()` 参数错误
- 统一从当前 `choices` 解析被选中的遗物/进化，避免二次查表偏移
- 点击事件补 `TextNode → parentElement` 兜底，减少卡面子节点点击失效
- 选择成功后立即解绑 `onclick`，避免双击重复结算
- 补空 DOM / 空候选保护，异常场景直接恢复战斗流转

### v1.4.3 波次进场保护

**目标：** 修掉“选完遗物立刻被上一波残留物清掉下一波，直接跳到后续奖励”的流程错误。

**本轮调整：**
- `startWave()` 不再写入无效的 `freezeT`，改为给新波敌人统一设置 `spawnGraceT=30`
- 进场保护期间，敌人不会移动、攻击、碰撞伤害玩家
- 进场保护期间，敌人不会被玩家弹道、近战、铃圈、火场、纸鸢命中
- 分裂体与召唤物补齐 `spawnGraceT` 字段，避免对象结构不一致

### v1.4.4 进化选池修正

**目标：** 修掉进化选择仍按随机出牌、会出现前置未成就给死词条的问题。

**本轮调整：**
- `showRelic()` 的进化分支改为 `pickEvolutionChoices()`，不再直接随机切 3 个
- 进化选择改为按武器与当前构筑打分
- `双频` 这类依赖第二圈的进化，当前置未成时会被强降权
- 如果已经没有剩余进化位，且当前也没有第二圈来源，则 `双频` 直接禁出
- `buildPickState()` 补充 `remainingEvoChoices / slowOnHit / ringSlow / ringNoDecay`


### v2.3 移动端深度优化 + 游戏性扩展 (2026-04-29)

**目标：** 修复移动端关键体验问题，实现元进度系统，增强游戏性。

**本轮调整：**

**移动端修复 (P0-P1)：**
- 修复暂停/结算覆盖层无法滚动：`touch-action: none` 从 body 移至 `.game-frame`，覆盖层设置 `touch-action: auto`
- Android 返回键处理：集成 Capacitor App backButton 监听器，游戏中暂停、暂停时恢复、标题时退出
- 安全区域适配：HUD、闪避/暂停按钮、遗物徽章使用 `env(safe-area-inset-*)`
- 横屏锁锁定优化 + 动态 Canvas 缩放适配所有视口

**生存波次：**
- 新增波次类型 `survival`：敌人源源不断，需击杀足量敌人才能结束
- 每个生存波次有独立击杀配额（初始敌人数的 1.5 倍）
- 定期刷新敌人（90-150 帧间隔），敌人数不超过 20
- 全部清完但刷新计时器未到则自动结束（防止太强卡关）

**精英波次视觉：**
- 精英/精英潮波次增加金色环境粒子（每 8 帧生成）
- 金色径向渐变屏幕覆盖层 + 金色边框脉冲
- 覆盖层透明度随游戏时间缓慢呼吸变化

**元进度解锁系统：**
- 14 项成就：初入地宫、四武器通关、全武器精通、墨将军克星、遗物学徒/大师、千斩、走阴老手、噩梦行者、墨上墨(S级)、镇祟者
- 成就数据持久化于 `mosui_meta` localStorage（version 2，向后兼容）
- 解锁奖励：`startRelic`（起始遗物）和 `goldInk`（金墨色）
- 起始遗物从预设池（狐墨、幽冥砂、沙墨等）随机选取
- 标题画面展示最近 6 个已解锁成就徽章 + 解锁提示

**结算画面增强：**
- 死因显示：显示击杀玩家的敌人名称
- 详细统计：总伤害、最高连斩、精英击杀数
- 个人最佳对比：与历史最佳波次比较并显示评语

**设置/音量菜单：**
- 暂停菜单新增音量滑块（0-100%），存入 localStorage
- GameSound 初始化时读取已保存音量
- 暂停时音量自动降至 10%（取 savedVol 和 0.1 中较小者）
- 新增「重新开始」按钮（重开当前武器/难度）
- 新增「退到标题」按钮（返回标题画面）

**Bug 修复：**
- Boss 击杀计数追踪：`g.bossKilled` / `g.mojiangjunKilled` 标记 + meta 递增
- `loadMeta` 语法错误修复（缺失闭合括号）
- `localStorage` 调用全部包裹 try/catch 确保测试环境兼容

### v4.1 Boss击杀结算演出
- Boss 死亡后 120 帧 Canvas 过场：大幅肖像居中 + 判词 + 金线渐消
- 三阶段动画：缩放入场(0-30) → 停留(30-90) → 渐出(90-120)
- 复用 `window._bossPortraitImgs` 预加载肖像
- 结算页 Grade 字母弹入 CSS 动画 (`gradeReveal` keyframes)

### v4.2 内容扩容
- 第 5 武器「召魂幡」(type:summon)：放置幡旗 → 周期发射追踪魂弹
- 5 个召魂幡进化：招魂(穿透)/散魄(爆裂)/灵将(范围+增伤)/连幡(双幡)/归将(回血)
- 遗物上限 6 件 (`pickRelicChoices` 检查)
- 3 个新成就：斩鬼王/三祟皆灭/六器加身
- 平衡：画皮 14→12 dmg，墨鬼王 550→500 HP，墨骨 85→72 HP

### v4.2.1 视觉和逻辑修复
- 13 处 `"C.ivory"` → `C.ivory` 颜色变量引用修复（火场/弹道/攻击内亮线）
- 火场渲染重复行删除
- `recallMark.life--` 从 render() 移至 update()
- 召魂幡魂弹新增追踪转向 (0.12 转向系数)
- 清除无用的 `spawned` 字段

### v4.3 召唤武器补全
- 3 个新遗物：魂幡灵(幡增伤+范围)/墨师珠(击杀减速墨池)/仙螺纹(铃幡控场)
- 2 个新誓印：暗潮(敌人加速+击杀回血)/墨封(遗物上限3+50%伤害)
- 新增 `enemySpdMult`/`maxRelicsOverride` 机制
- `win_fan` 成就 + `all_weapons` 升级 5 武器
- `BUILD_PREFS` 新增 summon 流派标签
- wiki.html 五武器/进化名/敌人标志补全
- index.html 首页数字全面更新

### v4.4 性能+手感+QoL
- Banner fires 跳过被动火场碰撞（性能优化）
- 波次清场墨纹从玩家位置扩散（Canvas 动画）
- Boss 击杀增加 30 帧慢动作
- HUD 波次信息显示誓印名称
- 召魂幡 spirit bolt 间隔 30→25 帧（DPS +20%）
- waveInkRipple 对象动画结束后清理

### v4.5 闪避手感重做
- 输入缓冲：冷却最后 8 帧按闪避自动排队执行
- 方向记忆：静止时闪避朝上次移动方向（非鼠标方向）
- 冷却 42→36 帧，无敌帧 14→16 帧

### v4.6 波次多样性
- 第肆波(鬼灯廊)新增 special:"horde" — 敌人蜂拥而出
- 第陆波(灰潮)新增 special:"elite" — 全部敌人变为精英
- 丰富中段波次节奏变化，避免中间波次体验过于平淡

*v4.6 更新于 2026-05-09。*

### v4.7 波次节奏扩展 + 2新遗物
- 第叁波(悬井口)新增 special:"survival" — 持续压力
- 第伍波(无面台)新增 special:"horde" — 蜂拥而出
- 第柒波(墨池)新增 special:"survival" — 持续压力
- 9个静态波次中5个有特殊标签，中段节奏更丰富
- 新遗物：墨镜碎影(站定0.5秒自动反射最近敌弹，冷却4秒)
- 新遗物：九转墨符(每命中6次，下次攻击全方向AOE爆发)
- RELICS: 92→94 (+2)
- mkPlayer新增字段: autoReflect/autoReflectReady/autoReflectCd/nineSealCount/nineSealReady
- rebuildPlayerStats保留列表扩展

*v4.7 更新于 2026-05-09。*

### v4.8 新誓印：墨旋
- 新增誓印"墨旋"：移动留下减速冰霜区，静止时受伤+30%
- CURSES: 17→18 (+1)
- 高风险高收益的机动型誓印，鼓励持续走位

*v4.8 更新于 2026-05-09。*

### v4.9 补丁 — CSS图标+BUILD_PREFS+首页数字
- 新遗物墨镜碎影/九转墨符补CSS图标
- BUILD_PREFS添加"防御"(melee/dash)和"爆发"(aoe/summon)标签
- 首页遗物数85→97，誓印数19→20

*v4.9 更新于 2026-05-09。*

### v4.10 召魂幡Bug修复 + 武器选择UI修复
- **P0修复**：召魂幡火场错误进入敌方火场分支伤害玩家，加 `!f.isBanner` 守卫
- 武器选择grid从4列改为5列适配第5武器
- 修复卡片装饰伪元素遮挡图标（加z-index分层）
- 遗物-幡适配审查通过

*v4.10 更新于 2026-05-09。*

### v4.11 代码审计修复 (7项Bug)
- **P0** stillT双重递增：autoReflect不再独立管理stillT，复用统一stillT逻辑
- **P0** cleanupWave未清理soulOrbs和pendingDeathbursts：追加清理
- **P1** hitDmgMult(镜花誓印)错误应用到所有伤害来源：限source==="hit"
- **P2** nineSealCount跨波次保留：从rebuildPlayerStats保留列表移除，startWave重置
- **P2** sqrt除零：移动归一化加 `||1` 守卫
- autoReflect冷却期间不再错误积累stillT

*v4.11 更新于 2026-05-09。*

### v4.12 新敌人：墨罐 (2026-05-09)
- 新增敌人 **墨罐 (mojar)**：HP55，中速近战，死后留下大范围减速墨池（r=80, 120帧）
- spawnEnemy 新增 deathSlow/deathSlowR/deathSlowT 属性传递
- onEnemyKilled 新增 deathSlow 逻辑：死亡时在原地生成 frost zone + 墨汁爆发粒子
- 加入 WAVE_TIERS tier 3/4，ENEMY_COST=2.0，DEATH_COLOR=ink
- 187 项测试全绿

*v4.12 更新于 2026-05-09。*

### v4.13 新遗物：墨迹残步 + 新成就 (2026-05-09)
- 新增遗物 **墨迹残步 (mojicanbu)**：移动充能（最多120帧），停下释放范围墨爆，充能越久范围越大伤害越高
- 与墨镜碎影(站定反射)形成策略对称——一动一静两种playstyle
- mkPlayer 新增 moveChargeMax/moveChargeT 属性
- rebuildPlayerStats ck 保留列表新增 moveChargeMax
- 新增成就"疾风步"——单局墨迹残步充能释放10次以上
- 187 项测试全绿

*v4.13 更新于 2026-05-09。*

### v4.14 冷门标签补遗：还骨泉+墨散淬 (2026-05-09)
- 新遗物 **还骨泉 (huanguquan)**：击杀15%概率回复3HP (tags:治疗/生存)
- 新遗物 **墨散淬 (mosancui)**：近战命中溅射周围敌人30%伤害 (tags:溅射/近战)
- onEnemyKilled 新增 killHealChance 概率回血逻辑
- hitE 新增 meleeSplash 近战溅射逻辑（仅melee武器，splashR=50）
- BUILD_PREFS 扩展：melee+溅射，dash/summon+治疗
- 首页遗物数97→100，敌人28→29
- 190 项测试全绿

*v4.14 更新于 2026-05-09。*

### v4.15 新敌人墨萤(致盲)+blindShot机制 (2026-05-09)
- 新敌人 **墨萤 (moying)**：远程墨粉，命中致盲90帧（攻击范围-40%），tier 2/3
- 新增 blindShot 敌人属性 → 玩家 blindT 计时器 → 攻击范围临时降低
- mkPlayer 新增 blindT，rebuildPlayerStats rk 保留
- spawnEnemy 新增 blindShot 属性传播
- rebuildPlayerStats ck 新增 killHealChance/killHealAmt/meleeSplash/meleeSplashRatio
- 190 项测试全绿

*v4.15 更新于 2026-05-09。*

### v4.16 新誓印：墨血 (2026-05-09)
- 新誓印 **墨血 (moxue)**：连击越高伤害越高(每层+8%，上限10层)，但受伤也增加(每层+5%)
- hitE 新增 comboDmgScale 连击增伤逻辑
- hurtP 新增 comboVuln 连击受伤增加逻辑
- mkPlayer/rebuildPlayerStats 新增 comboDmgScale/comboVuln 属性
- 首页誓印20→21
- 192 项测试全绿

*v4.16 更新于 2026-05-09。*

### v4.17 3新遗物填补冷标签 (2026-05-09)
- 新遗物 **墨爆印 (moboaoyin)**：爆炸/近战标签，近战击杀触发50范围AOE爆炸(40%攻击力)
- 新遗物 **蚀墨池 (shimoChi)**：持续/击杀标签，击杀留下90帧持续伤害减速区(复用frost+dmg)
- 新遗物 **骨续泉 (guxuquan)**：生命/生存标签，每清一波maxHp+2(上限+10)并回复等量HP
- onEnemyKilled 新增 killExplode(近战AOE)+killDotZone(DOT区) 逻辑
- wave cleared 新增 waveHpBonus HP成长逻辑
- mkPlayer/rebuildPlayerStats 新增 killExplode/killDotZone/waveHpBonus 等属性
- BUILD_PREFS melee 增加"爆炸"标签
- 首页遗物数字 100→103
- CSS伪元素图标 moboaoyin/shimoChi/guxuquan
- 195 项测试全绿 (37+5+143+10)

*v4.17 更新于 2026-05-09。*

### v4.18 成就补全+新敌人 (2026-05-09)
- 3新成就：连锁墨爆(killExplodeKills≥10)、盲斗(blindKills≥10)、百骨长生(waveHpHealed≥20)
- newGame 计数器：killExplodeKills/blindKills/waveHpHealed
- metaRecordRun 追踪：bestKillExplodeKills/bestBlindKills/bestWaveHpHealed
- 新敌人 **墨偶师 (mooushi)**：远程+召唤组合型，tier 2/3/4
- 成就总数 32→35，敌人 30→31
- 197 项测试全绿 (37+5+145+10)

*v4.18 更新于 2026-05-09。*

### v4.19 冰墨壁遗物+防御标签扩充 (2026-05-09)
- 新遗物 **冰墨壁 (bingmobi)**：防御/冰标签，受伤时在自身周围生成冰墨减速区域(半径50, 60帧)
- hurtP 新增 hurtFrost 逻辑，复用现有 frost 系统
- mkPlayer/rebuildPlayerStats 新增 hurtFrost 属性
- 防御标签遗物 2→3，冰标签遗物 5→6
- CSS伪元素图标 bingmobi
- 198 项测试全绿 (37+5+146+10)

*v4.19 更新于 2026-05-09。*

### v4.20 墨泉眼+墨焰溅+墨锁誓印 (2026-05-09)
- 新遗物 **墨泉眼 (moquanyan)**：治疗/生存标签，波次结束时若HP<50%回复20%最大生命
- 新遗物 **墨焰溅 (moyanqian)**：溅射/火标签，火焰击杀时向四周溅射20%攻击力的火焰伤害
- 新誓印 **墨锁 (mosuo)**：召物/诅咒标签，墨灵上限+2但每只墨灵使最大生命-10
- wave cleared 新增 lowHpWaveHeal 逻辑
- onEnemyKilled 新增 fireSplash 火焰溅射逻辑
- rebuildPlayerStats 新增 spiritHpPenalty 最大生命缩减
- 治疗 2→3，溅射 2→3，火 9→10
- 誓印 21→22，遗物 104→106
- 200 项测试全绿 (37+5+148+10)

*v4.20 更新于 2026-05-09。*

### v4.21 墨火眼+墨符坛+墨魂丹+墨蛛后 (2026-05-09)
- 新遗物 **墨火眼 (mohuoyan)**：爆炸/火标签，暴击时产生小范围爆炸(25%攻击力)
- 新遗物 **墨符坛 (mofutan)**：持续/法术标签，攻击命中留下墨符DOT区(复用frost)
- 新遗物 **墨魂丹 (mohundan)**：生命/爆发标签，HP<25%时攻击力+50%持续5秒
- 新敌人 **墨蛛后 (mozhuhou)**：HP85，召唤型(tier 4)
- 爆炸 2→3，持续 2→3，生命 2→3
- 遗物 106→109，敌人 31→32
- 201 项测试全绿

*v4.21 更新于 2026-05-09。*

### v4.22 2新成就 (2026-05-09)
- 新成就 **绝地逢生 (last_stand)**：墨魂丹低血爆发状态下击杀20个敌人
- 新成就 **蛛后克星 (spider_slayer)**：单局击杀3只墨蛛后
- game.js 新增 lowHpBurstKills/mozhuhouKills 计数器 + metaRecordRun 记录
- 成就 35→37，201 项测试全绿

*v4.22 更新于 2026-05-09。*

### v4.23 3新遗物填补冷标签组合 (2026-05-10)
- 新遗物 **墨爆弹 (mobaodan)**：爆炸/爆发标签，对HP<30%敌人自动引爆(50%攻击力AOE)
- 新遗物 **墨蚀域 (moshiyu)**：持续/溅射标签，攻击命中留大范围持续溅射区(复用frost)
- 新遗物 **续命墨 (xumingmo)**：治疗/生命标签，击杀35%几率回血4点，溢出转临时护盾
- executeExplode 在 damageEnemy 中触发(HP阈值检测+AOE)
- splashDot 在 hitE 中触发(留大范围frost zone)
- healToShield 在 killHealChance 溢出时触发
- BUILD_PREFS 标签补全：melee+生命/攻速, ranged+攻速, aoe+持续/诅咒, dash+生命, summon+持续/生命/诅咒
- 遗物 109→112，测试 201→202
- 202 项测试全绿

*v4.23 更新于 2026-05-10。*

### v4.24 2新遗物填补分裂+防御标签 (2026-05-10)
- 新遗物 **墨裂符 (moliefu)**：分裂/法术标签，命中20%几率分裂追踪弹幕
- 新遗物 **墨铁壁 (motiebi)**：防御/反击标签，受击后1秒无敌+反击波
- splitOnHit 在 hitE 中触发(最近敌人追踪弹幕)
- hurtRetaliate 在 hurtP 中触发(无敌+AOE反击)
- RANGES 新增 split(120)/retaliate(80)
- 遗物 112→114，测试 202→203
- 203 项测试全绿

*v4.24 更新于 2026-05-10。*

### v4.25 盲牟+墨蛉+墨爆连环+攻速标签修复 (2026-05-10)
- 新遗物 **盲牟 (mangmou)**：诅咒/爆发标签，致盲期间攻击伤害+60%
- 新敌人 **墨蛉 (moling)**：tier 2 远程减速弹(webShot)
- 新成就 **墨爆连环 (chain_execute)**：墨爆弹引爆击杀15敌
- BUILD_PREFS 死标签修复：急就章/霹雳木 tags 击杀→攻速
- 遗物 115→116，敌人 32→33，成就 37→38；测试编号口径以 `.claude/ctx-extract.js` 为准

*v4.25 更新于 2026-05-10。*

### v4.26 并发开发 — 墨涟爆+墨碑 (2026-05-10)
- 新遗物 **墨涟爆 (molianbao)**：溅射/爆炸标签，溅射源击杀时40%概率二次爆炸
- 新敌人 **墨碑 (mobei)**：tier 3，带盾+死后增益周围敌人(hasShield+deathBuff)
- RANGES 新增 splashBoom(55)
- 遗物 117，敌人 35，成就 38；content_test 当前最高编号 177，实际口径以 `.claude/ctx-extract.js` 为准
- agent 并发测试通过

*v4.26 更新于 2026-05-10。*

## 十三、待办

### P4 打磨

- ✅ 背景静态纹理预渲染到 offscreen canvas（buildBg）
- ✅ 镇魂铃连段数 combo 数字渐变色已调整（12/7阈值）
- 分身鬼分裂体死亡计入 kiteKills（有意为之，奖励清杂）
- 纸鸢匠被击杀时小兵强制清除不触发 onEnemyKilled 效果（有意为之）

### 后续构筑扩展

- 遗物可扩到48件，当前40件
- 新武器候选：墨笔（召唤型）、纸扇（范围型）
- 补"精英变体"敌人（高波次随机升级属性）
- 新关卡调制器：鬼市（随机商店位互换）、封印场（缩小安全区）
- 第三进化点（第捌波），需先扩展 EVOLUTIONS

---

## 十四、下一阶段优化计划（交给 Claude）

### 优先级原则

1. 先修“逻辑死词条 / 流程错发 / 构筑失真”，再碰数值平衡。
2. 先做基座型修复，优先一处改动覆盖多件遗物/多把武器。
3. 不扩新内容，除非当前系统已经稳定到可以承载。

### Phase A：选择与联动基座

**目标：** 继续清掉“给了选项但实际不成立”的词条，以及“拿到了词条但没有实际联动”的断链。

**Claude 任务包：**
- 给遗物和进化补一层“前置条件/死词条过滤”函数，不要只在单个 `id` 上写特判
- 把当前所有依赖前置的效果过一遍，尤其是：
  - 多段 / 第二圈 / 折返 / 减速 / 火场 / 闪避 / 弱点 / 召物
- 把 `scoreRelicChoice()` 和 `scoreEvolutionChoice()` 的状态输入统一，避免一边看 `stats` 一边看布尔，字段来源不一致
- 给“已成型构筑”加轻度保底，减少同一路线中途突然断牌

**验收标准：**
- 不再出现明显死词条：没有前置却给后置收益
- 同一路线连续两次选择不会互相打架
- Debug 开启时，卡面推荐理由能反映真实前置关系

### Phase B：战斗对象与残留物治理

**目标：** 继续处理“上一轮残留物影响下一轮”和“对象很多时逻辑抖动”的问题。

**Claude 任务包：**
- 统一梳理残留物生命周期：`attacks / fires / decoys / kites / frosts / eProj`
- 把“波次切换时需要清理 / 需要保留 / 需要进场豁免”的规则写成明确表，不要散落在 update 中
- 重点检查镇魂铃、火场、纸鸢、折返弹的跨波次行为
- 给对象压力系统补“来源级统计”，至少能看出卡顿主要来自哪类对象

**验收标准：**
- 波次切换后不会因为残留物直接清空新波
- 高压局里能从 debug 看出主要压力来源
- 不引入新的“对象永不消失”问题

### Phase C：数值上限与软封顶

**目标：** 开始做之前暂缓的“范围/频率/连锁”软封顶，但不要做生硬硬砍。

**Claude 任务包：**
- 给 `镇魂铃连段范围` 做软封顶函数，不要直接 `Math.min` 一刀切
- 审核这些增长项：
  - `comboCount` 对范围的放大
  - `atkSpd` 对冷却压缩
  - `critRate` 上限
  - `shieldStack` 上限
  - `fire / soul / bounce` 的连锁次数
- 把“上限值”和“增长曲线”写进文档，便于后续调参

**验收标准：**
- 后期构筑仍然爽，但不会一件成神或全屏失控
- 上限逻辑是可解释的，不是到处散落魔法数字

### Phase D：反馈与可读性

**目标：** 优化玩家理解成本，让“为什么强 / 为什么不出 / 为什么触发”更清楚。

**Claude 任务包：**
- 进化卡和遗物卡在 debug 模式下显示更准确的推荐理由
- 给关键联动补最小反馈：
  - 双频已生效
  - 弱点扩散已触发
  - 火场扩张已触发
  - 折返墨爆已触发
- HUD 或浮字只补关键提示，不要把屏幕做成日志流

**验收标准：**
- 玩家能看懂一次关键联动是否真实生效
- 反馈不影响战斗读图

### Claude 实施顺序建议

1. 先做 Phase A 的前置过滤基座。
2. 再做 Phase B 的残留物与对象生命周期梳理。
3. 然后做 Phase C 的软封顶。
4. 最后做 Phase D 的反馈层。

### 明确不要做

- 不要先加新武器、新遗物、新关卡
- 不要在没有统一策略前零散加更多 `if(r.id===...)` 临时补丁
- 不要先做大视觉重构，当前问题核心仍是逻辑基座

---

## 十五、范围来源清单

### 构筑范围（受 stats.range 乘算）

| 来源 | 类型 | 递增？ | 乘算方式 |
|------|------|--------|----------|
| 青铜镇铃舌 | 遗物 | 否（固定+0.25） | stats.range += 0.25 |
| 血烛祭片 | 遗物 | 否（固定+0.2） | stats.range += 0.2 |
| 劈山（剑进化） | 进化 | 否（固定+0.3） | stats.range += 0.3 |
| 扩域（铃进化） | 进化 | 否（固定+0.4） | stats.range += 0.4 |
| 血墨混染 | 遗物 | 否（条件触发） | 低血时 rng *= 1.4 |
| 斩妖剑第三段 | 武器连段 | 否（条件触发） | rng *= 1.25 |
| 镇魂铃连段 | 武器连段 | **是**，无限 | rng *= (1 + comboCount * 0.06) |
| 伏魔伞 | 武器逻辑 | 否 | dashRng = rng * (0.9 + spd * 0.1) |

stats.range 初始 1.0，遗物+进化理论最大值 1.0+0.25+0.2+0.3+0.4 = 2.15。镇魂铃连段不封顶是主要风险点。

### 固定范围（不受 stats.range 影响）

| 来源 | 范围 | 可扩展？ |
|------|------|----------|
| 夜灯残烬（磷火） | 28 | 火祟牙可固定扩到42，每火场一次 |
| 返照铜片（墨爆） | 24 | 否 |
| 爆裂进化（溅射） | 50 | 否 |
| 黄泉引路（减速墨迹） | 18 | 否 |
| 留影伞骨（诱饵吸引） | 120 | 否 |


## 十六、数值风险表

| 风险 | 来源 | 可能表现 | 观察指标 | 暂定方案 |
|------|------|----------|----------|----------|
| 镇魂铃范围无限长 | comboCount 无上限 × 0.06/击 | 后期 rng 超出画布，一铃全屏 | debug 面板 rng 值 | combo 封顶 15（rng 最大 1.9×基础） |
| 魂链扫全敌 | soulChain 每次 hitE 扫全部敌人 | 多目标命中帧卡顿 | debug 面板 fps 下降 | ✅ 已加每次攻击限4目标 |
| 铃木鱼叠太密 | ringSoulHit 每个 ring × 每个敌人 | 魂伤数字满屏，damageEnemy 调用爆炸 | attacks 数量 | ✅ 已加每 ring 限6次 |
| 火杀滚雪球 | fireOnKill + fireExpand + killHeal/killSpeed | 火场铺满屏幕，击杀链不停 | fires 数量 | LIMITS.fires=72 已封顶 |
| 收阴袋过稳 | killShield 每击杀+1层×5层=减20伤 | 高击杀波次几乎不掉血 | shieldStack 值 | ✅ 上限已降为3（减12伤） |
| 血契剑穗无限贴脸 | meleeCdRefund -8帧/击杀 | 剑高攻速下连续击杀冷却归零 | atkCd 值 | ✅ 已限 source==="hit" |
| 暴击链过强 | 骨血墨+朱砂封钉+铜镜照妖+判官断文 | 暴击率>60%，非暴击几乎消失 | critRate 值 | ✅ critRate 封顶 0.65 |
| 对象数量失控 | 后期构筑粒子/攻击/火场爆满 | 帧率暴跌 | debug 面板对象计数 | ✅ 已加全部 LIMITS + 粒子降频 |


---

### v1.5 Phase A-D 实施

**Phase A：选择与联动基座**
- 新增 `PREREQS` 前置条件映射（10件遗物），死词条在选牌前过滤，不再出现
- 新增构筑保底机制：标签重合3+时额外+5分，减少构筑断牌
- 过滤遗物：火祟牙(需夜灯)、返照铜片(需折返)、铃木鱼(需减速)、留影伞骨(需伞)、破镜残片(需伞)、风行纸马(需伞)、魂钱串(需魂链/铃木鱼+减速)、朱批残页(需铜镜)、血契剑穗(需近战)、灯油旧芯(需夜灯)

**Phase B：战斗对象与残留物治理**
- 新增 `cleanupWave(g)` 统一残留物生命周期管理
- 波次切换清理规则：eProj/attacks/particles/floatTexts/decoys 清空，fires 保留玩家火场（过滤非玩家火），kites/frosts 保留
- debug 面板新增来源级统计（pressure>40%时显示攻击类型/火场类型分解）

**Phase C：数值上限与软封顶**
- 新增 `CAPS` 常量集中管理所有软封顶值：critRate=0.65, bellCombo=15, shieldStack=3, atkCdFloor=4, soulChain=4, ringSoul=6
- 攻击冷却增加最低4帧保护，防止攻速堆叠导致冷却归零
- 所有硬编码封顶值统一引用 CAPS 常量

**Phase D：反馈与可读性**
- 弱点扩散触发时浮字"散"提示
- 火场扩张触发时追加火焰粒子
- 折返墨爆已有足够视觉反馈，不加额外文字

### v1.7 搭配审计 + 动画优化

**目标：** 审查全部遗物/进化搭配完整性，优化视觉反馈。

**搭配审计结果：**
- 40件遗物 + 16进化全部链路完整，零断裂、零死词条
- PREREQS(10件) 与运行时检查完全对齐
- cross-relic 协同链（火杀、魂链、折返墨爆、铃魂）全部正确触发
- 确认 seekBlade+bounce 交互为良性的设计选择
- 确认 soulKillChain 链式跳跃为自限制（低伤害值自然止链）

**代码质量（v1.6续）：**

| 优化项 | 内容 |
|--------|------|
| dst() 清理 | 从独立 Math.sqrt 改为调用 dstSq 的1行包装 |
| reject() 移除 | scoreEvolutionChoice 中无用包装替换为 return null |
| dstSq(e,p) 去重 | 敌人循环从2次计算合并为1次复用 |
| RANGES 常量 | 9项距离阈值从内联魔数提取到 gamedata.js |
| WAVE_SCALE 常量 | 波次HP/速度缩放因子提取到 gamedata.js |
| bumpScore() 提取 | 两个评分函数的重复 bump() 合并为模块级函数 |
| stageSpeedFactor | 最后一个不必要的 Math.sqrt 转为 dstSq 比较 |
| ring 碰撞归一化 | 碰撞检测风格与其他攻击类型统一 |
| 测试扩展 | smoke_test 12→28项，覆盖全部核心机制 |

**动画优化（10项）：**

| 改进 | 内容 |
|------|------|
| 近战斩击 | 加白色内弧线，增强刀锋感 |
| 弹道拖尾 | 从1帧拖尾扩到3帧渐隐（0.15/0.3/0.85三级透明度） |
| 敌方弹幕 | 加亮色核心 + 单帧拖尾 |
| 敌人死亡 | 缩放+旋转动画（从纯缩小改为 0.2~1.0 缩放 + 渐旋转） |
| 敌人HP条 | 低血变色（朱砂→褐→暗红，三段式） |
| 敌人受击闪白 | 加朱砂外环，暴击/普攻视觉区分更强 |
| 闪避/dash残影 | 3层半透明残影拖影 |
| 火场渲染 | 加亮色内核（#f7efe5 35%半径） |
| 冰冻区 | 加3条晶体线纹旋转动画 |
| 镇魂铃ring | 加内白边框，双层结构更清晰 |

**动画优化第二轮（6项）：**

| 改进 | 内容 |
|------|------|
| dashSlash 伞击弧线 | 3层渲染（运动拖影+主弧+亮芯），加强冲击感 |
| 折返弹/回弹弹色彩区分 | bounced(朱砂色) / echo(暗朱砂) / 普通(墨绿) 三种配色 |
| Boss 狂暴光环 | 激怒后脉动火焰光环 + 眼睛变朱砂色 |
| 冲锋方向指示 | 僵客蓄力时显示冲锋方向线 |
| 粒子形状分化 | 火粒子=水滴形、魂粒子=菱形、其他=圆形 |
| 弱点标记增强 | 脉动+外圈光晕，更醒目 |

**动画优化第三轮（4项）：**

| 改进 | 内容 |
|------|------|
| 波次公告装饰 | 全屏暗色遮罩 + 标题两侧装饰横线，仪式感更强 |
| 追踪墨刃 | 菱形弹体（与普通圆形弹区分） |
| 玩家笔触尾迹 | 面朝方向身后绘制二次贝塞尔曲线，水墨笔势感 |
| Boss血条增强 | 受击闪白覆盖 + 激怒后显示"· 狂"后缀 |

**反馈系统优化（7项）：**

| 改进 | 内容 |
|------|------|
| 屏幕震动强度分级 | shake()函数替代直接赋值，暴击5/Boss7/死亡8/普通3多档振幅 |
| 浮动伤害数字 | 命中敌人时在头顶显示伤害值，暴击朱砂大字/弱点墨绿/普通墨色 |
| 伤害数字漂移 | 比判词文字更快上浮（0.7 vs 0.5），X轴随机偏移避免堆叠 |
| 击杀连斩计数 | 1.5秒内连续击杀显示"X连斩"，右下角渐隐提示 |
| 遗物拾取闪屏 | 选择遗物后全屏朱砂色闪光覆盖，12帧渐消 |
| 低血量警告 | HP≤30%时屏幕持续脉动红色暗角，提醒玩家注意生存 |
| 玩家受伤震动增强 | 受击shake(4,4)、死亡shake(12,8)，振幅与受伤严重度挂钩 |

**武器视觉差异化（6项）：**

| 改进 | 内容 |
|------|------|
| 符骨笔弹体 | 从圆形改为新月形弧面，贴合骨笔笔势主题 |
| 镇魂铃涟漪 | ring内加3层同心装饰环，强化"声波扩散"视觉 |
| 斩妖剑墨溅 | slash弧线上加4点随机墨渍，增强刀锋墨迹感 |
| 伏魔伞骨线 | dashSlash加5条辐射骨架线，伞形轮廓感 |
| 武器色环 | 玩家外围色环区分武器：剑朱砂/笔墨绿/铃苔色/伞金色 |
| 前方指示点 | 面朝方向小圆点随武器变色，直觉辨识当前武器 |

**敌人视觉差异化（5项）：**

| 改进 | 内容 |
|------|------|
| 游魂波浪底 | 半圆顶+底部波浪曲线，幽灵飘动感 |
| 焚灵火焰冠 | 基础圆+5个三角火焰尖刺，随机闪烁 |
| 僵客方体 | 矩形身体替代圆形，表现僵硬感 |
| 分身鬼重影 | 主体+偏移3px的半透明外圈，克隆体视觉效果 |
| 纸鸢匠菱形 | 菱形体替代圆形，呼应纸鸢主题 |

**关卡特效增强（4项）：**

| 改进 | 内容 |
|------|------|
| 纸灰风线 | 6条水平飘动细线，增强风感 |
| 悬井漩涡 | 3条螺旋吸入线从外向内旋转 |
| 鬼灯吊链 | 灯笼上方加垂直悬吊线 |
| 墨池气泡 | 3个墨色气泡在未净化区域浮动 |

**HUD + 出入场打磨（7项）：**

| 改进 | 内容 |
|------|------|
| 敌人出场动画 | spawnGraceT期间缩放+淡入+墨环扩散，出生感更强 |
| 闪避冷却弧 | 玩家外围弧线显示闪避CD剩余，直觉判断能否闪避 |
| 护盾层数弧 | 收阴袋叠层以3段短弧线显示在玩家外围 |
| 波次进度条 | 底部细条显示当前波次敌人存活比例，附波次编号 |
| 玩家死亡爆墨 | 死亡瞬间12方向墨粒爆发，配合大范围墨迹和震动 |
| 胜利庆祝粒子 | 打败Boss后40颗朱砂+20颗金色粒子爆发+强震动 |
| 武器色环 | 已在武器差异化中实现，此处记录其HUD作用 |

**机制打磨（4项）：**

| 改进 | 内容 |
|------|------|
| Boss第二阶段弹幕 | 激怒后每60帧发射5颗旋转螺旋弹，与8向弹形成复合威胁 |
| 敌人死亡粒子 | 每种敌人用DEATH_COLOR专属颜色爆发粒子（Boss 12颗/普通6颗） |
| 攻击冷却内弧 | 玩家内圈弧线显示攻击CD进度，直观判断出手时机 |
| 攻击CD最大值追踪 | p.atkCdMax记录每次攻击的冷却值，支持冷却弧精确渲染 |

**测试维护：**
- 新增"距离阈值"版块（RANGES 9项自动渲染）
- 新增"波次缩放"版块（WAVE_SCALE 2项自动渲染）

*v1.7 更新于 2026-04-27。当前可玩：4武器、16进化（2个进化点）、40遗物、9波次、6类关卡调制器、9类普通敌人、1Boss、六构筑线加权选择。测试覆盖33项（28 smoke + 5 wave）。动画20，反馈7，武器6，敌人5，关卡4，HUD 7，机制4。共43项优化。*

### v1.7.1 动画打磨 + Bug修复

**目标：** 继续优化视觉反馈，修复连段追踪和击杀粒子相关的边缘情况。

**Bug修复（9项）：**

| 修复 | 内容 |
|------|------|
| comboHitId清理 | 敌人击杀或连段超时时清除comboHitId/comboHitCount，避免追踪死敌引用 |
| crit冲击环 | 暴击命中时6方向扩散朱砂粒子，强化暴击反馈 |
| Boss死亡爆发 | Boss死亡追加16方向accent+金色径向爆发粒子 |
| 蓄力判定一致性 | chargeTimer>50 → >=50，声效播放即生效，消除1帧延迟 |
| decoy攻击重定向 | 冲锋预备方向和远程弹道现在正确朝向decoy目标而非总是玩家 |
| DOM空值防护 | updateHUD/showEnd/showRelic/togglePause 全部getElementById增加空值检查 |
| 鼠标除零保护 | canvas尺寸为0时mouse坐标回退到玩家位置，避免NaN传播 |
| showEnd异常日志 | 空catch块改为console.error输出，方便诊断结算画面错误 |

**动画改进（14项）：**

| 改进 | 内容 |
|------|------|
| 处决暴击链光环 | execCritBonus激活时玩家外围显示脉动朱砂虚线环 |
| dash拖尾墨溅 | dash期间每3帧在玩家位置生成墨色粒子 |
| 武器特化击杀粒子 | 剑(墨溅5点)、笔(苔粒3×2)、铃(4方向苔粒)、伞(accent散射4×2) |
| 进化卡片脉动 | 进化卡片加朱砂色脉动光晕动画，区别于普通遗物卡 |
| 敌人出生动画 | 普通敌人弹跳渐入，精英敌人加冲击环和碎片散落 |
| 精英死亡消散 | 精英敌人死亡时碎片化瓦解+径向冲击波 |
| 屏幕闪白 | 暴击闪白(critFlash)和Boss闪白(bossFlash) |
| 粒子拖尾增强 | 移动每4帧方向性速度粒子，dash每2帧高密度拖尾 |
| 武器待机动画 | idleT追踪，低语微动效果 |
| 连段视觉翻新 | 颜色分级+风味短句替换单调连段数字 |
| HP条低血脉冲 | is-critical脉动动画 |
| HP条受伤/治疗闪烁 | is-damaged/is-healed过渡动画 |
| 遗物标签拾取弹跳 | is-new缩放弹跳动画 |
| Boss波次暗角脉动 | boss-wave-vignette全屏氛围效果 |

**CSS新增动画（10个关键帧）：**
hp-critical-pulse, hp-damage-flash, hp-heal-pulse, relic-pickup, kill-count-pop, boss-enraged-flash, weapon-switch-glow, boss-wave-vignette, hit-indicator-flash, dodge-ready-glow

**测试覆盖：** 36项冒烟测试全部通过（含进化筛选、Ash减速、Mask复活、双重击杀防护、护盾破坏、cleanupWave清理、CAPS封顶、PREREQS过滤等新测试）。

**代码规模：** game.js ~1855 行，game.css ~540 行。

### v1.6 数据提取 + 百科 + 逻辑优化

**目标：** 数据与逻辑分离、百科页面与游戏完全同步、热路径性能优化、死代码清理、魔数常量化。

**数据提取：**
- 从 game.js 中切出全部常量定义到独立 `gamedata.js`（~205行）
- 切出内容：W/H/A、C调色板、WEAPONS、RELICS、EVOLUTIONS、WAVES、STAGE_MODS、ETYPE、LIMITS、PREREQS、CAPS、RANGES、WAVE_SCALE、DEATH_COLOR、JUDGMENTS、PCOL、BUILD_PREFS
- game.html 和 wiki.html 均加载 gamedata.js，改一处数据两端同步
- 双击可用：file:// 协议正常运行
- 测试文件通过 `(0,eval)(dataCode)` 在 Node.js 中加载数据

**百科页面（wiki.html）：**
- 8个版块：武器、遗物（带标签筛选按钮）、进化（按武器分组）、敌人（Boss突出显示）、波次（含敌人组成+调制器）、关卡调制器、构筑线（6条）、数值封顶
- 数据全部从 gamedata.js 自动渲染，无硬编码
- 构筑线数据内联嵌入（DEVDOC中定义，gamedata.js中无此数据）

**导航：**
- 三个页面（index/game/wiki）顶部均有交叉导航链接
- game.html 的暂停、结算、武器选择界面也加入百科链接

**逻辑优化（game.js）：**

| 优化项 | 内容 | 效果 |
|--------|------|------|
| mkMinion 辅助函数 | 提取分身/召唤物创建逻辑，消除~30行重复对象字面量 | 可维护性 |
| RELIC_RULES 表驱动 | scoreRelicChoice 的50+条 if-else 转为声明式规则表 | 可维护性，新增遗物只需加一条规则 |
| dstSq() 平方距离 | 25+处热路径比较从 Math.sqrt 改为平方比较；dst() 改为调用 dstSq 的包装 | 每帧减少~70次 sqrt 调用 |
| hitMap O(1) 查找 | 攻击命中检测从 hitList.indexOf（O(n)）改为 hitMap 对象查找（O(1)） | 高 multi 攻击命中率提升 |
| hitE 单次遍历 | 恐惧/魂链/爆裂3次 forEach 合并为1次 | 每次命中减少2次全敌人扫描 |
| inkPoolCheck 合并 | inInkPool + inPurified 2个函数合并为 inkPoolCheck（返回0/1/2） | 区域扫描减半 |
| slowAmt 外提 | 敌人循环内重复计算改为循环外一次计算 | 微量 |
| hitList 分配消除 | 全部11处 hitList:[] 数组分配替换为 hitMap:{} | 每次攻击少分配一个数组 |
| RANGES 常量集中 | 9项距离阈值（恐惧/魂链/爆裂/诱饵/冲锋/弱点扩散/火场扩展/远程最小）从内联魔数提取为命名常量 | 可维护性、可调参 |
| WAVE_SCALE 常量 | 波次HP缩放(0.065/波)和速度缩放(0.012/波)提取为命名常量 | 可维护性、可调参 |
| reject() 清理 | scoreEvolutionChoice 中无意义包装函数替换为直接 `return null` | 可读性 |
| dstSq(e,p) 去重 | 敌人循环中 dstSq(e,p) 从2次计算合并为1次复用 | 微量 |

*v1.6 更新于 2026-04-26。当前可玩：4武器、16进化（2个进化点）、40遗物、9波次、6类关卡调制器、9类普通敌人、1Boss、六构筑线加权选择。并带性能观测、对象护栏、错峰 tick、遗物页修复、波次进场保护、进化选池修正、共享数据文件、百科页面、热路径优化、RANGES常量化、28项自动化测试覆盖全系统。*

**测试覆盖（28项 smoke_test + 5项 wave_test = 33项）：**

| 编号 | 测试内容 |
|------|----------|
| 1-4 | 4把武器各跑20帧+渲染、遗物选择3路、对象上限、性能追踪 |
| 5-6 | 9波完整推进、soulChain上限4 |
| 7-8 | 零直接push（代码扫描）、新敌人spawn+护盾 |
| 9-12 | critRate封顶、bellCombo封顶、遗物40件、returnInk hitMap |
| 13-15 | 进化选择、Boss狂暴、Boss 8方向弹幕 |
| 16-18 | 闪避无敌帧、冰冻区减速、纸鸢追踪伤害 |
| 19-22 | 护盾重生、焚灵火径、分身鬼分裂、纸鸢匠召唤 |
| 23-28 | inkPoolCheck合并函数、cleanupWave保留玩家火场、spawnGraceT保护、诱饵吸引、收阴袋护盾栈、mkMinion对象校验 |

### v1.5 Phase A-D 实施

**Phase A：选择与联动基座**
- 新增 `PREREQS` 前置条件映射（10件遗物），死词条在选牌前过滤，不再出现
- 新增构筑保底机制：标签重合3+时额外+5分，减少构筑断牌
- 过滤遗物：火祟牙(需夜灯)、返照铜片(需折返)、铃木鱼(需减速)、留影伞骨(需伞)、破镜残片(需伞)、风行纸马(需伞)、魂钱串(需魂链/铃木鱼+减速)、朱批残页(需铜镜)、血契剑穗(需近战)、灯油旧芯(需夜灯)

**Phase B：战斗对象与残留物治理**
- 新增 `cleanupWave(g)` 统一残留物生命周期管理
- 波次切换清理规则：eProj/attacks/particles/floatTexts/decoys 清空，fires 保留玩家火场（过滤非玩家火），kites/frosts 保留
- debug 面板新增来源级统计（pressure>40%时显示攻击类型/火场类型分解）

**Phase C：数值上限与软封顶**
- 新增 `CAPS` 常量集中管理所有软封顶值：critRate=0.65, bellCombo=15, shieldStack=3, atkCdFloor=4, soulChain=4, ringSoul=6
- 攻击冷却增加最低4帧保护，防止攻速堆叠导致冷却归零
- 所有硬编码封顶值统一引用 CAPS 常量

**Phase D：反馈与可读性**
- 弱点扩散触发时浮字"散"提示
- 火场扩张触发时追加火焰粒子
- 折返墨爆已有足够视觉反馈，不加额外文字

### v1.8 崩溃修复 + Boss打磨 + 常量去重 + 视觉优化

**目标：** 修掉P0崩溃点，打磨Boss战体验，提取硬编码常量，增加死亡慢动作。

**Phase 1：P0 崩溃修复（3项）**

| 修复 | 内容 |
|------|------|
| newGame武器ID校验 | 无效wid回退到斩妖剑（WEAPONS[0]），避免w=undefined导致后续崩溃 |
| inkPoolCheck zones空值保护 | 条件加 `\|\|!g.stage.zones`，防止zones数组为undefined时crash |
| 敌人数量上限 | LIMITS加 `enemies:80`，spawnEnemy/splitter/summoner三处push前检查容量 |

**Phase 2：Boss/平衡（3项）**

| 改进 | 内容 |
|------|------|
| 波次难度平滑 | 第陆波fenshen(1)移至第伍波，10→9（降10%），消除67%的单跳增幅 |
| Boss第三阶段（绝望） | HP<25%触发 `desperate`：攻速×1.4、弹数+2、震屏shake(8,8) |
| 胜利序列增强 | freezeT=35、60颗accent粒子、40层墨溅、武器色墨爆、震屏 |

**Phase 3：常量提取 + 去重（2项）**

| 改进 | 内容 |
|------|------|
| TUNING常量区块 | 20个命名常量（playerSpd/Hp/R、dodge参数、boss阈值、combo倍率等），game.js中~20处硬编码替换为TUNING.*引用 |
| 辅助函数 | `screenFlash(c,w,h,a,iC,oC,iR,oR)` 合并4处屏幕闪光，`collideSq(a,b,extraR)` 替换~15处碰撞检测，节省~42行 |

**Phase 4：视觉优化（3项）**

| 改进 | 内容 |
|------|------|
| 死亡慢动作 | `g.state="dying"` 中间状态，freezeT=22 + 同心墨色+accent双环扩散动画，12方向墨粒爆发 |
| Boss波次CSS激活 | startWave检测boss→给 `.game-frame` 加 `is-boss-wave` class，激活预置CSS关键帧动画 |
| Boss入场特效 | `bossWaveEntrance=50` 帧黑暗渐晕 + 震屏，仪式感登场 |

**TUNING 常量一览：**

| 常量 | 值 | 用途 |
|------|-----|------|
| comboWindow | 45 | 连段超时帧数 |
| chargeThreshold | 50 | 蓄力满阈值帧数 |
| playerSpd/playerHp/playerR | 3.4/100/15 | 玩家基础属性 |
| dodgeDuration/Cooldown/InvFrames | 10/42/14 | 闪避参数 |
| justDodgedWindow | 28 | 完美闪避窗口 |
| hurtInvFrames | 30 | 受伤无敌帧 |
| lowHpThreshold | 0.3 | 低血量阈值 |
| bossEnrageHp/bossDesperateHp | 0.5/0.25 | Boss阶段切换HP比例 |
| bossEnrageSpdMult | 1.3 | Boss狂暴速度倍率 |
| reviveHpRatio | 0.3 | 复活血量比例 |
| fastAtkCdMult/fastAtkParticleMult | 0.55/0.5 | 快速攻击冷却/粒子倍率 |
| combo3Arc/combo3Range/combo3Dmg | 1.4/1.25/1.35 | 剑第三段加成 |
| shieldStackReduction | 4 | 每层护盾减伤值 |
| inkPoolDmgMult | 1.3 | 墨池敌人增伤倍率 |

**代码规模：** game.js 1878 行 (+23 vs v1.7.1)，gamedata.js 217 行 (+10)。

*v1.8 更新于 2026-04-27。*

### v1.9 演示体验打磨

**目标：** 解决5个hackathon演示体验缺口——标题画面、过渡动画、环境音、叙事氛围、操作引导。

**标题画面（HTML+CSS）：**
- 新增 `#titleScreen` overlay，游戏首次打开时显示
- 游戏名"墨祟"大字 + "走阴录"副标题 + 氛围文案 + "进入地宫"按钮
- 所有元素交错淡入动画（0.2s~1.5s延时）
- 点击按钮隐藏标题、显示武器选择
- 重新开始回到武器选择（不回标题）

**Overlay淡入动画（CSS）：**
- `.overlay__inner` 加 `overlay-enter` 关键帧：从 scale(0.97)+translateY(8px) 渐入
- 所有遮罩层（遗物/进化/暂停/结算）自动获得入场动画，零JS改动

**环境音循环（sound.js）：**
- 3个低频振荡器（55Hz正弦+82.41Hz正弦+110Hz三角）构成暗色底噪pad
- 0.08Hz LFO驱动基频微漂移，增加呼吸感
- 音量0.035，不干扰战斗音效
- `startGame()` 中启动，`showEnd()` 中0.5s渐消停止
- `GameSound.startAmbient()/stopAmbient()` 对外暴露

**波次氛围文字（gamedata.js + game.js）：**
- WAVES 每条新增 `flavor` 字段，描述当前地宫区域的叙事氛围
- 波次公告渲染时在关卡描述下方显示 flavor 文字（13px 灰色，55%透明度）
- 示例："纸门之后，邪祟初现。小心试探。" "画皮娘子镇守此地。她有千面，你的刀只有一面。"

**操作引导召回（game.js）：**
- 按 H 键可切换 controlsHint 显示/隐藏
- 原有3秒自动隐藏逻辑保留

**代码规模：** game.js 1892 行 (+14)，gamedata.js 217 行（行内修改），sound.js 535 行 (+48)。

*v1.9 更新于 2026-04-27。*

### v1.10 波次节奏 + 结算打磨 + 音效扩展

**目标：** 改善波次间节奏感，增强结算画面表现力，补充关键UI音效。

**波次间喘息延迟：**
- 所有敌人清除后，延迟50帧（~0.83秒）才弹出遗物选择
- 延迟期间：波次清除动画立即播放、"波次完成"文字渐现渐隐、waveClear音效触发
- 延迟结束后：调用 showRelic() 弹出遗物/进化选择
- `g.waveCleared` + `g.waveClearT` 标记控制，startWave 时重置
- 波次清除动画从 showRelic() 移至 wave check 触发点

**结算画面增强：**
- 三行显示：第一行基础统计，第二行构筑身份（武器→进化），第三行遗物名称列表
- 构筑身份行用 title-face 字体+ink颜色突出显示
- 遗物名称行用 ash 色+缩小字号，不抢视觉焦点
- overlay 宽度从 480px 扩至 540px 适配多行内容
- CSS 新增 `.end-build` 和 `.end-relics` 样式类

**UI音效扩展（sound.js）：**
- `waveClear`：上行三音（440→554→659Hz 正弦），轻快愉悦的波次完成提示
- `bossIntro`：低频锯齿波隆隆声（40Hz）+ 高频正弦下行（880→440Hz），Boss登场仪式感
- Boss波次触发音效从 `bossEnrage` 改为 `bossIntro`（bossEnrage 仍保留，Boss狂暴时仍使用）

**代码规模：** game.js 1903 行 (+11)，sound.js 567 行 (+32)。

*v1.10 更新于 2026-04-27。*

### v1.11 UI打磨 + 书法序列

**目标：** 增强关键瞬间的视觉冲击力，补充UI交互反馈。

**暂停画面增强：**
- 暂停overlay新增操作提示（WASD/空格/鼠标/H键）
- 动态显示当前波次/斩祟数/遗物数（`#pauseStats`）

**快捷键：**
- R键：在game over/victory画面可直接重开，无需点击按钮
- 波次清除文字从固定"波次完成"改为动态显示波次名（如"第壹波 · 纸门 · 完成"）

**胜利书法序列（Canvas叠加）：**
- 胜利35帧冻结期间，画面暗化+墨色圆环+accent色内环渐扩
- 中央52px楷体显示武器名（如"斩妖剑"）
- 下方18px"走 阴 完 毕"副标题
- 所有元素随冻结进度渐入（0→100%透明度）

**死亡书法序列：**
- 死亡22帧冻结期间，画面暗化
- 中央44px楷体"魂 归 黄 泉"渐入
- 配合原有同心墨色+accent双环扩散动画

**遗物类型徽章：**
- 遗物卡片type字段改为独立徽章样式（`.relic-type-badge`）
- 圆角小标签，背景微灰，字号0.68rem

**武器选择hover音效：**
- 鼠标悬停武器卡片触发 `uiBlip`（660Hz正弦，0.04音量，0.06秒）
- `snd()` 调用需 `GameSound` 已初始化（首次点击后生效）

**音效扩展（sound.js）：**
- `uiBlip`：轻量UI交互提示音
- 总音效数：22个

**代码规模：** game.js 1937 行 (+34)，sound.js 577 行 (+10)，game.css 619 行 (+42)，game.html 95 行 (+9)。

*v1.11 更新于 2026-04-27。*

### v1.12 游戏体验增强

**目标：** 补充关键UX反馈，增强视觉细节。

**屏外敌人指示器（Canvas渲染）：**
- 当敌人在竞技场边界外时，在屏幕边缘显示三角形箭头指向敌人方向
- Boss用accent色，普通敌人用ink色，50%透明度
- 箭头位置沿屏幕边缘clamp，不超出画布
- 忽略已死亡或有spawnGraceT的敌人

**Boss绝望增强：**
- Boss进入绝望阶段时额外生成12方向火焰粒子环
- 配合原有：墨爆+accent墨迹+冻结+震屏+浮字"回光返照"

**遗物卡片hover音效：**
- 鼠标悬停遗物/进化卡片触发 `uiBlip`

**标题画面装饰（CSS）：**
- `.title-screen` 伪元素 `::before`/`::after` 添加径向渐变墨斑
- 左上角墨色墨斑(180px)、右下角accent色墨斑(140px)
- 延迟1.8秒后淡入，增加水墨氛围

**代码规模：** game.js 1954 行 (+17)，game.css 638 行 (+19)。

*v1.12 更新于 2026-04-27。*

### v1.13 深度游戏性扩展

**目标：** 利用扩展的代码预算，增加游戏深度和视觉层次。

**Boss冲锋攻击（绝望阶段）：**
- Boss HP<25%后，每180帧发起一次高速冲锋（速度5.5，持续14帧）
- 冲锋路径留下火场（每2帧一个），触碰造成1.4倍伤害
- 冲锋前触发 `bossEnrage` 音效 + 震屏
- 使用独立的 `bossChargeT/bossChargeCdT` 状态，不与僵客冲锋系统冲突

**关卡专属环境音：**
- `STAGE_AMBIENT` 配置表：6种关卡调制器各有独特音色
- 纸灰风(ash)：锯齿波65/98/130Hz，更快LFO(0.15Hz)，风声感
- 悬井(well)：纯44/66/88Hz正弦，最慢LFO(0.04Hz)，深沉压迫
- 无面戏台(mask)：三角波+五度音程55/73.42/110Hz，诡异调性
- 鬼灯阵(lantern)：高音区73.42/110/146.83Hz，明亮而不安
- 墨池(inkpool)：最低36.71/55/73.42Hz，含锯齿波，深渊感
- 净坛(calm)：原55/82.41/110Hz（默认）
- 每次startStage自动切换环境音色调

**精英敌人变体：**
- 第3波起，15%概率生成精英敌人（Boss/分裂体/召唤物除外）
- 精英：HP×1.5、速度×1.3
- 视觉：脉动金色光环（sin波0.12Hz），shadowBlur=8
- 渲染于hitFlash之后，prepT之前

**新手引导提示：**
- `showHint(g,key,text)` 系统，每种提示只触发一次
- 首次遗物选择："选择一件遗物 — 它会强化你的构筑"
- 首次进化选择："选择一项武器进化 — 强化你的攻击方式"
- 首次Boss波次："Boss战 — 注意她的多阶段变化"
- 提示显示为ash色14px楷体，150帧（2.5秒），不干扰战斗

**波次墨迹转场：**
- 非首波开始时，画面从中心展开墨色圆形遮罩（30帧）
- 墨迹从全屏→收缩至零，揭示新的波次场景
- 使用 `rect + arc(逆时针)` 的 hole-punch 技巧实现
- `g.inkWipe` 计时器，update中递减，render中绘制

**代码规模：** game.js 1998 行 (+44)，sound.js 584 行 (+7)。

*v1.13 更新于 2026-04-27。*

### v1.14 难度系统 + UX反馈

**目标：** 增加难度选择和多层次反馈系统。

**难度选择（平常/险途/噩梦）：**
- HTML单选按钮，武器选择页面下方
- DIFF配置表：normal/hard/nightmare 三档 hpM/spdM/dmgM
- hard: HP×1.35、速度×1.15、伤害×1.25
- nightmare: HP×1.8、速度×1.3、伤害×1.5
- spawnEnemy中应用所有倍率，startGame读取radio值传入newGame

**遗物协同反馈：**
- 拾取遗物时检测与已拥有遗物的tag交集
- 有交集时显示金色"协同 · {tag}"浮动文字
- C.gold色(9c835a)，700字重，shadowBlur=8

**敌人初见提示：**
- ETYPE每条新增tip字段（如"远程弹射，注意闪避弹幕"）
- g.encountered对象追踪已遇类型
- 首次遭遇显示"{名} — {tip}"，120帧

**代码规模：** game.js 2048 行 (+50)，gamedata.js 217 行。

### v1.15 演示打磨 — 视觉/音效/信息层

**目标：** 打磨演示体验的所有感官层。

**连斩里程碑：**
- 3连斩 / 5连斩 / 10连斩 / 百鬼夜行(20连)
- 大号accent色楷体(28-40px)，shadowBlur=12
- 5连+震屏，10连+屏幕闪光，20连+强震屏
- 5连斩+10%伤害，10连斩+20%伤害（damageEnemy中应用）

**结算画面增强：**
- 显示难度标签（颜色区分：ash/accent/深红）
- 显示游戏时长（分:秒格式，基于g.time/60fps）
- 难度专属通关文案（噩梦："万邪辟易"，险途："胆识过人"）

**小地图雷达：**
- 左下角80×80px半透明面板
- 以玩家为中心，敌人相对位置为彩色点
- Boss=accent大点、精英=gold中点、普通=ink小点
- 玩家=f7efe5白色小点

**方向伤害指示：**
- 受击时从攻击源方向渲染红色径向渐变扇形
- 0.7弧度宽，ddR为画面最大尺寸×0.7
- 20帧衰减，存储在g.dmgDir

**暂停体验：**
- 暂停时环境音量降至10%，恢复时回60%
- Canvas叠加30%墨色遮罩

**敌人血条优化：**
- 仅在HP<maxHp时显示（满血不显示），减少视觉噪音

**HUD增强：**
- 武器名后显示进化路径（如"墨笔 → 连珠 + 爆裂"）
- 波次信息前加难度标签（险途/噩梦）
- 波次进度条加宽至200px，>80%时发光

**死亡回放：**
- 死亡圆环动画中显示"为你送终"+ 击杀者名称
- 存储在deathCircle.killer字段

**低血量晕影：**
- HP<30%时持续脉冲红色径向晕影
- 强度随血量比例加深（6%-18%透明度）

**治疗反馈：**
- killHeal和fireHeal触发时显示苔绿色"+N"浮动文字
- 12-15px，600字重，C.moss色

**进化专属音效：**
- playEvoPickup(): 四音和弦(220/277/329/440Hz)
- 交错入场（每音间隔0.04秒），三角波+正弦混合
- 0.35秒衰减，比遗物拾取音更庄重

**代码规模：** game.js 2138 行 (+90)，sound.js 601 行 (+17)，gamedata.js 217 行。

*v1.15 更新于 2026-04-28。*

### v1.16 视觉打磨 + 评级系统

**目标：** 提升打击感和重玩价值。

**弹道拖尾粒子：**
- 玩家弹丸每3帧生成1个accent色拖尾粒子（8帧生命期）
- 敌方弹丸每4帧生成1个ink色拖尾粒子（6帧生命期）
- 仅在性能裕度>40%时激活，避免卡顿

**关卡环境粒子：**
- 每120帧（~2秒）生成1个缓慢上浮的环境粒子
- 颜色随关卡调制器变化：calm=ink, ash=ash, well=moss, mask=ghost, lantern=fire, inkpool=ink
- 140帧生命期，极慢漂移速度

**结算评级系统：**
- 综合评分：通关+40分、击杀数×0.3（上限100）、遗物数×2、难度加成(hard+10/nightmare+20)、通关时HP比例×10
- S(90+)=深红、甲(75+)=accent、乙(60+)=ink-soft、丙(40+)=ash、丁(<40)=ash
- 结算画面顶部大字显示评级

**闪避/冲刺拖尾：**
- 闪避时每2帧生成ink色粒子（墨迹飞溅感）
- 冲刺时每2帧生成accent色粒子

**暴击增强：**
- 暴击粒子从6个增至8个，8方向均匀分布
- 暴击闪光持续12帧（原10帧），内圈透明度更高

**攻击命中微冻结：**
- 所有命中（非暴击）添加1帧冻结
- 暴击保持3帧冻结（快速连击时1帧）
- 给予每次命中"重量感"

**墨迹遗物视觉：**
- inkTrail激活时，每12帧除减速区域外额外生成1个ink粒子

**代码规模：** game.js 2169 行 (+31)，gamedata.js 217 行，sound.js 601 行。总计 2987 行。

*v1.16 更新于 2026-04-28。*

### v1.17 逻辑维护 + 反馈增强

**目标：** 审查边界条件、修复潜在问题、增强关键反馈。

**代码审查结果：**
- waveTotal除零：已有 `g.waveTotal>0` 守卫，非问题
- deathCircle.life递减：fade使用 `r/maxR` 而非life，逻辑正确
- forEach中push/hp=0：标准forEach快照长度，不会崩溃但可能跳过元素（可接受）

**完美闪避反馈环：**
- justDodged激活时，玩家周围显示扩展金色圆环
- 圆环从 p.r+6 扩展至 p.r+20，透明度随时间衰减
- C.gold色，shadowBlur=6

**Boss阶段转换强化：**
- 怒阶段（50%HP）：显示"画皮娘子 · 怒"大字公告 + bossFlash=8
- 绝望阶段（25%HP）：显示"画皮娘子 · 绝望"大字公告 + bossFlash=12
- bossFlash渲染适配为12帧最大值

**离屏敌人指示器增强：**
- Boss=accent大三角（10px）、精英=gold中三角（7px）、普通=ink小三角（6px）
- 透明度分层：Boss 0.7、精英 0.6、普通 0.4

**遗物拾取HP条治疗动画：**
- updateHUD追踪HP变化，HP上升时触发 `.is-healed` CSS动画
- 500ms后自动移除class，利用CSS已有动画

**波次清除庆祝增强：**
- 原有玩家周围12粒子 + 新增画面中心16粒子径向爆发
- 颜色随关卡调制器变化，增强完成感

**攻击命中微冻结：**
- 非暴击命中添加1帧冻结，暴击保持3帧
- 所有命中都有"打击重量感"

**代码规模：** game.js 2189 行 (+20)，gamedata.js 217 行，sound.js 601 行。总计 3007 行。

*v1.17 更新于 2026-04-28。*

### v1.18 视觉差异 + 打击感强化

**目标：** 让武器选择更有意义、Boss死亡更震撼、打击感更强。

**武器专属玩家剪影：**
- 近战(剑)：面向方向延伸的3px笔直线段
- 远程(笔)：面向方向的三角笔尖
- 范围(铃)：头顶4px圆形铃铛+连接线
- 冲刺(伞)：身后2.5px弧形伞骨
- 透明度0.4，叠加在武器颜色环之上

**分数倍率显示：**
- 连斩时在"X连斩"下方显示金色"x1.5"文字
- 倍率 = 1 + min(killStreak × 0.1, 2.0)
- 与连斩文字同步淡出

**击杀里程碑音效：**
- playKillMilestone(): 四音上行和弦(523/659/784/1047Hz)
- 正弦+三角混合，交错入场(每音间隔0.06秒)，0.3秒衰减
- 10/25/50/100击杀时触发

**Boss死亡慢动作：**
- Boss被击杀时触发30帧慢动作(g.slowMo)
- 慢动作期间每隔一帧跳过update，实现半速效果
- 叠加暗色晕影（强度0.15→0），增强戏剧性

**难度专属死亡文案：**
- 噩梦死亡："噩梦未竟，魂散地宫。"
- 险途死亡："险途折戟，来日再战。"
- 平常死亡：保持原"纸灰掩面"文案

**代码规模：** game.js 2219 行 (+30)，gamedata.js 217 行，sound.js 617 行 (+16)。总计 3053 行。

*v1.18 更新于 2026-04-28。*

### v1.19 Bug修复 + 联动修正

**目标：** 修复减速+伤害联动失效、胜利动画缺失、音效错配等隐藏bug。

**减速联动修复（核心）：**
- `slowT` 赋值从 `=` 改为 `Math.max()`，修复多减速源互相覆盖问题
- 影响3处：slowOnHit(hitE line 718)、ringSlow(update line 1039)、inkTrail(update line 1046)
- 修复后：玄冰简穗(slowOnHit=0.4) + 扩域进化(ringSlow) 不再互相覆盖，取最大值

**铃木鱼联动反馈：**
- ringSoulHit触发时追加"魂+N"浮动伤害数字
- 新增"soul"类型浮动文字渲染样式（墨绿色、楷体）
- 使铃+减速组合的额外伤害可视化

**胜利动画修复（关键）：**
- `update()` 未处理"victory"状态，导致freezeT永不递减
- 现象：胜利时canvas水墨动画（墨圈+武器名）永远停在vProg=0不可见
- 修复：loop中增加"victory"状态到update调用条件，update中新增victory handler递减freezeT
- showEnd改为仅freezeT<=0时触发，让胜利动画先播完再出结算画面

**音效修复：**
- 墨盾鬼护盾重生播放"shieldBreak"改为"frostCreate"，避免误导

**测试覆盖：** 36项冒烟测试全部通过。

**代码规模：** game.js ~2228 行 (+9)，gamedata.js 217 行，sound.js 617 行。总计 3062 行。

*v1.19 更新于 2026-04-28。*

### v1.20 移动端迁移

**目标：** Capacitor Android 打包 + 双虚拟摇杆，桌面端键鼠操控不变。

**架构原则：**
- game.js 核心逻辑不动，仅加 5 处移动输入桥接点（+18 行）
- 新增 `mobile-controls.js`：虚拟摇杆渲染 + 触摸事件（独立文件，非触摸设备自动跳过）
- Capacitor 只做打包壳，不侵入游戏代码
- 双端共用同一份 game.js，修 bug / 加内容两端同时受益

**新增文件：**

| 文件 | 行数 | 职责 |
|------|------|------|
| `mobile-controls.js` | 190 | 触摸检测、双虚拟摇杆、闪避/暂停按钮、canvas 渲染 |
| `capacitor.config.json` | 8 | Capacitor 配置（appId/webDir/横屏） |
| `package.json` | 22 | npm 脚本（www同步/cap同步/Android打开） |
| `.gitignore` | 4 | 忽略 node_modules/www/android/ios |

**mobile-controls.js 设计：**
- 触摸设备检测：`ontouchstart in window || navigator.maxTouchPoints > 0`
- 左摇杆（移动）：左下角 `(W*0.17, H*0.74)`，半径 50px
- 右摇杆（瞄准+攻击）：右下角 `(W*0.83, H*0.74)`，拖动即瞄准+攻击
- 闪避按钮：`(W*0.68, H*0.72)`，标签"闪"
- 暂停按钮：右上角 `(W-30, 30)`，标签"停"
- 半屏分配 + touchId 追踪，支持双指同时操作
- 死区 8px，归一化输出 -1~1
- 全部 canvas 绘制，水墨风格（半透明墨色圆，匹配游戏调色板）
- 通过 `window._mobileInput` 注入输入状态，通过 `window._renderMobileControls(c,W,H)` 渲染

**game.js 桥接改动（5 处，+18 行）：**

| 位置 | 改动 |
|------|------|
| update() 移动 (~L825) | 读取 `_mobileInput` dx/dy 作为移动源，替代 keys |
| update() 面朝 (~L833) | 摇杆 aimAngle 覆盖 mouse 瞄准 |
| update() 攻击 (~L869) | `_mobileInput.attacking \|\| mouse.down` |
| update() 闪避/暂停 (~L808,837) | 移动端 dodge/pause 触发 |
| render() 末尾 (~L1819) | 调用 `_renderMobileControls` 钩子 |

**sound.js 移动端修复：**
- `init()` 增加 AudioContext suspended 状态处理
- 移动浏览器首次触摸时 `ctx.resume()` 激活音频

**game.html 更新：**
- viewport meta 加 `user-scalable=no, viewport-fit=cover`
- 加载 `mobile-controls.js`（在 game.js 之后）

**game.css 更新：**
- `body.game-active` 加 `touch-action:none; overscroll-behavior:none; user-select:none`
- 移动全屏 canvas 样式：去除边框阴影，`max-width:100vw; max-height:100vh`

**Capacitor 配置：**
- appId: `com.mosui.zouyinlu`
- webDir: `www/`（npm script 同步 game 文件）
- Android 横屏锁定：`android:screenOrientation="sensorLandscape"`

**打包命令：**
```bash
npm run www          # 同步文件到 www/
npm run cap:sync     # 同步 + cap sync
npm run cap:open:android  # 用 Android Studio 打开
```

**测试覆盖：** 36项冒烟测试全部通过。

**代码规模：** game.js ~2245 行 (+17)，mobile-controls.js 190 行，sound.js 620 行 (+3)，game.css 691 行 (+34)。总计 3750 行。

*v1.20 更新于 2026-04-28。*

### v1.21 Bug修复 + 横屏锁定 + 文档规整

**目标：** 修复 v1.20 遗留 bug，完善移动端横屏体验，清理项目文件结构。

**Bug 修复（7 项）：**

| Bug | 严重度 | 修复 |
|------|--------|------|
| 移动端暂停后无法恢复（update() 在 paused 状态直接 return，mobile pause 检测永远不执行） | 严重 | 将 mobile pause 检测移至 state guard 之前 |
| 暂停时双层遮罩叠加（render 35% + loop 30% = 54%） | 高 | 移除 loop() 中的重复遮罩 |
| 护盾重生播放错误音效（frostCreate） | 中 | 改为 chargeReady |
| 伤害数字显示原始攻击力（未含连击/弱点加成） | 中 | 使用实际 dmg 值 |
| 窗口失焦时按键粘滞（无 blur 事件清理 keys） | 中 | 添加 window blur handler 清空 keys + mouse.down |
| render() 异常时 canvas save/restore 不平衡 | 中 | 包裹为 try-finally |
| 移动端竖屏无提示 | 中 | 新增旋转提示 + Screen Orientation API 锁定 |

**移动端横屏锁定（三层）：**
- CSS：`@media (orientation: landscape)` 控制旋转提示显隐
- JS：`screen.orientation.lock("landscape")` + resize/orientationchange 监听
- Android：`android:screenOrientation="sensorLandscape"`（已有）
- 竖屏时全屏黑底显示"↻ 请旋转手机横屏游玩"，横屏自动消失

**项目清理：**
- 删除 `game.js.bak`（过期备份）和 `PLAN_v1.4.md`（已执行的旧计划）
- 新增 `README.md`（中文项目说明）
- DEVDOC 版本号更新

**测试覆盖：** 36项冒烟测试全部通过。

**代码规模：** game.js ~2250 行，mobile-controls.js ~200 行，game.css ~710 行。

*v1.21 更新于 2026-04-28。*

### v2.0 深度游戏性扩展

**目标：** 增加誓印系统、元进度、程序化波次、第二Boss、精英敌人等，大幅提升重玩价值。

**誓印系统（诅咒）：**
- 6 种誓印（墨缚/噬魂/纸薄/贪墨/孤行/鬼见愁），开局前选择
- 双刃剑设计：获得力量的同时承担代价
- CSS 新增 `.curse-card` / `.curse-skip` 样式
- game.js 新增 `beginRun()` 中应用誓印效果
- showRelic 中检查 noWaveHeal/noEvolution 标记

**元进度系统（localStorage）：**
- `mosui_meta` 键存储跨局统计：总击杀、总局数、最高波次、最佳评级、Boss击杀、武器通关、遗物发现
- `loadMeta()/saveMeta(m)` 读写 localStorage
- `calcGrade(g)` 五级评分：S/甲/乙/丙/丁
- 标题画面显示元统计（总击杀、最佳评级等）
- 结算画面显示评级 + 新纪录标识 + 图鉴发现数

**程序化波次生成：**
- `generateWave(wi, diff)` 替代硬编码 WAVES，支持无限波次
- `WAVE_BUDGETS[12]` 威胁预算递增：5→7→9.5→12→14.5→17.5→21→25→28→32→36→Boss
- `WAVE_TIERS[5]` 敌人解锁层级，逐级引入新敌人
- `ENEMY_COST` 每种敌人消耗预算值
- 波次名从 `WAVE_PLACES[14]` 随机取，风味文从 `WAVE_FLAVORS[5]` 随机取
- `CN_NUM` 扩展至 12 项（拾/拾壹/拾贰）
- 胜利条件从 `WAVES.length` 改为 `WAVE_BUDGETS.length`

**第二Boss：墨将军（mojiangjun）：**
- `ETYPE` 新增条目：HP 480、速度 0.8、半径 28、伤害 15
- 专属 AI（独立于画皮娘子的狂暴/弹幕系统）
- `e.type!=="mojiangjun"` 守卫防止共享 Boss 通用代码

**精英敌人系统：**
- 4 种精英能力：blink（瞬移）、deathburst（死亡爆炸）、enrage（激怒）、armored（装甲）
- 精英 HP×1.5、速度×1.3
- 金色脉动光环视觉

**帧延迟系统：**
- `g.pendingDeathbursts[]` 替代 `setTimeout`
- update 中每帧递减 timer，到期后执行效果
- 避免 setTimeout 在游戏循环外的竞态条件

**结构化遗物增强（relicPower）：**
- 从字符串匹配改为结构化字段：soulDmg、killHeal、decoyHP、stats.dmg、stats.critDmg
- 噬魂誓印的 relicPower 从 1.5 提升至 1.8

**Bug 修复（17 项中完成 12 项）：**

| 修复 | 内容 |
|------|------|
| generateWave 引用错误 | `_pick()`/`_ri()` 定义在 gamedata.js |
| WAVES[g.wave] undefined | HUD 改用 g.announce，胜利用 WAVE_BUDGETS.length |
| justDodged 全武器加成 | 所有武器享受完美闪避 +20% 伤害 |
| maxHpOverride/extraStartRelics | beginRun 正确应用誓印初始效果 |
| extraRelicChoice | pickRelicChoices 支持 4 选 1 |
| relicPower 结构化 | 不再用字符串匹配 |
| deathburst 帧化 | setTimeout → pendingDeathbursts |
| Boss AI 守卫 | mojiangjun 不共享画皮娘子代码 |
| Boss 名动态化 | 硬编码"画皮娘子"→ e.name |
| 重复触摸事件移除 | 去掉 game.js 中重复 touchstart 监听 |
| 重复 spawnEnemy 键 | elite/enraged 重复赋值清理 |
| document.querySelector 防护 | 测试 mock 兼容 |

**平衡调整：**
- 镇魂铃增强：dmg 10→13, cd 36→28
- 孤行誓印削弱：stats +0.5 → +0.35
- 噬魂誓印增强：relicPower 1.5→1.8
- WAVE_BUDGETS 扩展至 12 波 + Boss

**代码规模：** game.js ~2400 行 (+150)，gamedata.js ~300 行 (+83)，game.css ~810 行 (+100)。总计 ~3510 行。

*v2.0 更新于 2026-04-29。*

### v2.1 移动端横屏 UI 优化

**目标：** 针对手机横屏（高度 340-500px）全面重绘 UI 叠加层，优化虚拟摇杆操控体验。

**横屏手机 CSS（game.css +120 行）：**

两个高度分级媒体查询，放在现有触摸查询之后：

| 查询 | 目标设备 | 关键调整 |
|------|----------|----------|
| `max-height: 500px` | 短屏手机（iPhone/三星等） | 极度紧凑：padding 10px 18px，字体 0.55-1.15rem，HUD 缩小 |
| `min-height: 501px` + `max-height: 680px` | 较高手机/小平板 | 适度紧凑：padding 14px 24px，字体略小 |

**标题画面重构：**
- 从垂直堆叠改为 CSS Grid 两列布局
- 左列：标题 + 副标题，右列：描述文字
- 底部横跨：按钮 + 链接
- 关闭装饰伪元素和入场动画

**武器选择优化：**
- 4 列超紧凑卡片（短屏 gap 6px，padding 8px）
- 标题 0.88rem，描述 0.66rem

**遗物/诅咒选择优化：**
- 从水平滚动改为 3 列网格布局（更适合横屏的宽高比）
- 卡片极度紧凑（padding 8px 8px）

**HUD 缩小：**
- HP 条 80px × 7px（原 120px × 10px）
- 所有文字降至 0.62-0.7rem
- Boss 名 top: 30px

**自适应虚拟摇杆（mobile-controls.js）：**

| 改进 | 内容 |
|------|------|
| CTRL 动态对象 | 摇杆/按钮尺寸随 canvas 显示比例缩放 |
| 位置下移 | leftBase/rightBase Y 从 0.74→0.82（更贴拇指） |
| 闪避按钮重定位 | dodgePos 从 (0.68, 0.72)→(0.73, 0.82) |
| 攻击阈值降低 | ATTACK_THRESHOLD 0.003→0.001（更快响应） |
| fitCanvas 联动 | 每次 resize/orientation 时重算控件尺寸 |

**测试覆盖：** 36项冒烟测试全部通过。

**代码规模：** game.js ~2400 行（未改），mobile-controls.js ~263 行 (+63)，game.css ~930 行 (+120)。

*v2.1 更新于 2026-04-29。*

### v2.2 平衡修正与系统深化

**目标：** 修复关键 bug、平衡调整、新增特殊波次和 Boss 阶段机制。

**致命 Bug 修复：**
- 删除 game.js 中重复的敌方弹道处理循环（原 1129-1136 / 1137-1144 行），该 bug 导致弹道每帧移动两次、玩家受双倍伤害

**遗物平衡调整：**

| 遗物 | 修改前 | 修改后 |
|------|--------|--------|
| 血烛祭片 | 范围+20%，无代价 | 范围+25%，受伤+10%（禁物代价） |
| 无字谶符 | 固定+5魂伤 | +8魂伤 + 每遗物+1（后期成长） |
| 骨血墨 | critRate+15%，非暴击-20% | critRate+18%，非暴击-12%（DPS正向） |

**波次系统增强：**
- `WAVE_TIERS` 重构：波1起出现游魂，波2起出现焚灵，加速敌人多样性
- 特殊波次系统（`generateWave` 新增 `special` 字段）：
  - 群魔潮（Horde）：+70%威胁预算，海量敌人
  - 精英潮（Elite）：所有敌人皆为精英
  - 生存波（Survival）：+30%预算，持续压力
  - 噩梦专属精英狂潮（Elite Horde）：+40%预算且全精英
  - 波3起20%概率触发特殊波

**墨将军 Boss 增强：**
- HP 条加入 60%/25% 阶段标记线
- 阶段名称显示（>60%: 墨将军, 25-60%: 墨将军 · 召书, <25%: 墨将军 · 狂书）
- 画皮娘子保持原有的 enrage/desperate 标记系统

**难度精英缩放：**
- `spawnEnemy` 精英概率随难度调整：普通 max 0.3, 困难 +8%, 噩梦 +18%
- 困难模式精英概率上限提升至 0.38，噩梦模式 0.48

**测试修复：**
- `wave_test.js` 更新以适配 v2.0 程序化波次系统
- `killAll` 帧数从 40 增至 90（处理 freezeT + deathT + waveClearT 时间线）
- 提前 update 帧数从 8 增至 20

**代码规模：** game.js ~2440 行 (-14 行，删除重复循环)，gamedata.js ~315 行 (+15)，wave_test.js ~145 行 (+3)。

*v2.2 更新于 2026-04-29。*

### v2.4 移动端稳健修复 + UI 精细化 (2026-04-29)

**目标：** 修复移动端崩溃 Bug、完善触摸控制视觉反馈、连击计数修正。

**致命 Bug 修复：**
- **mobile-controls.js 初始化崩溃：** `updateControlSizes()` 在位置变量（`leftBase`/`rightBase`/`dodgePos`/`pausePos`）定义之前被调用，导致 TypeError，整个触摸控制系统静默崩溃。移动端因此退化为基础的 touch→mouse 模拟，双摇杆、闪避按钮全部失效。修复：将 `updateControlSizes()` 调用移至变量初始化之后。
- **誓印弹窗移动端超时防护：** 触摸端誓印选择 20 秒无操作自动跳过，防止因触摸事件异常导致的卡死在弹窗。

**连击系统修正：**
- **`comboCount` 计数时机：** 从 `pAtk()`（每次攻击）移至 `hitE()`（确认命中敌人）。修复"空挥也算连击"的 bug，连击数现在准确反映实际命中次数。

**暂停菜单修复：**
- **按钮 ID 冲突：** 暂停页面和结算页面的"重新开始"按钮 ID 重复（`restartBtn`），导致事件监听绑定混乱。修复：暂停页按钮改为 `pauseRestartBtn`/`pauseQuitBtn`。
- **重新开始/退到标题逻辑完善：** 正确清理旧游戏状态（`G=null`、停止环境音效、清除按键状态），防止状态残留。
- **暂停按钮边沿触发：** 修复持续按住暂停按钮会反复 toggle 的问题。改为上升沿触发（`pauseDown && !pauseWasDown`），单次按下仅触发一次暂停。
- **暂停按钮视觉反馈：** 新增 `pauseFlash` 计数器（18帧），按下后有短暂高亮反馈。

**移动端触摸控制完善：**
- **摇杆背景面板：** Canvas 底部 38% 区域添加水墨渐变暗色面板，提升控制元件在复杂游戏场景上的可读性。
- **摇杆释放复位：** `touchend` 时将摇杆基点重置回默认位置（`leftBase`/`rightBase`），防止浮动摇杆位置漂移。
- **闪避按钮位置优化：** 从 `(0.73, 0.82)` 移至 `(0.62, 0.78)`，与右摇杆拉开距离减少误触。
- **右摇杆基础位置：** 从 `0.86` 移至 `0.88`，更靠近右边缘适配握持手势。
- **移动端瞄准方向保持：** 右摇杆释放后通过 `_lastMobileAim` 保持最后瞄准方向，防止弹回中心。
- **闪避排队清理：** 非闪避按键状态下自动清除 `dodgeQueued`，防止残留的闪避指令。

**渲染容错：**
- 渲染循环 catch 块新增 `_renderErrors` 计数器，仅前 3 次错误输出到 console，用于移动端问题诊断。

**本会话修复 (2026-04-29 第二阶段)：**
- **`relicPower` 指数叠加修复：** 噬魂诅咒的 1.8x 遗物效果加成每次拾取遗物时重复乘以已累积的 `soulDmg`/`killHeal`/`decoyHP`，导致数值指数膨胀。改为仅对当前遗物新贡献的增量应用倍率，不重复乘累积值。首次拾取时一次性加成 `dmg`/`critDmg`，由 `_relicPowerApplied` 标记防重复。
- **`quitToTitle` / `restartRun` 弹窗清理：** 新增隐藏 `relicPopup` 和 `cursePopup` 的逻辑，防止从暂停菜单退到标题/重新开始时弹窗残留在画面上。
- **`togglePause` 重叠弹窗防护：** 遗物/誓印弹窗显示期间禁止暂停，防止暂停遮罩层与弹窗重叠。
- **难度选择 JS 兜底：** 新增 `updateDiffLabels()` 函数，监听 radio change 事件并 toggle `.is-selected` class，为不支持 `:has()` 的浏览器提供视觉反馈。CSS 同步添加 `.difficulty-option.is-selected` 规则。
- **纸薄诅咒额外遗物通知：** `extraStartRelics` 选择的遗物现在显示浮动文字 "誓印赐物: X"，玩家可见获得的额外遗物（此前静默添加）。
- **`_relicPowerApplied` 标记：** 新增于 `mkPlayer()` 中，追踪噬魂诅咒的一次性加成是否已应用。

**代码规模：** game.js ~2709 行 (+39)，mobile-controls.js ~324 行，gamedata.js ~334 行，game.css ~1072 行 (+1)。

*v2.4 更新于 2026-04-29。*

### v2.5 内容扩展 + 元进度完善 (2026-04-29)

**目标：** 扩展敌人/遗物/成就内容，完善元进度追踪，增强视觉反馈。

**新增敌人（2种）：**
- **墨鸦 (moya)：** 飞行远程敌人，生命低但速度极快（spd 1.9），远距离弹射攻击。优先点杀型威胁。
- **石俑 (shiyong)：** 重甲盾兵，生命极高（hp 110），自带 45 点护盾，移速极慢。破盾后集火击杀。

**新增遗物（5件）：**
- **墨鸦翎：** 移速+12%，击杀后攻速大幅提升。机动型遗物。
- **石心：** 防御+18%，移速-6%。生存与机动的取舍。
- **余烬扇：** 站在火场上每秒回复 3HP。若已有磷火遗物，火场击杀后扩大范围。
- **封魔符：** 攻击减速效果提升至 35%。控场特化。
- **回魂香：** 击杀回复 3HP，击杀后短暂加速。生存+机动双收益。

**新增成就（4项）：**
- **誓印皆立：** 使用过全部 6 种誓印。
- **精英猎手：** 累计击杀 50 个精英敌人。追踪 `meta.eliteKills`。
- **遗物学者：** 发现 35 件遗物。奖励起始遗物。
- **火中取栗：** 单局用火焰击杀 10 个敌人。追踪 `meta.bestFireKills`。

**元进度追踪更新：**
- `loadMeta()` 新增 `eliteKills:0`、`bestFireKills:0` 默认值。
- `metaRecordRun()` 新增精英击杀累加和火焰击杀最佳记录。
- `newGame()` 新增 `fireKills:0` 游戏内计数器。
- `onEnemyKilled()` 新增火焰击杀检测（`source==="fire"`）。

**视觉增强：**
- **闪避定向粒子：** 闪避时向闪避反方向喷射 8 颗定向墨粒子，强化"瞬身"视觉反馈。
- **连击 30 里程碑：** 新增"修罗道"浮动文字 + 金色粒子 + 强震屏，在百鬼夜行（20连）之上再加一层。

**数据表更新：**
- `WAVE_TIERS` 第 2-5 梯队新增墨鸦/石俑出现。
- `ENEMY_COST` 新增墨鸦 (1.8) / 石俑 (3)。
- `DEATH_COLOR` 新增墨鸦 (ink) / 石俑 (soft)。

**代码规模：** game.js ~2745 行 (+36)，gamedata.js ~351 行 (+17)。

**Bug 修复 (深度审计发现)：**
- **`showCurse` setTimeout 泄漏：** 移动端 20s 自动跳过定时器在 `restartRun`/`quitToTitle` 后未被清除，定时器回调可能使用已废弃的 `g` 引用调用 `beginRun(g)` 导致状态污染。修复：回调中添加 `G===g` 守卫，确保仅在游戏对象仍为当前时才执行。
- **RAF 泄漏：** `requestAnimationFrame(loop)` 在 `quitToTitle` 后持续运行，G=null 时虽无渲染但持续消耗移动端电量。修复：新增 `_loopActive` 标志，`quitToTitle` 停止循环，`startGame` 重新启动。
- **移动端输入卡死：** `input.attacking`/`dodgeDown` 在标签页切换或系统中断丢失 `touchend` 事件时可能永久卡住。修复：`mobile-controls.js` 新增 `visibilitychange` 监听，页面隐藏时重置全部输入状态。

**视觉扩展：**
- **墨鸦 / 石俑独特外观：** 墨鸦渲染为菱形鸟身+双翼，石俑渲染为方形重甲。
- **精英能力标签：** 精英敌人上方显示能力单字标签（瞬/爆/狂/甲），便于识别威胁类型。

*v2.5 更新于 2026-04-29。*

### v2.6 内容深拓 + 联动强化 (2026-04-29)

**目标：** 扩展弹道体积联动链、火径体系、新敌人/誓印、中Boss遭遇。

**弹道体积联动链（projSize synergy）：**
- 新增 6 件联动遗物：聚灵墨斗→墨罡印→厚墨（弹体变大+dmg+减速），需前置遗物解锁
- 新增 `CAPS.projSize=2.0` 封顶保护，3 处代码（折返弹/远程弹/三连刃）强制 clamp
- PREREQS 新增 6 条：mogangyin(需聚灵墨斗/墨蛟筋)、houmo(需projSize>1.2)、yanmoyan(需夜灯)、binghe(需裂冰)、lianhuanfu(需蝶印)、molonglin(需石心)

**新增遗物（17件，40→57）：**

| 层 | 数量 | 遗物 |
|----|------|------|
| 基础数值/机制 | 11 | 聚灵墨斗(projSize+0.3+returnInk+0.15)、墨蛟筋(projSize+0.25)、炎墨砚(fireTrail)、吞魔符(fireDmg)、狐墨(dmg每遗物)、幽冥砂(probSize+slow)、沙墨(def+hp)、墨龙鳞(def每精英杀)、破军符(critRate每精英杀)、霹雳木(fearCrit)、寒星铁(critDmg+slow)、静心咒(decoyHP+def) |
| 联动（PREREQS） | 6 | 墨罡印、厚墨、焰墨砚、连环符、墨龙鳞、裂冰诀（均需前置遗物满足条件方出现） |

**新增敌人（2种，13→15）：**
- **炎俑 (yanyong)：** 近战火径敌人，HP 95、spd 1.3、dmg 13，移动留火场，cost 2.2
- **速傀 (sukui)：** 高速远程敌人，HP 38、spd 2.2、dmg 8，快速弹射，cost 1.4

**新增誓印（2种，6→8）：**
- **狂血 (kuangxue)：** 杀敌后攻速+移速短时提升，受击额外受伤+30%
- **墨瘾 (moyin)：** 弹道+1，射程-15%，移速-8%

**中Boss遭遇：**
- Wave 6 50%概率触发中Boss（非噩梦难度），使用最终Boss类型但 HP×0.55
- 入场特效减半（25帧）、无 bossIntro 音效、标签显示"中Boss"
- `midBoss:true` 标志存储于敌人对象，贯穿 spawnEnemy→入场→死亡全流程

**性能优化：**
- 4 处渲染循环添加屏幕空间裁剪（fires/eProj/attacks/particles），减少 ~30-50% draw call
- 小粒子（ps≤2.5）改为简单 arc 路径，跳过复杂 blob 绘制

**武器平衡：**
- AOE 武器（镇魂铃）dmg 13→16
- Dash 武器（伏魔伞）dmg 18→16
- 静心咒 decoyHP 30→20+def 12%

**百科更新：**
- 构筑线从 7 条扩至 9 条（新增弹道体积联动、火径压制）
- 所有新增遗物/敌人/誓印自动渲染

**测试覆盖：** smoke_test 36项 + wave_test 5项 + content_test 11项 = **52项全部通过**。

**代码规模：** game.js ~2810 行 (+65)，gamedata.js ~380 行 (+29)，content_test.js 153 行（新增），wiki.html 构筑线+2。

*v2.6 更新于 2026-04-29。*

### v2.7 毒蛛 + 三遗物 + 深度测试 (2026-04-30)

**新增敌人（1种，15→16）：**
- **毒蛛 (duzhu)：** 毒径减速敌人，HP 50、spd 1.6、r 13、dmg 8，移动留毒场（poison=true），cost 1.7
  - 毒场每 25 帧生成，半径 14，持续 70 帧
  - 毒场对玩家造成伤害并减速（slowT+=30），绿色苔藓风格渲染
  - 加入 WAVE_TIERS tier 2-3（wave 4+）
  - DEATH_COLOR: moss

**毒场系统：**
- `fire.poison` 新分支：与 `f.slow`（冰区）、`f.owner==="player"`（玩家火场）、默认（敌方火场）并列
- 毒场渲染：外圈深绿 `rgba(77,107,86,0.5)` + 内圈浅绿 `rgba(140,180,120,0.6)`
- 玩家减速：`p.slowT=Math.max(p.slowT,30)`，与冰冻区共享减速机制

**新增遗物（3件，57→60）：**

| ID | 名称 | 标签 | 效果 |
|----|------|------|------|
| mojiangling | 墨将令 | 击杀/法术 | 击杀精英时爆发墨汁，80px范围内造成 stats.dmg×8 伤害 |
| guishouyin | 鬼手印 | 闪避/魂 | 完美闪避时吸取120px内最近敌人灵魂，造成 soulDmg+5 并回复3HP |
| zhiyanwen | 纸鸢纹 | 远程/控场 | 弹道首次命中留下减速墨圈（r=22, life=50, slow=true） |

**遗物实现细节：**
- 墨将令：挂载于 `onEnemyKilled()` 精英死亡分支，对范围内非Boss敌人调用 `damageEnemy()`
- 鬼手印：挂载于 `dodge()` 函数末尾，寻找最近敌人，造成魂伤+回血+粒子特效
- 纸鸢纹：挂载于弹道命中逻辑，每颗弹道仅触发一次（`_slowFieldDone` 标记）

**测试覆盖：** 新增 3 项测试（#48 毒蛛、#49 新遗物、#50 WAVE_TIERS），总计 **14 项 content_test 全部通过**。

**代码规模：** game.js ~2860 行 (+50)，gamedata.js ~385 行 (+6)，content_test.js ~195 行 (+42)。

*v2.7 更新于 2026-04-30。*

### v2.8 蛊师 + 誓印扩展 + 环境事件 (2026-04-30)

**新增敌人（1种，16→17）：**
- **蛊师 (gushi)：** 支援型敌人，HP 65、spd 1.1、r 14、dmg 0（不直接攻击），cost 2.8
  - 每 60 帧为 90px 范围内非Boss敌人施加增益（`buffed=60`），+30%移速 +35%伤害
  - 深紫光环渲染，被增益敌人显示暗紫色边框
  - 加入 WAVE_TIERS tier 3-4（wave 6+）

**新增誓印（2种，8→10）：**
- **迷雾 (miwu)：** 5秒周期，其中1.5秒视野缩小至 120px 径向渐变，但移速+30%
  - 渲染使用 `destination-in` 合成模式创建黑洞效果
- **鬼火 (guihuo)：** 敌人死亡产生追踪魂球（spd 1.5, dmg × 0.4, life 180），但击杀回血+5
  - 魂球渲染为暗紫核心+苍白内核，带 shadowBlur 发光

**新增遗物（3件，60→63）：**

| ID | 名称 | 标签 | 效果 |
|----|------|------|------|
| shiguping | 噬蛊瓶 | 生存/火 | 站在毒场上回复2HP而非受伤，反转毒蛛威胁 |
| yexingyi | 夜行衣 | 闪避/机动 | 迷雾中增伤+30%，魂球击中回血+3而非受伤 |
| hunsuolian | 魂锁链 | 处决/击杀 | 连斩每层额外+3%伤害，十连斩以上暴击率+15% |

**遗物-誓印联动设计：**
- 噬蛊瓶 + 毒蛛：将威胁转为资源
- 夜行衣 + 迷雾/鬼火誓印：将惩罚转为增益
- 魂锁链 + 斩妖剑：强化连斩体系的核心循环

**环境事件系统（STAGE_HAZARDS）：**
- Wave 3+ 非 Boss 波次 30% 概率触发随机环境事件
- **阴风 (yinfeng)：** 每 180 帧随机方向推力影响玩家和所有敌人
- **墨雨 (moyu)：** 每 45 帧随机位置掉落墨点，接触扣 5 HP
- **鬼火焰 (guihuoyan)：** 每 120 帧生成追踪火球（spd 1.2, dmg 3, life 120）

**百科更新：**
- 构筑线从 11 条扩至 15 条（新增毒径求生、暗夜行者、连斩之道、弹幕领域）

**测试覆盖：** 新增 4 项测试（#51 蛊师、#52 新誓印、#53 新遗物、#54 环境事件），总计 **18 项 content_test 全部通过**。

**代码规模：** game.js ~2950 行 (+90)，gamedata.js ~400 行 (+15)，content_test.js ~280 行 (+85)。

*v2.8 更新于 2026-04-30。*

### v2.9 墨灵伙伴 + 伪装/寄生系统 (2026-04-30)

**新系统：墨灵伴侣**
- 墨灵玉(molingyu): 召唤墨灵环绕玩家，自动追踪攻击附近敌人
- 墨灵以小墨球形态绕玩家旋转，每38帧发射墨弹攻击220px内敌人
- 墨灵弹为独立攻击类型(spirit)，碰撞后触发hitE完整流程

**新敌人类型：**
- 画皮(huapi): 伪装成墨色光点，玩家靠近后现形突袭。速度2.1、伤害14，高威胁伏击单位
- 墨蛭(mozhi): 极速小型寄生者，接触后吸附玩家持续吸血(2HP/30帧)，需闪避甩脱

**新遗物 (4件)：**
- 墨灵契(molingqi): 墨灵伤害+50%、墨灵数量+2（前置：墨灵玉）
- 破妄瞳(powangtong): 首次攻击现形画皮必暴击+50%伤害，暴击率+5%
- 蛭反(zhifan): 被墨蛭吸附时攻速+35%、伤害+25%

**新誓印 (1件)：**
- 灵噬(lingshi): 墨灵伤害翻倍，但每波开始每只墨灵扣2HP

**交互设计：**
- 画皮伪装→破妄瞳克制（首次必暴）形成战术反制
- 墨蛭吸附→闪避甩脱→蛭反增益形成风险收益博弈
- 灵噬高伤害→扣血惩罚→需要权衡墨灵数量

**游戏逻辑修改：**
- game.js: spawnEnemy添加mimic/disguised/leech/attached标志
- update(): 画皮检测距离触发现形(55px)，墨蛭接触吸附+跟随+吸血
- startDodge(): 闪避时清除所有吸附墨蛭
- pAtk(): 蛭反检测吸附状态增伤+减CD
- hitE(): 破妄瞳检测mimicRevealT强制暴击+增伤
- startWave(): 灵噬检测扣除墨灵数×2的HP
- render(): 画皮伪装渲染为墨绿光点，墨蛭吸附时在玩家身上渲染，墨灵渲染为带光环的墨球

**数据更新：**
- RELICS: 63→67 (+4)
- ETYPE: 17→19 (+2: huapi/mozhi)
- CURSES: 10→11 (+1: lingshi)
- PREREQS: 16→17 (+1: molingqi)
- LIMITS: 新增inkSpirits:6

**测试覆盖：** 新增 8 项测试（#55-#62），总计 **26 项 content_test 全部通过**。

**代码规模：** game.js ~3030 行 (+80)，gamedata.js ~430 行 (+14)，content_test.js ~360 行 (+80)。

*v2.9 更新于 2026-04-30。*

### v2.10 墨阵系统 + 墨童自爆 (2026-04-30)

**新系统：墨阵 (Ink Formations)**
- 墨守阵(moshouzhen): 静止1秒后生成墨守圈(50px,90帧)，圈内玩家减伤25%
- 墨攻阵(mogongzhen): 每击杀3敌释放墨爆脉冲(80px)，对范围内敌人造成武器伤害×2
- 阵眼(zhenyan): 墨阵范围+40%、触发阈值减半（静止0.5秒、击杀2敌）
- 墨竭(mojie)誓印: 墨阵效果翻倍（双倍范围/持续时间），但最大生命-25%
- 墨阵为独立ground对象，render中绘制为虚线圆(守)/扩散圈(攻)

**阵地系统实现：**
- g.formations数组管理所有墨阵
- 守阵在hurtP中减伤判定（dstSq<r²）
- 攻阵在life=4帧时爆炸，遍历enemies造成伤害
- 静止检测通过movedThisFrame标志，连续不移动积累stillT

**新敌人：墨童(motong)**
- 小型快速近战(HP28,spd1.7)，死后延迟55帧自爆
- 爆炸半径60px，伤害12，对玩家造成伤害
- 炸弹倒计时在render中渲染为渐变红色警告圈

**交互设计：**
- 墨守阵鼓励"站桩输出"玩法，与闪避型build形成取舍
- 墨攻阵+骨笛残声/破军符=高频击杀触发连爆
- 墨竭高风险高收益，配合墨守阵弥补生存缺陷
- 墨童死后自爆迫使玩家注意尸体位置，增加走位深度

**数据更新：**
- RELICS: 67→70 (+3: moshouzhen/mogongzhen/zhenyan)
- ETYPE: 19→20 (+1: motong)
- CURSES: 11→12 (+1: mojie)
- PREREQS: 17→18 (+1: zhenyan)

**测试覆盖：** 新增 6 项测试（#63-#68），总计 **32 项 content_test 全部通过**。

**代码规模：** game.js ~3095 行 (+65)，gamedata.js ~440 行 (+11)，content_test.js ~430 行 (+70)。

*v2.10 更新于 2026-04-30。*

### v2.11 墨镜/回春阵/墨涡/墨蝠/墨瘴 (2026-04-30)

**新遗物（3件）：**

**墨镜（mojing）** — 镜器 | 防御/法术
- 30%概率反弹敌方弹道，反弹伤害翻倍
- 标记：`reflectChance`=0.3, `reflectDmgMult`=1
- 实现：在敌弹碰撞检测中（game.js:1378）插入反射判定，通过Math.random()<reflectChance决定是否反弹
- 反射时将弹道反转方向、加倍伤害，从g.eProj移除并pushAttack加入玩家攻击池
- 反射粒子效果：accent色粒子；

**回春阵（huichunzhen）** — 阵器 | 生存/控场
- 静止1.2秒生成回春圈，圈内每秒回复3HP
- 标记：`healFormation`=true
- 实现：与墨守阵共用stillT机制，独立阈值（formBoost减半）；生成"heal"类型阵
- 阵生命周期中每60帧（1秒）检查玩家是否在圈内，是则+3HP
- 阵渲染：moss色虚线圆，alpha=0.12填充+0.35描边
- formDouble/mojie：半径56→112，生命120→240
- 前置：需持有任一墨阵（moshouzhen或mogongzhen）

**墨涡（mowo）** — 阵器 | 击杀/控场
- 每击杀5敌在脚下释放墨涡，牵引并伤害周围敌人
- 标记：`vortexOnKill`=true, `vortexKills`计数
- 实现：onEnemyKilled递增vortexKills；update检测>=5时生成"vortex"阵并重置
- 阵每帧牵引圈内敌人向中心（拉力与距离成反比），每12帧造成35%阵伤害
- 阵渲染：三层旋转弧线+渐大填充圆
- wave开始时reset vortexKills

**墨吸（moxi）** — 阵器 | 击杀/控场
- 击杀敌人延长所有墨阵15帧
- 标记：`formationLeech`=true
- 实现：onEnemyKilled遍历g.formations将life延长15帧（不超过maxLife）
- 与所有阵型遗物协同，提供阵的持续维持

**墨甲（mojia）** — 护具 | 生存/控场
- 防御+15%，在墨阵内额外+15%防御
- 标记：`formDef`=true, stats.def+=0.15
- 实现：hurtP中检查p.formDef，若玩家在任一类型阵内则额外减伤15%
- 与所有阵型遗物协同，提供阵内生存强化

**新敌人（1种）：**

**墨蝠（mofu）** — 高速俯冲撞击
- HP 25, SPD 3.2, R 9, DMG 10, ATK/R 60, ATK/CD 50
- 机制 `swoop`：三段状态机 idle→prep→swoop
  - idle：距玩家<180px时进入prep
  - prep：减速前移35帧后锁定方向冲刺
  - swoop：2.8倍速冲刺22帧，命中1.5倍伤，出界或超时回idle
- 渲染：蝙蝠翼形多边形+翼展动画wingFlap；prep状态闪烁accent虚线
- 配色：rgba(23,19,16,0.3)/ink，死亡墨色
- WAVE_TIERS[1]出现，ENEMY_COST=1.1

**新环境事件（1种）：**

**墨瘴（mozhang）** — 毒瘴飘移
- 每200帧生成毒瘴云（R30, 生命220），随机初速度飘移
- 碰壁反弹，玩家在云内每20帧扣2HP
- 渲染：moss色双层椭圆波动云，间隔200帧

**Bug修复（4项）：**
1. formDouble错误降低了atkFormation触发阈值（修复：formBoost才减半）
2. stillT精确相等导致阵无法连续触发（修复：>=检测+重置stillT=0）
3. 墨灵契拾取后已有墨灵伤害未更新（修复：relic应用后遍历更新所有spirit.dmg）
4. deathburst碰撞公式用r1²+r2²而非(r1+r2)²（修复：mr=db.r+p.r; dstSq<mr*mr）

**数据更新：**
- RELICS: 70→75 (+5: mojing/huichunzhen/mowo/moxi/mojia)
- ETYPE: 20→21 (+1: mofu)
- STAGE_HAZARDS: 3→4 (+1: mozhang)
- PREREQS: 18→19 (+1: huichunzhen)

**测试覆盖：** 新增 10 项测试（#69-#78），总计 **42 项 content_test 全部通过**。

**代码规模：** game.js ~3250 行 (+155)，gamedata.js ~465 行 (+25)，content_test.js ~540 行 (+112)。

*v2.11 更新于 2026-04-30。*

### v2.12 墨爆/墨弦/墨蝶 (2026-04-30)

**新遗物（2件）：**

**墨爆（mobao）** — 阵器 | 击杀/法术
- 墨阵消失时引爆，对范围内敌人造成伤害
- 标记：`formationDetonate`=true
- 实现：在formation生命周期中，life<=0时若玩家有formationDetonate，引爆AOE
- 伤害 = weapon.dmg × stats.dmg × 1.2，范围=阵半径
- 引爆视觉：accent粒子12个+ink粒子8个+震动(5,2)
- 前置：需持有任一墨阵（moshouzhen/mogongzhen/huichunzhen/mowo）

**墨弦（moxian）** — 阵器 | 法术/控场
- 两个以上墨阵间生成墨弦，触弦敌人受伤
- 标记：`inkStrings`=true
- 实现：update中每12帧遍历formation对，用distPointToSeg检测敌人-线段距离
- 弦线伤害 = weapon.dmg × stats.dmg × 0.25
- 弦线渲染：虚线连接每对墨阵，alpha=min两阵生命比×0.3
- 前置：需持有至少2件阵型遗物（含mobao）
- 新增工具函数：distPointToSeg(px,py,x1,y1,x2,y2) — 点到线段最短距离

**新敌人（1种）：**

**墨蝶（modie）** — 脆弱飞行远程
- HP 18, SPD 2.8, R 8, DMG 6, ATK/R 200, ATK/CD 80
- 机制 `deathBuff`：死亡时buff半径130内所有敌人180帧（伤害+35%）
- 渲染：蝴蝶双翼多边形+振翅动画wingFlap，moss色填充
- 配色：rgba(77,97,86,0.35)/C.moss，死亡moss色
- WAVE_TIERS[1]出现，ENEMY_COST=1.6

**数据更新：**
- RELICS: 75→77 (+2: mobao/moxian)
- ETYPE: 21→22 (+1: modie)
- PREREQS: 19→21 (+2: mobao/moxian)

**测试覆盖：** 新增 7 项测试（#79-#85），总计 **48 项 content_test 全部通过**。

**代码规模：** game.js ~3310 行 (+60)，gamedata.js ~475 行 (+10)，content_test.js ~600 行 (+60)。

*v2.12 更新于 2026-04-30。*

### v2.13 墨钉/墨澄 (2026-04-30)

**新遗物（2件）：**

**墨钉（moding）** — 凶器 | 控场/近战
- 攻击钉住首个命中敌人1.5秒（30帧），禁止移动
- 标记：`attackPin`=true
- 实现：damageEnemy中若e.hp>0则设置e.pinned=30帧
- 敌人update：pinned>0时spd强制为0，逐帧递减
- 渲染：accent色十字钉标记，闪烁动画
- 前置：仅限近战武器

**墨澄（mochen）** — 阵器 | 法术/控场
- 站在墨阵内时攻速+20%
- 标记：`formClarity`=true
- 实现：pAtk计算atkCd时，若玩家在任一墨阵内，cdMult×0.8
- 与所有阵型遗物协同，强化阵内站桩输出

**数据更新：**
- RELICS: 77→85 (+8: moding/mochen/motao/molian/moyi/moze/moxiu/mowei)
- PREREQS: 21→25 (+4: moding/mochen/molian/moyi)

**墨涛（motao）** — 墨具 | 击杀/控场
- 击杀敌人释放墨涛，推开周围敌人
- 标记：`killPulse`=true
- 实现：onEnemyKilled遍历100px内敌人，按距离反比推开

**墨涟（molian）** — 阵器 | 法术/控场
- 站在墨阵内每0.5秒释放墨涟，伤害阵内所有敌人
- 标记：`formRipple`=true
- 伤害=weapon.dmg×stats.dmg×0.18，moss色粒子效果

**墨移（moyi）** — 闪具 | 闪避/机动
- 闪后留影，3秒内再闪回归原位
- 标记：`recallDash`=true，前置：闪避武器
- 实现：首次闪保存recallMark{x,y,life:180}，再闪时若mark有效则传送回mark位置
- 视觉：accent虚线圈+粒子爆发

**墨泽（moze）** — 墨具 | 击杀/机动
- 击杀敌人获得3秒移速+20%
- 标记：`speedBurstT`=180（3秒）
- 实现：onEnemyKilled设置speedBurstT=180，moveScale中speedBurstT>0时+0.2速度
- 叠加于killSpdTimer之上，moss色粒子提示

**墨嗅（moxiu）** — 墨具 | 击杀/处决
- 击杀延长连斩窗口15帧（上限150帧），更易维持高连斩
- 标记：`scentStreak`=true
- 实现：onEnemyKilled将killStreakT+15（上限150），延长连斩判定窗口

**墨威（mowei）** — 阵器 | 法术/击杀
- 每个活跃墨阵+6%伤害
- 标记：`formDmgBonus`=true
- 实现：pAtk计算伤害时，dmg×(1+g.formations.length×0.06)

**测试覆盖：** 新增 10 项测试（#86-#95），总计 **58 项 content_test 全部通过**。

**代码规模：** game.js ~3410 行 (+60)，gamedata.js ~505 行 (+25)，content_test.js ~730 行 (+150)。

*v2.13 更新于 2026-04-30。*

### v2.14 墨碎/墨蚀/墨怒 (2026-04-30)

**新遗物（3件）：**

**墨碎（mosui）** — 凶器 | 暴击/溅射
- 暴击时溅射碎片，对周围80px内敌人造成35%伤害
- 标记：`critShrapnel`=true
- 实现：hitE中暴击判定后，遍历附近敌人调用damageEnemy，"shrapnel"伤害源

**墨蝕（moshi）** — 墨具 | 持续/法术
- 攻击附加墨蚀，每秒2点伤害持续5秒（100帧），可叠3层
- 标记：`corrosive`=true，前置：墨涟
- 实现：damageEnemy中设置e.corrode（上限3层），e.corrodeT=100
- 敌人update中每60帧（1秒）造成 corrode×2 点伤害，粒子提示
- corrodeT归零时层数清零

**墨怒（monu）** — 墨具 | 生命/爆发
- 生命低于50%时，伤害+25%，移速+15%
- 标记：`lowHpFury`=true，前置：铜镜
- 实现：pAtk中若hp≤maxHp×0.5则最终伤害×1.25
- moveScale中若hp≤maxHp×0.5则速率+0.15

**新敌人（2种）：**

**墨蛹（moyong）** — 蛹体
- 慢速无攻击蛹体（HP55，速度1.2），死后孵化2只墨蚋
- 属性：spawnsOnDeath=true，spawnType="morui"，spawnCount=2
- 实现：onEnemyKilled中检测spawnsOnDeath，调用spawnEnemy在死亡位置生成morui
- 波次：Tier1/Tier3

**墨蚋（morui）** — 幼虫
- 极速近战小虫（HP12，速度3.6，攻击CD26帧）
- 仅由墨蛹孵化生成，不直接出现在波次池中

**UI/体验打磨：**
- 破盾：增加震屏（dur=5, amp=3）+ 顿帧2帧
- critFlash：持续帧数12→18（更明显的金色闪光）
- 处决闪光：窗口从freezeT>4扩大到freezeT>1，alpha随freezeT线性衰减
- 浮动文字：dmg/critDmg/weakDmg/soul/heal基础字号+2px，全部增加text-shadow
- 连斩计数器：基础字号16→20px，倍率12→14px，增加accent色辉光
- 波次进度条：高度3→5px
- 命中闪光：alpha上限限制1.0，防止过曝

**引擎增强：**
- spawnEnemy支持opts.x/opts.y位置覆盖（用于敌人孵化/分裂等中途生成场景）
- 覆盖时跳过arena边缘定位逻辑

**数据更新：**
- RELICS: 85→88 (+3: mosui/moshi/monu)
- ETYPE: 22→24 (+2: moyong/morui)
- PREREQS: 25→27 (+2: moshi/monu)

**测试覆盖：** 新增 8 项测试（#96-#103），总计 **66 项 content_test 全部通过**。

**代码规模：** game.js ~3500 行 (+80)，gamedata.js ~545 行 (+40)，content_test.js ~780 行 (+50)。

*v2.14 更新于 2026-04-30。*

### v2.14.1 稳定性热修 + UI图标补丁 (2026-04-30)

**稳定性修复：**
- 修复 `damageEnemy()` 内部直接读取未定义 `p` 的崩溃。该问题会在墨蚀/魂链/护盾/多武器长跑中触发，表现为对局中后段随机中断。
- 将 `pAtk()`、墨涟、铃圈绘制中的局部 `ri` 循环变量改名，避免测试环境去掉 IIFE 后覆盖随机整数函数 `ri()`，同时降低全局污染风险。
- `pushAttack()` 统一初始化 `hitMap/life/maxLife`，并对投射物/墨灵弹做速度有效性修复。后续所有攻击入口走同一安全网，减少“弹射物卡住无伤害”的隐性来源。
- 墨灵自动弹从直接 `pushLimited(g.attacks,...)` 改走 `pushAttack()`，保证攻击对象结构一致。

**遗物/誓印流程修复：**
- 遗物弹窗增加一次性选择锁，防止双击/连点导致重复应用、直接进入后续项或状态错乱。
- 纸薄/起始遗物浮字从 `startWave()` 前移动到 `startWave()` 后创建，避免被 `cleanupWave()` 清掉。

**UI/图标补丁：**
- 武器选择卡和遗物选择卡新增纯 CSS 水墨小图标，不引入图片资源。
- 卡片增加淡墨底纹、选中态和更清晰的标题层级，提升“东方志怪/水墨器物”的识别度。

**测试覆盖：**
- `smoke_test.js` 增加第37项：镇魂铃 + 魂链 + 墨蚀 + 减速联动的 60 秒长跑，用于回归约49秒崩溃窗口。
- 当前通过：`smoke_test` 37项、`wave_test` 5项、`content_test` 69项。

*v2.14.1 更新于 2026-04-30。*

### v2.14.2 移动端手感 + HUD可读性优化 (2026-04-30)

**目标：**
- 处理移动端右摇杆死区与 HUD 后期遮挡两个直接体验问题。
- 把纸薄开局赠物从“字段测试”提升到真实 `beginRun()` 流程回归。

**改动：**
- `mobile-controls.js` 新增 `nearestAimVector()` 和 `updateRightAim()`，右摇杆 `touchstart/touchmove` 共用同一套死区逻辑。
- 右摇杆轻触不再立刻攻击；只有拖出 `ATTACK_THRESHOLD` 后才攻击。死区内仍保留最近敌人自动瞄准方向。
- HUD 遗物条最多展示前 5 件遗物，超过部分显示 `+N件`，避免后期遮挡画面和移动端压 UI。
- `content_test.js` 的纸薄测试新增真实 `beginRun()` 验证：开局应获得 2 件遗物、消耗 `extraStartRelics`、最大生命变为 60。
- 新增 HUD 汇总测试，防止后续改 UI 时再次把遗物条铺满。

**测试覆盖：**
- `node --check game.js`
- `node --check mobile-controls.js`
- `node smoke_test.js`：37项通过
- `node wave_test.js`：5项通过
- `node content_test.js`：70项通过

**未解决：**
- 纸薄已通过真实流程测试，但如果实机仍只显示 1 件，需要优先排查缓存/打包同步。
- 移动端右摇杆阈值需要实机手感确认，当前策略是“死区内自动瞄准、拖出死区攻击”。

*v2.14.2 更新于 2026-04-30。*

### v2.14.3 数值平衡 + UI增强 (2026-04-30)

**目标：**
- 缩小四武器DPS差距，解决斩妖剑DPS约为符骨笔2倍的问题。
- 增加战斗状态可视化反馈。

**数值平衡改动：**

| 项目 | 改前 | 改后 | 变化 |
|------|------|------|------|
| 斩妖剑 dmg | 22 | 20 | -9% |
| 符骨笔 dmg | 14 | 16 | +14% |
| 符骨笔 cd | 20帧 | 18帧 | -10% |
| 伏魔伞 dmg | 16 | 18 | +13% |
| combo3Dmg | 1.35× | 1.25× | -7% |
| 石俑 shield | 45 | 35 | -22% |

剑/笔差距从 2:1 缩至 1.4:1。

**UI/反馈增强：**
- 墨蚀指示器：被腐蚀敌人显示苔绿色虚线环 + 层数数字，便于追踪DOT状态
- 连斩计数器持久化：窗口 90→120帧（1.5→2秒），从第1斩即显示，5连/10连字号放大+辉光增强
- 缓存版本 `?v=2143`

**测试覆盖：**
- `node --check` 全部通过
- smoke 37项 / wave 5项 / content 70项 = 112项全通过

**未解决：**
- 墨碎溅射值(35%)和spirit伤害(4)仍需实机观察。
- 遗物强度梯度暂未调整。

*v2.14.3 更新于 2026-04-30。*

### v2.14.4 UI反馈 + 战斗可视化 (2026-04-30)

**遗物选择优化：**
- 协同标签：遗物选择卡片新增"协·{tag}"徽章，当选中遗物与已持有遗物有共同标签时显示，帮助玩家识别搭配潜力
- `relicCardHtml()` 新增 `ownedRelics` 参数，`showRelic()` 调用处传入 `g.relics`

**战斗状态指示：**
- Buff图标：玩家下方显示彩色圆点+单字标签（速/攻/暴/盾/蓄），实时显示当前激活的临时增益
  - 金色=速度、苔绿=攻击、赤色=暴击、冰蓝=护盾、金色蓄=蓄力
- Boss八向弹幕预警：boss发射8方向弹幕前20帧显示墨色虚线射线，逐渐变长变亮，提示玩家走位

**CSS新增：**
- `.relic-synergy` 类：金色半透明背景标签，内联于遗物卡片类型徽章旁

**测试覆盖：**
- `node --check` 全部通过
- smoke 37项 / wave 5项 / content 70项 = 112项全通过

*v2.14.4 更新于 2026-04-30。*

### v2.14.5 数值平衡 + 图标扩展 (2026-04-30)

**目标：**
- 系统化数值平衡：缩小敌人/遗物/誓印之间的强度差距
- 扩展遗物图标覆盖：新增火焰/灵魂/护盾/面具/镜子/珍珠/诅咒 7组图标

**数值平衡改动：**

| 项目 | 改前 | 改后 | 原因 |
|------|------|------|------|
| 石俑 HP | 110 | 95 | EHP 145→123，后期不再过度spongy |
| 石俑 shield | 35 | 28 | 与HP同比例下调 |
| 纸人替魄 decoyHP | 30 | 45 | 30HP在第3波后即被秒，45HP可持续到第6波 |
| 镇墓兽首 thorns | 30% | 50% | 反弹3伤害太弱，提高到5-6伤害 |
| 迷雾 spd | +30% | +40% | 视野损失的代价需要更强补偿 |
| 鬼火 killHeal | +5 | +8 | 追踪魂球风险大，回血需更显著 |
| 孤行 stats | +35% | +30% | 全属性+35%+攻速过强，适度削减 |

**图标扩展（+14个遗物有专属图标）：**
- 火焰组：夜灯残烬、血烛祭片、火祟牙 — 火苗形状
- 灵魂组：引魂灯笼、鬼脊蝶印、招魂残幡、骨笛残声 — 魂光形状
- 护盾组：纸甲残片、镇墓兽首 — 盾牌+十字
- 面具：傩面碎片 — 椭圆+双目
- 镜子：铜镜照妖 — 菱形+反光线
- 珍珠：墨龙珠 — 圆珠+光晕
- 诅咒组：祟面香灰、血墨混染 — 圆印+斜线

**测试覆盖：**
- `node --check` 全部通过
- smoke 37项 / wave 5项 / content 70项 = 112项全通过

*v2.14.5 更新于 2026-04-30。*

### v2.14.6 性能压测脚本 (2026-04-30)

**目标：**
- 按开发规范 Priority #4 补性能压测脚本，覆盖高压场景。

**新增 `stress_test.js`（10项）：**

| # | 场景 | 覆盖 |
|---|------|------|
| 1 | Bell AOE 满连击 | max combo + 40敌人 + 200帧 |
| 2 | 火场饱和 | fireOnKill + 8焚灵 + 8炎俑 |
| 3 | 墨灵满编 | 6只墨灵 + 40敌人 |
| 4 | 分裂链爆炸 | 10只分身鬼同时击杀 |
| 5 | 召唤物洪流 | 4纸鸢匠 + 400帧 |
| 6 | 远程弹幕饱和 | 20游魂 + 200帧 |
| 7 | perfMul 节流验证 | 填满粒子池 → 验证降频 |
| 8 | 综合高压 | 镇魂铃+fireOnKill+魂链+墨灵+召唤+分裂 |
| 9 | 60秒极限长跑 | boss + 35敌人 + 3600帧 + 增援 |
| 10 | 粒子预算 | 符骨笔+折返+30敌人 + 500帧 |

**验证内容：**
- 所有对象池不超过 LIMITS 上限
- perfMul 在高压时正确降频（mul < 0.5）
- 3600帧长跑在 5 秒内完成

**测试覆盖：**
- smoke 37 + wave 5 + content 70 + stress 10 = 122 项全通过

*v2.14.6 更新于 2026-04-30。*

### v2.15 彩蛋成就 (2026-05-05)

**新增 8 个彩蛋成就（ACHIEVEMENTS 扩展 18→26）：**

| ID | 名称 | 条件 | 检测方式 |
|----|------|------|----------|
| no_move_win | 不动如山 | 全程不按WASD通关 | `usedMoveKey` 追踪 |
| low_hurt_win | 一刀不漏 | 受伤≤3次通关 | `hurtCount` 计数 |
| speed_clear | 暴走夜行 | 单波30秒内清场 | `waveFirstKillT` → 清除时间差 |
| paper_win | 纸糊的人 | 纸薄誓印通关 | `maxHpOverride<=60` |
| no_relic_win | 赤手空拳 | 不拾取任何遗物通关 | `relics.length===0` |
| massacre | 百鬼夜行 | 单局击杀100+ | `kills>=100` |
| perfect_boss | 完美谢幕 | Boss战不受伤 | `bossHurtThisWave` 追踪 |
| no_evolve_win | 孤勇者 | 孤行誓印通关 | `player.noEvolution` |

**检测点埋设（game.js）：**
- `newGame()`: 初始化 `hurtCount=0, usedMoveKey=false, bossHurtThisWave=false`
- `hurtP()`: `hurtCount++`, boss波时 `bossHurtThisWave=true`
- `keydown`: WASD/方向键按下时 `usedMoveKey=true`
- `onEnemyKilled()`: 记录 `waveFirstKillT`
- `startWave()`: 检测boss波，重置 `bossHurtThisWave`
- 波次清除: 检测快速清场 (<1800帧/30秒)
- `metaRecordRun()`: 写入所有彩蛋字段

**测试覆盖：**
- content_test 新增 7 项 (108-114)，覆盖成就注册、计数器、追踪字段、meta写入
- smoke 37 + wave 5 + content 77 + stress 10 = 129 项全通过

*v2.15 更新于 2026-05-05。*

---

## 十七、当前Bug追踪（改完删条目）

### 🔴 严重

- 暂无自动化可复现的严重问题。

### 🟡 平衡

- **墨碎(critShrapnel)**: 已改为仅暴击触发，但35%溅射值可能仍需观察
- **spirit伤害**: 基础5→4（20%降低），需进一步测试实际对局表现

### 🟡 功能

- **移动端右摇杆瞄准**: 已改为死区内自动瞄准、拖出死区攻击；仍需实机确认阈值是否舒适

### 🟢 已修复 (v2.14.5, 2026-04-30)

- ✅ critShrapnel每次攻击都触发→改为仅暴击触发
- ✅ 弹射物速度安全网
- ✅ 49秒崩溃窗口：修复 `damageEnemy()` 未定义 `p`
- ✅ 弹射物入口一致性：`pushAttack()` 统一补 `hitMap/life/maxLife/速度`
- ✅ 遗物选择连点/重复应用：一次性选择锁
- ✅ 誓印赐物浮字：移到 `startWave()` 之后创建
- ✅ 纸薄开局赠物：`beginRun()` 回归测试确认2件遗物
- ✅ 移动端右摇杆：死区内自动瞄准、拖出死区攻击
- ✅ HUD遗物条：超过5件改为 `+N件` 汇总
- ✅ 武器平衡：剑-9%/笔+27%/伞+13%，剑笔差距 2:1→1.4:1
- ✅ 石俑EHP：110+35盾→95+28盾，有效生命 145→123
- ✅ combo3Dmg：1.35→1.25，剑三连击 24.6→21.67 均伤
- ✅ 遗物协同标签：选择时显示"协·{tag}"提示搭配潜力
- ✅ Buff状态图标：玩家下方彩色圆点实时显示临时增益
- ✅ Boss八向弹幕预警：发射前20帧显示墨色虚线射线
- ✅ 数值平衡梯度：镇墓兽首50%反伤、纸人替魄45HP、迷雾+40%速、鬼火+8击杀回血、孤行+30%属性
- ✅ 遗物图标扩展：+14个专属图标（火焰/灵魂/护盾/面具/镜子/珍珠/诅咒）

### 🟢 v2.16 (2026-05-05) 成就展示 + Bug修复 + 性能优化

**新功能：**
- ✅ 游戏结束画面显示新解锁成就名称（带边框标签样式）
- ✅ 88件遗物全部拥有专属CSS图标（从30个扩展到88个）

**Bug修复：**
- ✅ 死因显示优化：爆裂/墨童爆裂/鬼火焰/墨雨/墨瘴（之前显示未知）
- ✅ 墨蛭位置计算：indexOf从调用两次优化为一次
- ✅ weakTargets迭代：for...in改为Object.keys().forEach（避免继承属性风险）
- ✅ 死亡敌人护盾再生：已死亡敌人(e.hp<=0)不再触发护盾恢复
- ✅ metaRecordRun运算符优先级：grade比较条件加括号防止短路错误
- ✅ eProj碰撞后splice缺少continue（虽然碰巧安全但模式脆弱）
- ✅ 诱饵距离覆写bug：敌人攻击距离检查改用真实目标而非始终玩家（decoys实际生效）
- ✅ flaky test 113修复：给玩家invTimer=200防止随机碰撞导致bossHurt误触发

**性能优化：**
- ✅ perfMul缓存：每帧计算一次，spawnP/spawnInk复用缓存值
- ✅ 敌人分离碰撞：敌人≤3只时跳过O(n²)分离计算
- ✅ 条件阴影渲染：setShadow/clearShadow辅助函数，5个热路径在高负载时自动关闭shadowBlur
- ✅ 预计算常量：Math.sqrt(W*W+H*H)*0.6 → _DIAG_R06（每帧省一次sqrt）
- ✅ pullToStageWell：dstSq先做阈值判断，通过后才算sqrt（每敌人每帧省一次sqrt）
- ✅ ptInArc：dstSq替代sqrt做range判断（每次攻击命中检测省一次sqrt）
- ✅ eProj离屏警告：dstSq替代sqrt做距离判断
- ✅ distPointToSegSq：墨弦三重嵌套循环用平方距离替代两次sqrt
- ✅ atan2+cos/sin → 向量归一化：11处热路径（魂球/鬼火/墨阵涡旋/追踪弹/风筝/墨灵/击杀脉冲/敌人冲锋/俯冲/Boss冲锋/墨将军冲锋）
- ✅ 追踪弹转向：2×atan2+cos/sin → 叉积+旋转矩阵（省2次atan2）
- ✅ 敌人分离推力方向：ang+cos/sin → 向量归一化
- ✅ 诱饵循环提前退出：无诱饵时跳过O(N×D)迭代

**移动端诊断：**
- ✅ mobile-controls.js增强日志：初始化步骤、canvas检测、触控绑定、摇杆坐标
- ✅ game.js游戏开始时记录摇杆初始化状态
- ✅ 环境检测结果(native/touch/maxTouchPoints)写入诊断

**测试：** 132项全通过（37 smoke + 5 wave + 80 content + 10 stress）

**代码质量：**
- ✅ 移除死代码distPointToSeg（仅保留distPointToSegSq）
- ✅ _DIAG_R06改用W/H变量替代硬编码960/640

---

### ✅ v2.17 (2026-05-05) 第三进化 + 鬼市 + 墨灵扩展 + 新环境事件

**目标：** 补完构筑深度（第三进化）、添加首个正面互动关卡（鬼市）、扩展墨灵流派、丰富环境事件。

**内容扩展（gamedata.js）：**
- [x] 第三进化层：4种新进化 — 回斩(killDmgBoost)/梭破(projTravelDmg)/回鸣(ringHeal)/影迹(dashTrail)
- [x] 鬼市(guishi)关卡调制器：NPC商贩出现，可交换遗物或HP换遗物
- [x] 墨灵遗物扩展：+4件新遗物 — 爆墨灵(spiritExplode)/愈墨灵(spiritHeal)/寒墨灵(spiritSlow)/分墨灵(inkSpiritCount+2)
- [x] 新环境事件：+2种 — 阴兵借道(yinbing)/纸剑雨(zhijian)
- [x] PREREQS 补全3条 + WAVE_PLACES 添加入口

**game.js 机制实现：**
- [x] 第三进化：wave 8 触发"终进化"选择，HUD/结算显示 evolution3
- [x] 回斩(killDmgBoost): onEnemyKilled 标记 → pAtk 1.6x增伤并清除
- [x] 梭破(projTravelDmg): pushAttack 记录 spawnX/Y → hitE 计算飞行距离，最多+50%
- [x] 回鸣(ringHeal): 铃铛命中每敌+2HP（上限maxHp）
- [x] 影迹(dashTrail): 冲刺路径每6帧生成冰冻区（frost zones）
- [x] 爆墨灵(spiritExplode): 墨灵弹命中→60px AOE 40%伤害
- [x] 寒墨灵(spiritSlow): 墨灵弹命中→目标 slowT=40
- [x] 愈墨灵(spiritHeal): 墨灵弹击杀→恢复5HP
- [x] 分墨灵: 墨灵数量+2（inkSpiritCount+=2）
- [x] 鬼市(showMerchant): HTML弹窗提供"交换遗物"和"HP换遗物"两个选项
- [x] 鬼市商贩渲染：幽灵商贩形象+脉冲光晕+浮动金币粒子
- [x] 阴兵借道：6-8只鬼骑兵横扫屏幕，碰撞造成伤害
- [x] 纸剑雨：纸剑从顶部坠落，旋转渲染，碰撞造成伤害
- [x] 阴兵/纸剑渲染：ash色鬼魂光晕+纸剑旋转矩形

**代码质量：**
- [x] findNearestEnemy(g,ox,oy,rSq) 公共函数，替换4处内联循环
- [x] 移除死代码 distPointToSeg

**移动端修复：**
- [x] ensureMobileButtons() JS函数 + one-shot touchstart监听
- [x] AudioContext 恢复：visibilitychange 事件监听
- [x] 窗口级 touchstart/touchend/touchcancel 事件转发

**测试：** 147项全通过（37 smoke + 5 wave + 95 content + 10 stress）
- content_test 54a: STAGE_HAZARDS 数量 4→6
- content_test 118-125: v2.17 新内容测试（第三进化/鬼市/墨灵遗物/新环境事件/PREREQS/evolution3初始化/findNearestEnemy/rebuildPlayerStats）

**Bug修复（2026-05-06）：**
- ✅ 鬼市交换遗物不撤销旧遗物效果：新增 `rebuildPlayerStats(g)` 函数，交换后从 mkPlayer() 基础重建属性，保留运行时状态，重新应用所有遗物和进化
- ✅ 鬼市DOM缺失状态泄漏：`showMerchant` 提前return时恢复 `g.state="playing"`，避免卡死
- ✅ 分墨灵(fenmo)初始化脆弱：`inkSpiritCount+=2` 改为 `(p.inkSpiritCount||0)+2`

**数值平衡（2026-05-06）：**
- ✅ 回斩(killDmgBoost): 1.6x → 1.45x（击杀后增伤过于接近常驻）
- ✅ 回鸣(ringHeal): +2HP → +1HP/命中（AOE武器命中频率高，恢复过强）
- ✅ 愈墨灵(spiritHeal): +5HP → +3HP/击杀（被动击杀不应等同主动击杀回复）
- ✅ 影迹(dashTrail): 冰霜频率6帧→4帧，半径35→40，寿命50→70帧，新增2点伤害
- ✅ 纸剑雨(zhijian): 间隔55→80帧（伤害6过高且频率过密）

**信息架构 + 移动端UX（2026-05-06）：**
- ✅ 波次进度：HUD wave pill 显示剩余敌人数"余N"
- ✅ 小地图增强：提高可见度(alpha +0.1)、加粗边框、显示危险物(红点)、鬼市商贩(金点)
- ✅ Boss名称DOM同步：HUD显示Boss名+狂/绝望状态
- ✅ 状态图标系统：23种buff定义(collectBuffs)，HUD最多显示8个图标(permanent/temp/negative)
- ✅ 遗物快速查看：右键/长按遗物标签显示效果tooltip
- ✅ 遗物标签pointer-events: auto，支持交互
- ✅ 移动端伤害数字放大：13px→15px(伤害)，16px→19px(暴击)
- ✅ 摇杆底部安全区补偿：读取env(safe-area-inset-bottom)并上移基点
- ✅ 摇杆灵敏度设置：localStorage持久化，暂停菜单滑块(0.5-1.5)
- ✅ 浮字渲染移动端检测(_isMob)适配

**视觉增强（2026-05-06）：**
- ✅ 精英/Boss出场脉冲：金/紫色扩展环，16帧渐隐
- ✅ 场景氛围墨点：战斗间歇飘浮淡墨微粒
- ✅ content_test 136: spawnPulse 属性测试

**Bug修复 — 代码审计（2026-05-06）：**
- ✅ 商人无遗物交换：g.relics.length===0 时显示"无遗物可换"并返回，不消耗商贩
- ✅ 商人HP不足：提前返回并重置choiceLocked，不消耗商贩
- ✅ 商人遗物池重复：过滤已拥有遗物ID后再取新遗物
- ✅ 商人HP路径一致性：swap和HP两条路径均使用rebuildPlayerStats重建属性
- ✅ showRelic DOM缺失：提前return时设置g.state="playing"并启动波次，防止卡流程
- ✅ rebuildPlayerStats字段缺失：rk数组补充kiteKills和_killBoost，防止交换遗物时丢失进度
- ✅ aliveCount帧内分配：filter().length 改为计数循环，减少每帧GC压力
- ✅ tooltip长按竞态：touchstart中缓存touches[0]，防止400ms setTimeout中TouchList过期
- ✅ mobile-controls.js灵敏度类型校验：加入typeof number + isFinite守卫，防止NaN移动
- ✅ 愈墨灵(spiritHeal)效果文案：+5HP → +3HP，与代码实际数值一致

**音效扩展（2026-05-06）：**
- ✅ 鬼市交易音效(merchantTrade)：金属钱币叮当声（三角波+正弦波）
- ✅ 鬼市环境音(guishi)：低频嗡鸣（82/123/165Hz三频叠加+LFO颤音）

**测试：** 147项全通过（37 smoke + 5 wave + 95 content + 10 stress）
- content_test 126: collectBuffs 函数测试
- content_test 127: BUFF_DEFS 完整性校验
- content_test 128: 商贩音效存在性测试
- content_test 129-133: 审计修复回归（商人0遗物/HP不足/遗物重复/showRelic守卫/重建字段）

**HTML标准合规（2026-05-06）：**
- ✅ 补齐 meta 标签：description / theme-color / og: / twitter:card
- ✅ 补齐 iOS Web App 标签：apple-mobile-web-app-capable / status-bar-style / title
- ✅ 添加 manifest.json 和 SVG favicon
- ✅ 添加 `<noscript>` 回退提示

**性能优化（2026-05-06）：**
- ✅ 敌人离屏裁剪：渲染前跳过画布外的敌人（减少30-60%绘制调用）
- ✅ 渐变缓存：screenFlash 径向渐变复用，消除每帧 4-9 次 GC 分配
- ✅ 粒子优化：accent类型粒子去除 save/translate/rotate/restore，改世界坐标旋转
- ✅ 连击阴影：combo shadow 在性能压力模式下禁用（perfMul throttle）

**内容扩展（2026-05-06）：**
- ✅ 灰烬之路成就：单局磷火覆盖 ≥ 50% 时解锁，奖励金墨色
- ✅ 全法器皆通成就：四种武器各通关一次解锁
- ✅ 火覆盖追踪：metaRecordRun 中计算玩家火场面积占比

**测试：** 155项全通过（37 smoke + 5 wave + 103 content + 10 stress）
- content_test 129-133: 审计修复回归测试
- content_test 134-135: 新成就测试

*v2.17 更新于 2026-05-06。（共 155 项测试）*

### v2.18 QoL + 内容扩展 + 音效补齐 (2026-05-06)

**QoL：**
- R键快速重开（游戏中/暂停/结束画面均可）
- quickRestart() 函数：保持武器+难度，立即重开

**新内容：**
- 墨契诅咒（moqi）：攻击范围-30%，暴击回血+3HP/次
- 镜殿关卡（mirror）：敌人死亡射出残影弹向玩家
- STAGE_POOLS 扩展：镜殿和鬼市加入后期波次池
- 精英敌人金色三角头顶标记（视觉识别）

**测试：** 155项全通过（37 smoke + 5 wave + 103 content + 10 stress）
- content_test 137: 墨契诅咒数据和应用
- content_test 138: 镜殿关卡残影弹机制

*v2.18 更新于 2026-05-06。（共 155 项测试）*

### v2.19 音效扩展 + 视觉增强 + Boss打磨 (2026-05-06)

**音效修复：**
- 修复 `snd("hurt")` 调用不存在的音效键 → `snd("playerHurt")`

**新增音效（5种，30→35）：**
- `bossDeath`: Boss死亡低沉和弦（锯齿波+三角波，110/146.83/220Hz）
- `achievementUnlock`: 成就解锁上行号角（5音，523-1319Hz）
- `playerDeath`: 玩家死亡下行哀鸣（锯齿波220→55Hz）
- `comboMilestone`: 连斩里程碑短促高音（三角波880→1100Hz）
- `reflect`: 弹道反射金属碰撞音（方波1800→2400Hz）

**音效接入点：**
- Boss死亡：`snd("bossDeath")` 紧跟 `snd("enemyDeath")`
- 成就解锁：`showEnd()` 检测新成就时播放
- 玩家死亡：`hurtP()` HP=0进入dying状态时立即播放（不再等showEnd）
- 连斩3/5/10/20/30：每个里程碑播放 `comboMilestone`
- 弹道反射：墨镜遗物反弹时播放 `reflect`

**视觉增强：**
- 镜殿(mirror)关卡新增视觉渲染：四角镜框装饰、水平闪光纹、玩家虚影反射
- 墨将军Phase 2("召书")：新增freezeT(4帧)、bossFlash(6帧)、16+10墨粒子、enraged光环
- 墨将军Phase 3("狂书")：新增bossFlash(10帧)、24+16墨粒子、enraged光环
- 墨将军狂暴光环现在正确显示（之前enraged标记未设置）

**测试：** 155项全通过（37 smoke + 5 wave + 103 content + 10 stress）

*v2.19 更新于 2026-05-06。（共 155 项测试，35 种音效，8 种环境音）*

### v2.20 性能优化 + 桌面增强 + 代码规范 (2026-05-06)

**性能优化（热路径）：**
- 渲染循环中 `filter().length` 替换为计数循环（消除每帧GC分配）
- `findNearestEnemy()` 内联对象分配 `{x:ox,y:oy}` → 直接计算dx/dy（消除内循环GC）
- `canvas.getBoundingClientRect()` 改为缓存版本，仅在 resize/fullscreen 时刷新（消除每帧布局回流）
- 添加 `getCanvasRect()` / `invalidateCanvasRect()` 缓存函数

**桌面增强：**
- P键替代暂停（Escape之外的备选）
- F键全屏切换（`requestFullscreen` / `exitFullscreen`）
- 方向键添加 `preventDefault()` 防止页面滚动
- `setupWeaponSelect()` 添加空值保护

**代码规范改进：**
- 移除死代码：`_DIAG_R06`、`dst()` 函数
- 新增 `forEachLiveEnemy(g,fn)` 公共辅助函数（消除8+处重复的 `hp<=0||spawnGraceT>0` 模式）
- 连斩里程碑从5段 if-else → 数据驱动查找表
- 13个魔法数字移入 `gamedata.js TUNING`：
  - `killStreak5Dmg`/`killStreak10Dmg`: 连斩伤害加成
  - `eliteBaseChance`/`eliteWaveScale`/`eliteMaxChance`/`eliteHardBonus`/`eliteNightmareBonus`: 精英生成率
  - `defFormReduction`/`formDefReduction`: 防御减伤
  - `perfThresh*`/`perfMul*`: 性能自适应阈值

**测试：** 155项全通过（37 smoke + 5 wave + 103 content + 10 stress）

*没有改变游戏平衡性。*

*v2.20 更新于 2026-05-06。（共 155 项测试，38 种音效，8 种环境音）*

**game.js 审计修复：**
- 修复重复条件 `o===e||o.hp<=0||o===e`（移除冗余第三个条件）
- 修复水蛭 indexOf(-1) 边界：Math.max(0, ...) 防止坐标系异常
- 修复 calcGrade 中 maxHp 可能为0的除零保护
- 诅咒选择期间状态设为"prep"，防止游戏循环在背后运行（时间累积、玩家可移动/攻击）
- 诅咒选择自动解除从仅移动端改为桌面+移动端通用（20秒超时）
- beginRun 恢复状态为"playing"

**wiki.html 补全：**
- 新增环境事件(STAGE_HAZARDS)章节，6种事件自动渲染
- eOrder 从硬编码24项改为 Object.keys(ETYPE) 动态生成，Boss排最后
- 构筑线计数从硬编码"三十六条"改为动态读取 BUILDS.length
- 修复"墨蚀叠毒"武器名 '透骨针'→'任意'

**gamedata.js：** 零问题（8维度全量审计通过）

**测试：** 155项全通过（37 smoke + 5 wave + 103 content + 10 stress）

*没有改变游戏平衡性。*

*v2.18.2 更新于 2026-05-06。（共 155 项测试）*

### v2.21 代码质量 + 循环优化 (2026-05-06)

**目标：** 消除重复的敌人生存守卫模式，提取魔法数字为可调常量，减少热路径GC压力。

**forEachLiveEnemy 重构（17处）：**
- 击杀脉冲(killPulse)、死亡增益(deathBuff)、暴击碎片(critShrapnel)
- 闪避夺魂(dodgeSoulGrab) — 同时修复了缺失的spawnGraceT检查
- 墨阵攻击/涡旋/引爆(3处)、墨弦(inkStrings)、墨涟(formRipple)
- 攻击命中检测：弹射/斩击/环/墨灵弹(4处)
- 火场tick伤害(fire tick)
- 墨灵爆发(spiritExplode)
- 离屏敌人指示器(render)

**filter().length → some()：**
- 生存模式Boss上限检测：`filter().length > 0` → `some()`，消除每帧数组分配

**RANGES 扩展（+8项）：**
- killPulse/critShrapnel/dodgeSoulGrab/inkSpirit/spiritExplode/swoop/mimicReveal/buffAura

**TUNING 扩展（+10项）：**
- killStreakWindow/speedBurstDuration/spawnGraceDuration/deathAnimDuration
- buffedSpdMult/defaultSlowAmount/fireTickInterval
- survivalMaxEnemies/survivalSpawnBase/survivalSpawnJitter

**game.js 常量引用更新：**
- 20+处硬编码数值替换为 RANGES/TUNING 引用
- killPulse范围、critShrapnel范围、dodgeSoulGrab范围
- inkSpirit攻击范围、spiritExplode范围
- buffAura范围、swoop触发范围、mimicReveal范围
- killStreakT窗口、speedBurstT持续时间
- spawnGraceT默认值和渲染除数
- 敌人buffed速度乘数、默认减速量
- 火场tick间隔（玩家火+毒火）
- 生存模式敌人上限和生成计时器

**测试：** 156项全通过（37 smoke + 5 wave + 104 content + 10 stress）
- content_test 142: RANGES/TUNING常量完整性 + forEachLiveEnemy覆盖率 + filter().length消除验证

*没有改变游戏平衡性。*

*v2.21 更新于 2026-05-06。（共 156 项测试）*

### v2.22 性能修复 + Bug修复 + 代码清理 (2026-05-07)

**目标：** 消除热路径中的对象分配、修复潜在渲染Bug、提取平衡关键常量。

**性能优化（消除GC压力）：**
- 连斩里程碑数组提升为模块级常量 `KILL_MILESTONES`（之前每次击杀重新创建5个对象+数组）
- 诱饵(dstSq)目标计算：消除 `{x:targetX,y:targetY}` 临时对象分配，改为IIFE内联计算
- `dstSq(p,{x:db.x,y:db.y})` → `dstSq(p,db)`：deathburst碰撞检测消除冗余对象创建
- `p.leeches.filter()` → 反向splice循环：消除每帧数组分配（水蛭少时无影响，多时减少GC）
- `var mr=ep.r+p.r;` 在eProj碰撞中已使用collideSq：移除冗余的手动距离计算

**Bug修复：**
- 精英deathburst缺少 `maxTimer` 字段 → 渲染时 `progress` 为NaN（之前静默失败不显示爆炸环）
- `_isMob` 变量作用域：从 `dmg` 分支内提升到 `floatTexts.forEach` 循环顶部，确保 `critDmg` 分支可正确读取
- 死代码 `var ks=g.killStreak;` 重复声明（被下一行立即覆盖）

**TUNING常量新增（+5项）：**
- `killSpeedBonus`:0.25, `speedBurstBonus`:0.2, `lowHpFuryBonus`:0.15, `lowHpFuryThreshold`:0.5（moveScale速度加成）
- `eliteHpMult`:1.5, `eliteSpdMult`:1.3, `eliteArmoredSpdMult`:0.7（精英属性倍率）
- `reviveInvFrames`:60（复活无敌帧数）

**测试：** 157项全通过（37 smoke + 5 wave + 105 content + 10 stress）
- content_test 143: KILL_MILESTONES常量 + 新TUNING项 + 内联里程碑消除 + deathburst修复验证 + leeches.filter消除 + dstSq冗余对象消除

*没有改变游戏平衡性。*

*v2.22 更新于 2026-05-07。（共 160 项测试）*

### v2.23 常量提取 + Wiki同步 + 数据表审计 (2026-05-07)

**目标：** 完成剩余平衡关键常量提取，修正Wiki过期文案，同步数据表计数。

**TUNING新增（+8项）：**
- `lowHpFuryDmgMult`:1.25, `lowHpRangeThreshold`:0.35, `lowHpRangeMult`:1.4（低血增伤/增范围）
- `killBoostDmgMult`:1.45（回斩进化增伤倍率）
- `bossChargeRange`:200, `bossNormalAtkInterval`:140（Boss冲锋距离/普攻间隔）
- `bossPhase2Hp`:0.6, `bossPhase3Hp`:0.25（墨将军阶段HP阈值）
- `eProjWarnMin`:120, `eProjWarnMax`:280（敌弹离屏预警范围）

**game.js常量引用更新（10处）：**
- pAtk: lowHpFury/lowHpRange/killBoost 倍率
- Boss AI: 普通Boss冲锋距离/攻击间隔、墨将军阶段阈值
- 渲染: 敌弹离屏预警范围硬编码 → TUNING常量

**Wiki同步：**
- 进化文案："四条进化路线" → "五条进化路线"
- 导航：添加"事件"(sec-hazards)链接
- RANGES/波次缩放章节：补锚点ID(sec-ranges/sec-scaling)

**DEVDOC数据表计数修正：**
- RELICS: 88→92, EVOLUTIONS: 16→20, ACHIEVEMENTS: 28→27
- PREREQS: 27→29, LIMITS: 9→10, RANGES: 9→17, TUNING: 13→70

**测试：** 157项全通过（37 smoke + 5 wave + 105 content + 10 stress）

*没有改变游戏平衡性。*

*v2.23 更新于 2026-05-07。（共 160 项测试）*

### v2.24 FPS计数器 + 暂停信息增强 (2026-05-07)

**目标：** 为性能调试和游戏信息提供实时可见性。

**FPS计数器：**
- F3键切换Canvas内FPS显示（利用已有`G.perf.fps`的EMA计算）
- 显示：FPS + 敌人数(E) + 攻击数(A) + 粒子数(P) + 火场数(F)
- 暂停菜单：stats行追加实时FPS数值

**改动：**
- game.js render末尾：F3切换 `_showFps` 标志，Canvas左上角11px monospace文字
- game.js togglePause：pauseStats文本追加 `· FPS N`
- game.js keydown：添加F3键监听

**测试：** 157项全通过（37 smoke + 5 wave + 105 content + 10 stress）

*v2.24 更新于 2026-05-07。（共 160 项测试）*

### v2.25 Boss登场卡片 + FPS计数器 (2026-05-07)

**目标：** 增强Boss遭遇的戏剧性，添加性能调试工具。

**Boss登场卡片：**
- Boss波开始时显示全屏深色遮罩(0.5 alpha)
- 金色(accent)双层边框 420×110 / 400×100
- 48px金色Boss名称（墨将军/画皮娘子）
- 16px灰色Boss副标题（角色描述）
- 90帧(1.5秒)动画：30帧淡入 + 60帧淡出

**FPS计数器：**
- F3键切换Canvas内FPS显示（利用已有EMA计算）
- 显示FPS + 敌人数(E) + 攻击数(A) + 粒子数(P) + 火场数(F)
- 暂停菜单stats追加FPS数值

**README操作表扩展：**
- 新增 F全屏、F3调试、R快速重开 三个桌面快捷键

**测试：** 157项全通过（37 smoke + 5 wave + 105 content + 10 stress）

*v2.25 更新于 2026-05-07。（共 160 项测试）*

### v2.26 UX打磨 — 时序 + 反馈 + HUD (2026-05-07)

**目标：** 让关键游戏时刻更有"存在感"，改善玩家信息可见性。

**时序延长（让玩家有时间感受）：**
- 死亡冻结：22→50帧（0.37s→0.83s），玩家能看到"魂归黄泉"书法
- 胜利冻结：35→100帧（0.58s→1.67s），玩家能读完"走阴完毕"和武器名
- 波次清除：50→80帧（0.83s→1.33s），给 relic 选择前的喘息空间

**音效改进：**
- 胜利音效从showEnd移至胜利瞬间（与粒子/震动同步）
- Boss波清除播放bossDeath而非普通waveClear（区分重要性）
- Deathburst爆炸添加hazardWarn音效（之前无声）

**HUD增强：**
- HP显示：`100` → `100/100`（显示当前/最大值，方便纸薄诅咒等玩法）
- 低血量警告alpha增强：0.06-0.18 → 0.12-0.40（更容易注意到危险）

**wave_test.js更新：**
- killAll等待帧数从90→120，适配waveClearT延长

**测试：** 157项全通过（37 smoke + 5 wave + 105 content + 10 stress）

*v2.26 更新于 2026-05-07。（共 160 项测试）*

### v2.27 PREREQS修复 + 鬼市退出 + 移动端摇杆可见度 (2026-05-07)

**目标：** 修复玩家实测反馈的3项问题。

**P0 — 遗物前置过滤修复：**
- fengmofu(封魔符)：新增PREREQS `slowOnHit>0||ringSlow`。"提升至35%"暗示需要已有减速，之前无前置可出现在无减速构筑中
- PREREQS总数：29→30

**P1 — 鬼市退出选项：**
- 新增"继续赶路"卡片：不交易关闭弹窗，90帧冷却防止立即重新触发
- 冷却期过后可再次触发鬼市（未消耗商人）
- gamedata添加 `_merchantCooldown` 状态字段

**P3 — 移动端摇杆可见度大幅提升：**
- 底部面板：76%处深色面板(0.9alpha 55%)+顶部渐变过渡+装饰线
- 摇杆base alpha：0.3→0.5(闲置)/0.5→0.7(激活)
- 摇杆填充alpha：0.12→0.25，描边：0.45→0.7
- 拇指alpha：0.45→0.6(闲置)/0.85→0.9(激活)
- 摇杆尺寸放大：STICK_R 62→68, THUMB_R 28→32, HIT_R +40→+60
- 初始化增强：检测Android/iOS UA字符串兜底，2秒超时强制初始化

**设计实装验证：**
- 56个遗物boolean标记全部在 game.js 中存在引用
- 12个抽样遗物/进化全部正确实装

**测试：** 157项全通过（37 smoke + 5 wave + 105 content + 10 stress）

*v2.27 更新于 2026-05-07。（共 160 项测试）*

### v2.28 热修 — 鬼市波次覆盖Bug (2026-05-07)

**Bug：** showMerchant 三个退出路径均调用了 `startWave(g)`，导致鬼市交易或离开后当前波次被跳过，敌人被刷新重置。

**修复：**
- 离开路径(continue): `startWave(g)` → 仅 `g.state="playing"` 恢复当前波次
- 交易路径(swap/hp): `startWave(g)` → 仅 `g.state="playing"` 恢复当前波次
- DOM守卫路径: `startWave(g)` → 仅 `g.state="playing"`
- showRelic 中的 `startWave` 调用不受影响（波间选择，正确行为）

**测试：** 157项全通过 + 全属性实装验证（无遗漏引用）

*v2.28 更新于 2026-05-07。（共 160 项测试）*

### v2.29 首个稳定版 — 代码审计 + 崩溃修复 (2026-05-07)

**目标：** 全面审计消除已知崩溃向量，准备好 GitHub Pages 部署。

**崩溃修复（1项）：**
- quickRestart 中 `stopAmbient()` 未守卫 → `if(window.GameSound)GameSound.stopAmbient()`
  3处调用已有守卫，仅此1处遗漏。sound.js 未加载时抛 ReferenceError 导致重开功能完全失效。

**Bug修复（6项）：**
- `recallMark` + `justDodged` 加入 rebuildPlayerStats 的 rk 数组
  修复鬼市交换遗物后"墨移"闪避标记丢失导致召回失效的静默Bug
- 4处 `.closest()` 调用补 feature-detection 守卫（relic tooltip 右键/长按、武器选择点击/悬停）
  极罕见浏览器（无 Element.closest 支持）上防止 TypeError
- `quitToTitle` 中清理 `.relic-tooltip` 残留 DOM 元素（之前仅在 CSS hidden，从未 remove）

**审计确认无问题（20+项）：**
- 状态机6条路径全部安全，无卡死/重入风险
- 事件处理无泄漏（onclick覆盖+popup样式守卫）
- 数组splice全部使用反向迭代，无越界
- 除法全部有零值守卫或`||1`兜底
- DOM 访问全部有 null 检查
- 56个遗物boolean标记 + 全属性 setters 100% 在 game.js 中有读取点
- rebuildPlayerStats 正确定义 rk(35项) + ck(12项) 防止状态泄漏

**GitHub Pages 兼容性确认：**
- 所有脚本/CSS/链接使用相对路径 ✓
- index.html 作为着陆页 ✓
- 无需构建步骤 ✓
- 无需外部 API 依赖 ✓

**测试：** 157项全通过（37 smoke + 5 wave + 105 content + 10 stress）

*v2.29 稳定版 更新于 2026-05-07。（共 160 项测试）*

### v3.0 候选版 — 移动端渲染+UI可读性修复 (2026-05-07)

**目标：** 彻底解决移动端摇杆可见性、Canvas文本可读性、字体栈和UI对比度问题。

**移动端摇杆渲染管线修复：**
- `_renderMobileControls` 添加 `c.save()`/`c.restore()` 隔离Canvas状态
  防止前方渲染（雾/魂球/环境事件)泄漏fillStyle/strokeStyle/lineWidth/shadowBlur到摇杆绘制

**Canvas文本可读性（最小字号提升）：**
- 精英能力标签(敌人上方): 9px→11px (bold) — 移动端从3.5→4.3物理px
- 腐蚀伤害数字(敌人上方): 8px→10px (bold) — 移动端从3.1→3.9物理px
- Buff图标标签(玩家附近): 8px→10px (bold) — 移动端从3.1→3.9物理px

**字体栈修复（gamedata.js C.fontBody）：**
- 移除末尾重复泛型 `,serif`（`sans-serif`已是最后的有效回退）

**CSS对比度修复：**
- 闪避按钮冷却态: color alpha 0.3→0.5, border alpha 0.15→0.25

**测试：** 160项全通过（37 smoke + 5 wave + 108 content + 10 stress）

*v3.0 候选版 更新于 2026-05-07。（共 160 项测试）*

### v3.0 稳定版 (2026-05-07)

**移动端摇杆 CTRL 未初始化 Bug 修复：**
- 根因：`var CTRL = {}`（L54）在 `fitCanvas()`（L46）之后初始化。`fitCanvas` 调用 `updateControlSizes` 写 `CTRL.STICK_R` 时 `CTRL` 为 `undefined`
- 修复：`var CTRL = {}` 移至第28行（`fitCanvas` 定义之前）
- 症状：`TypeError: Cannot set properties of undefined (setting 'STICK_R')`

**桌面端误触发修复：**
- `isTouch` 移除 `'ontouchstart' in window` 和 `maxTouchPoints`，仅用 UA 正则判断移动设备

**技术债务清理建议 (TODO)：**
- mobile-controls.js 347行 → 可拆分为渲染/输入/初始化三个模块
- ~~`isNative` Capacitor 检测与 `isTouch` UA检测有重叠，可统一为单一 `needsMobileUI()` 函数~~（v3.0.1 已完成）
- game.js ~3800行 → 可考虑拆分为 state/combat/render/ui 四个文件
- 测试从 Node mock 迁移到 Playwright 浏览器测试以覆盖真实渲染

**已知限制：**
- 部分 Capacitor WebView 的 UA 不含 Android 关键词，依赖 `isNative` 优先检测

**测试：** 160项全通过

*v3.0 稳定版 更新于 2026-05-07。*

### v3.0.1 热修 — 移动端输入隔离 (2026-05-07)

**Bug：** APK 操控调试时，`game.js` 在 `startGame()` 中只要发现 `__forceMobileInit()` 就会强制初始化摇杆；而 `mobile-controls.js` 在所有网页环境都会暴露该函数，导致桌面网页端也可能被切到 `_mobileInput` 模式，影响键鼠瞄准、按钮显示和暂停提示。

**修复：**
- 新增统一 `needsMobileUI()` 判定：仅 Capacitor native 或 URL 调试开关 `?mobile=1` / `?mobileControls=1` / `?controls=mobile` 启用移动UI
- `__forceMobileInit()` 增加环境守卫，非移动UI环境直接拒绝初始化
- 移除网页端首次 `touchstart` 懒初始化和 2 秒无条件兜底初始化，防止触屏笔记本/移动浏览器误进 APK 摇杆层
- `startGame()` 只有在 `IS_TOUCH` 为真时才触发摇杆兜底初始化
- 移动按钮显示改为 `body.is-mobile-ui .mobile-btn`，不再依赖 `(hover:none)` 或 `maxTouchPoints`

**测试覆盖：**
- content_test 新增移动输入隔离静态回归，防止强制初始化入口再次污染网页端
- 当前 161 项测试通过（37 smoke + 5 wave + 109 content + 10 stress）

**已知限制：**
- 普通移动网页默认回退到原有轻触鼠标模式；需要调试 APK 摇杆时可在浏览器 URL 加 `?mobile=1`

*v3.0.1 热修 更新于 2026-05-07。*

### v3.0.2 热修 — 移动输入诊断 + 闪避优化 (2026-05-07)

**目标：** 让 APK 真机更容易排查输入问题，同时把闪避按钮从“状态型开关”收口成“一次性请求”。

**改动：**
- debug 面板新增移动端状态行，显示 `mobileUI`、`dx/dy`、`aim`、`lastAimMode`、`dodgeRequest`
- 右摇杆松手后保留最后瞄准角，避免回到鼠标方向造成瞬时抖动
- 闪避按钮改用 `requestMobileDodge()` 一次性请求，`update()` 统一消费并清零
- `mobile-controls.js` 的输入对象增加 `dodgeRequest` / `leftActive` / `rightActive` / `lastAimMode`

**测试：**
- content_test 新增移动输入诊断和闪避请求静态回归
- 当前 162 项测试全通过（37 smoke + 5 wave + 110 content + 10 stress）

*v3.0.2 热修 更新于 2026-05-07。*

### v3.0.3 热修 — 移动端 UI 抛光 (2026-05-07)

**目标：** 让 APK 端的暂停/闪避按钮更像明确的触控控件，并给 HUD 留出更干净的顶部空间。

**改动：**
- `mobilePauseBtn` / `mobileDodgeBtn` 增加 `aria-label`
- `body.is-mobile-ui` 下的移动按钮改为更大的方形控件，带阴影和毛玻璃质感
- 移动端 HUD 顶部、遗物条、BUFF 条、Boss 名称重新收边，减少按钮和状态文本互相挤压

**测试：**
- 延续 v3.0.2 的 162 项测试结果，新增 HTML/CSS 静态回归后累计 163 项

*v3.0.3 热修 更新于 2026-05-07。*

### v3.1.1 架构整理 — 输入契约 + 运行时预留 (2026-05-08)

**目标：** 在不改玩法、不改数值、不重做视觉的前提下，把后续长期迭代的架构入口固定下来，避免移动端、桌面端、渲染和 UI 继续互相污染。

**架构文档：**
- 新增 `ARCHITECTURE.md`
- 明确 `data / input / core / render / ui / platform` 六层边界
- 补充脚本加载顺序、测试体系、关键不变量
- 新增“预留插槽”章节，定义未来扩展默认接法

**统一输入帧：**
- `game.js` 新增 `buildInputFrame(g)`
- 桌面键鼠和移动摇杆统一输出 `dx / dy / aimAngle / attacking / dodgeQueued / isMobile`
- `update()` 只读取输入帧，不再直接散读 `keys`、`mouse`、`_mobileInput`

**运行时命名空间预留：**
- 新增 `window.MOSUI`
- 预留 `MOSUI.hooks / input / platform / profiles / ui / debug`
- 暴露 `MOSUI.input.buildFrame` 和 `MOSUI.input.lastFrame`
- 移动端挂入 `MOSUI.input.mobile`
- 移动端平台能力挂入 `MOSUI.platform.mobileControls`
- 移动控制参数读取口挂入 `MOSUI.profiles.control.mobile`

**移动端架构整理：**
- `mobile-controls.js` 增加 section 标记
- 明确 platform detection、canvas sizing、joystick math、auto-aim、touch bridge、rendering、input state、lifecycle 的职责边界
- 保留现有 `_mobileInput`、`_tickMobileAutoAim`、`_renderMobileControls` 兼容入口

**测试：**
- `content_test.js` 新增架构回归：
  - 155：统一输入帧、section marker、模块边界
  - 156：`MOSUI` 命名空间和扩展插槽
- 当前 187 项测试通过（37 smoke + 5 wave + 135 content + 10 stress）

**测试稳定性：**
- 修复 `wave_test.js` 在 survival 随机波次下的偶发误报：`killAll()` 等待波次结算期间若刷出新怪，会继续清理后再判断。

*v3.1.1 架构整理 更新于 2026-05-08。（共 187 项测试）*

### v4.26 Agent 自动化封板 (2026-05-10)

**目标：** 在导入 ccswitch 前，把主 Claude + 专职 agent 的内容生产线固定成可复用规则，减少后续加遗物/敌人/成就时反复翻 6 个文件的成本。

**自动化入口：**
- 新增 `AGENT_SYSTEM.md`，作为 agent 系统总纲。
- `.claude/ctx-extract.js` 作为事实源，输出当前内容数量、标签缺口、最大测试号、关键代码片段。
- `.claude/validate-agent-output.js` 校验 agent 原始输出和合并后输出，拦截 ES6 语法、错误测试编号、直接 push、直接扣血等高危模式。
- `package.json` 新增 `ctx`、`test:syntax`、`test:all`、`validate:agent` 脚本。

**文档收口：**
- `DEVELOPMENT.md` 更新为 v4.26 开发规范，加入 agent 合并检查表和当前内容体量。
- `ROADMAP.md` 更新为 v4.26+，下一阶段聚焦自我迭代流水线。
- `ARCHITECTURE.md` 更新为 v4.26，新增 Automation 层，移除易漂移的旧行号依赖。
- `README.md` 更新当前内容数量和测试命令。

**已知待处理：**
- `content_test.js` 的历史 summary 文本与实际 `// Test N:` 块存在漂移，下一轮应优先修正。
- validator 目前以风格和高危模式拦截为主，下一轮可升级为缺字段/缺图标/缺权重提示。

**测试：**
- `npm run ctx`
- `npm run test:all`

*v4.26 Agent 自动化封板 更新于 2026-05-10。*

### v4.27 ~ v4.28 方案 B 流水线定型 (2026-05-10)

**背景：** v4.26 跑了几轮真实内容生产，暴露并发瓶颈：agent 在同一 worktree 修文件，patch 锚点冲突率 100%（2 并发必撞一次 content_test 末尾或 gamedata 数组末尾）。

**方案演进：**
- **方案 A (worktree 隔离)**：每个 agent 独立 worktree 改文件 + 跑测试，主 Claude `git apply` 合并。落地后冲突率仍 100%，因为 apply 对上下文锚点敏感，第二个 patch 必拒。
- **方案 B (block merger)**：agent 只输出 JSON block（任务 id + 数据条目 + 字段 + css + test_lines），主 Claude 跑 `merge-content-blocks.js` 统一按 test_id 升序插入。彻底消除并发冲突。

**新基础设施：**
- `.claude/sequencer.js`：发号器。`reserve/release/commit/list`，统一分配 task_id + test_id + lease 文件，禁止 agent 自选。状态 `.claude/state/sequencer.json`。
- `.claude/fix-test-count.js`：扫 content_test 里所有 `// Test N:` 独特 id，自动校准 `ALL N TESTS PASSED`。解决人肉维护 count 漂移。
- `.claude/merge-content-blocks.js`：合并器。支持 relic / evolution 两种 type；relic 可带 player_fields / ck_fields / css_rules / mechanic_insertions（含 anchor 定位）。按 test_id 升序插入。
- `.claude/worktree-manager.js`：git worktree 辅助（create/finish/cleanup/list）。方案 A 用，方案 B 不依赖。
- `.claude/agents/content-executor.md`：方案 B 的 executor prompt 模板。agent 只生成 JSON，不碰源文件。
- content_test.js 新增 `testRelicStat` helper：一行调用替代 10 行 try/errors.push，老测试保留向后兼容。

**数据实测（14 批 40+ 项）：**

| 方案 | 批次 | 并发 | 冲突 apply | 主 Claude 手修 |
|------|------|------|-----------|--------------|
| A (worktree) | 批 1-3，6 项 | 2 | 3/3 (100%) | 14 Edit |
| B (merger) | 批 4-14，34 项 | 3-5 | 0/11 (0%) | 0 |

方案 B 在并发 5 下仍 0 冲突 0 手修，agent 单项 tool call 从 11-15 降到 3，tokens 从 46k 降到 38k。

**内容产出：**
- 遗物 132 → 170（+38，其中 19 在批 1-14）
- 进化 25 → 30（+5）
- 冷标签 12 组全部填满 ✅
- 测试 187 → 201 全绿（smoke 37 + wave 5 + content 201 + stress 10 = 253）

**已知保留：**
- wave_test 偶发 flake（molizexi 分裂时序），和本轮改动无关，下一轮单治。

**测试：**
- `npm run test:all`
- `node .claude/sequencer.js list`
- `node .claude/fix-test-count.js`

*v4.27~v4.28 方案 B 流水线定型 更新于 2026-05-10。*

### v4.31 自动化治理与发展说明 (2026-05-10)

**目标：** 在高并发内容生产后，把方案 B 从“能跑”收束成“可审计、可测试、可继续扩张”的工程系统。

**新增自动化：**
- `.claude/content-block-rules.js`：方案 B JSON block 共享门禁，供 merger 和 fixture 测试复用。
- `.claude/test-content-block-fixtures.js`：好/坏 block 样例测试，覆盖缺 `.ink-icon`、乱用 CSS var、`assert()`、重复测试号。
- `.claude/audit-content-invariants.js`：跨文件内容巡检，默认 report-only；发布前可加 `--strict`。
- `.claude/AUTOMATION_GUIDE.md`：统一解释 skill / validator / sequencer / merger / fixture / audit 的职责和命令流。

**脚本：**
- 新增 `npm run test:block-fixtures`
- 新增 `npm run test:automation`
- 新增 `npm run audit:content`
- `npm run test:syntax` 纳入新的 `.claude` 自动化脚本语法检查

**当前巡检结果：**
- `audit:content` 报告 `molielian` 缺 CSS 图标。
- `moyong` / `morui` 未出现在 WAVE_TIERS。
- RELIC_RULES 覆盖为 40/180，后续内容治理应优先提升。

**测试：**
- `npm run test:block-fixtures`
- `npm run audit:content`

*v4.31 自动化治理 更新于 2026-05-10。*

### v4.32 内容治理 + block 门禁补强 (2026-05-10)

**内容治理结果：**
- 遗物 180 → 186。
- `molielian` CSS 图标补齐。
- `moyong` / `morui` 加入 WAVE_TIERS。
- RELIC_RULES 覆盖 40 → 75。
- 冷标签“召唤 / 冲刺 / 范围”从 1 各补到 3。

**自动化修复：**
- `.claude/content-block-rules.js` 新增 `test_lines` 字符串数组项尾逗号检查。
- 新增 `console_log` 包装检查，必须形如 `"'  console.log(\"...\");',"`。
- `.claude/agents/content-executor.md` 同步修正 JSON block 模板。
- 新增坏样例 fixture：缺 test line 逗号、坏 console_log 包装。

**测试：**
- `npm run test:block-fixtures`
- `npm run test:automation`
- `npm run audit:content` → 0 errors / 0 warnings

*v4.32 内容治理 更新于 2026-05-10。*

### v4.33 测试硬化首批 (2026-05-10)

**新增测试文件：**
- `robust_test.js`：186 遗物 × 5 武器 = 930 组合 fn 可执行；38 进化全可执行；30件/全量组合叠加不抛异常；rebuild 3x 一致性。
- `seeded_test.js`：mulberry32 PRNG 替换 `Math.random` 实现确定性；5武器×同种子→相同 snapshot；5武器×1800帧长跑无崩溃。
- `visual_smoke_test.js`：Node http server + 无头 Chromium，走流程 首页→武器→战斗，5断言：title/武器/canvas/非空/无console错。依赖 `playwright@1.58`（匹配本机 chromium-1208），自动探测 `%LOCALAPPDATA%\ms-playwright\`。

**stress_test 扩展：**
- Test 11 帧时间 P99 预算：1200帧 combined-pressure 场景，预算 avg<3ms / p95<8ms / p99<16ms（60fps 帧时 16.7ms）；实测 avg=0.03ms / p95=0.08ms / p99=0.20ms，远低于预算。

**门禁：**
- `audit:content --strict` 接入 `test:all`，ERROR 非 0 直接阻塞。
- `test:all` 从 269 扩到 283 项。

**Bug 修复：**
- `jiuzhuanmofu`（九转墨符）latent bug：`nineSealCount:0` / `nineSealReady:false` 默认值与 fn 设置值相同，导致不持有遗物也永远触发"每命中6次AOE爆发"机制。改为 `hasNineSeal` 标志位，mkPlayer 加字段、ck 加入、机制入口 `if(p.hasNineSeal)` 判断。
- `wave_test.killAll` flake：`moyong` 死亡孵化 `morui` 的帧延迟让 alive 敌人在 state 切换后残留。改为两阶段清场——phase 1 循环到 state 切换，phase 2 额外 60 帧 drain 新生敌人。5/5 test:all 连跑零 flake。

**测试基线变化：**
- v4.32: 275 项（smoke 37 + wave 5 + content 223 + stress 10）
- v4.33: 283 项（smoke 37 + wave 5 + content 223 + stress 11 + robust 4 + seeded 3 + strict audit）

*v4.33 测试硬化首批 更新于 2026-05-10。*

### v4.34 冷标签补齐 + RELIC_RULES 扩容 (2026-05-11)

**内容（5 条遗物）：**
- `mozhaojun`（墨召魂引，召唤/击杀）— 击杀 25% 召唤墨魂，机制接 `onEnemyKilled`（方案 A 单项）。
- 方案 B 批量 4 条：`mosufeng`（攻速/暴击）、`mofanyu`（范围/控场）、`mokuaihun`（攻速/魂）、`mochongguang`（冲刺/暴击）。纯 stats 加成，零机制代码。

**RELIC_RULES：** 75/186 → 86/191，含 v4.32 6 条 + v4.34 5 条新遗物权重。

**管线发现：**
- HTML entity 复发：4/4 方案 B agent 全部把 `<` 输出成 `&lt;`，即便 prompt 特别告警也无效。确认是输出通道固有 bug，只能靠 `fix:entities` 和 block rules 兜底。用户后续加 `html-entity-test-lines.json` fixture。
- CSS `border-left/right/bottom` 被 block-rules 拦截（正确行为），agent 没预见。主 Claude 改成 `clip-path:polygon(...)` 绕过。

**测试基线：** 283 → 288 项（content 223 → 228）；遗物 186 → 191。

*v4.34 冷标签补齐 + RELIC_RULES 扩容 更新于 2026-05-11。*

### v5.0-prep 测试与构筑吸收 (2026-05-11)

**视觉冒烟扩展 7→10 项：**
- Test 6/7：Escape 通过 `window.dispatchEvent` 触发 pauseOverlay；resumeBtn click 关闭。
- Test 8/9/10：移动端 viewport 375x812 竖屏 + 812x375 横屏；无 mobile console 错。
- 修 Test 3 真实 bug：weapon-pick 后先弹 cursePopup 才进 playing，原测试没处理导致 G 一直 null（所以 pause 测试永远失败）。补 `#cursePopup .curse-skip` click 流程。

**构筑审计（只读）：** 统计 191 遗物按 tag 分布——冷标签召唤/冲刺/范围各 4 件；但 BUILD_PREFS 覆盖已不缺件，真问题是 RELIC_RULES 覆盖 86/191 = 45%，105 件缺构筑权重——"有数量没构筑入口"。

**RELIC_RULES 扩容 86 → 106：** 按 V5_PREP 原则"内容是否被系统吸收"，补 20 条构筑权重：
- 召唤核心 6 条：`molingyu/molingqi/baomo/yumo/hanmo/fenmo`（召魂幡流派基石）
- 击杀链 4 条：`lianhuanfu/mogongzhen/yanmoyan/mowo`
- 控场墨阵 5 条：`moshouzhen/zhenyan/molian/moxian/moding`
- 生存防御 5 条：`zhenmu/zhifan/motiebi/molonglin/moshouren`

**下一步方向：** 继续 RELIC_RULES 扩容 106 → 130+ / 流派成型审计 / Boss 里程碑。

*v5.0-prep 测试与构筑吸收 更新于 2026-05-11。*

### 版本号规则

改完一个bug → 删掉对应条目 → 版本号末尾+1
