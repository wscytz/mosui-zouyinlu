# v3.0 代码审计 — 待修清单

## 🔴 功能Bug
_（暂无待修功能bug，等待手机实测）_

## 🟡 代码质量

### Q1 — 死变量 `_rafId` ✅ 已修复
- game.js:3628， requestAnimationFrame 返回值存了但从未读取
- → 移除 `_rafId` 变量及两处赋值

### Q2 — `C.fontBody` 未充分使用（暂缓）
- gamedata.js 定义了 `C.fontBody` 但 game.js 中仅 3 处使用
- 25+ 处重复硬编码，替换工作量大、风险低、暂缓

### Q3 — 魔法颜色字符串 ✅ 已修复
- `"#f7efe5"` → `C.ivory`（12处替换）
- `"rgba(0,0,0,0)"` → `C.clear`（4处替换）
- `"rgba(163,58,45,...)"` 保留（alpha值各异，无法统一替换）

### Q4 — 重复触摸检测 ✅ 已修复
- → 提取为模块级 `IS_TOUCH` 常量，3处引用统一

### Q5 — 向量归一化重复模式（低风险）
- `Math.sqrt(dx*dx+dy*dy)||1` 后跟除法，10 处相同逻辑
- 抽取有风险（每处变量名不同），暂时保留

### Q6 — 彩蛋成就 meta 写入但未展示（中风险）
- 8 个成就 meta 属性写入 localStorage 但 end 画面不展示
- `noMoveWins`, `lowHurtWins`, `fastWaveClears`, `paperWins`, `noRelicWins`, `bestSingleRunKills`, `perfectBossKills`, `noEvolveWins`
- `bestFireCoverage` 有消耗者（ash_road 成就），其余 8 个没有

## 🟢 正面发现
- 无死函数
- 无不可达代码
- 无内存泄漏（已修）
- 数组操作全部安全
- 除法全部有零值守卫

---

## mobile-controls.js 审计

### M1 — 死变量 `CW` `CH` ✅ 已修复
- → `var CRATIO = 960/640;` 直接计算

### M2 — 画布尺寸常量重复 ✅ 已修复
- → 移除 CW/CH，仅保留 W/H

### M3 — 冗余 `updateControlSizes()` 调用 ✅ 已修复
- → 移除第87行重复调用

### M4 — 冗余 `getElementById` ✅ 已修复
- → `canvasCoord` 改用闭包中的 `cvs`

### M5 — 冗余 `var canvas` ✅ 已修复
- → 事件绑定直接用 `cvs`
