# v3.0 代码审计 — 待修清单

> 历史归档：本文只记录 v3.0 审计结果，不作为当前 Bug 清单。当前 Bug 和版本记录以 `DEVDOC.md` 末尾为准。

## 🔴 功能Bug
_（暂无待修功能bug，等待手机实测）_

## 🟡 代码质量

### Q1 — 死变量 `_rafId` ✅ 已修复
- game.js:3628， requestAnimationFrame 返回值存了但从未读取
- → 移除 `_rafId` 变量及两处赋值

### Q2 — `C.fontBody` 未充分使用（有意保留）
- 硬编码字体栈与 C.fontBody 不同（`Kaiti SC,serif` vs `Noto Sans,sans-serif`）
- 盲目替换可能改变渲染，需逐处审核

### Q3 — 魔法颜色字符串 ✅ 已修复
- `"#f7efe5"` → `C.ivory`（12处）、`"rgba(0,0,0,0)"` → `C.clear`（4处）
- `"rgba(163,58,45,...)"` 保留（alpha值各异）

### Q4 — 重复触摸检测 ✅ 已修复
- → 模块级 `IS_TOUCH` 常量，3处引用统一

### Q5 — 向量归一化重复（有意保留）
- 10处变量名不同，`||1` 守卫是关键安全逻辑，抽取风险>收益

### Q6 — 彩蛋meta未展示（有意保留）
- 8属性写入localStorage但无UI消费，需设计展示方案

## 🟢 正面发现
- 无死函数
- 无不可达代码
- 无内存泄漏（已修）
- 数组操作全部安全
- 除法全部有零值守卫

---

## mobile-controls.js 审计

### M1 — 死变量 `CW` `CH` ✅ 已修复
- → `var CRATIO = 960/640;` 直接计算

### M2 — 画布尺寸常量重复 ✅ 已修复
- → 移除 CW/CH，仅保留 W/H

### M3 — 冗余 `updateControlSizes()` 调用 ✅ 已修复
- → 移除第87行重复调用

### M4 — 冗余 `getElementById` ✅ 已修复
- → `canvasCoord` 改用闭包中的 `cvs`

### M5 — 冗余 `var canvas` ✅ 已修复
- → 事件绑定直接用 `cvs`
